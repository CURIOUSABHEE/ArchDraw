import type { TutorialData } from './index';

export const youtubeTutorial: TutorialData = {
  id: 'youtube-architecture',
  title: 'How to Design YouTube Architecture',
  description: "Design the world's largest video platform. Learn video upload pipelines, transcoding at scale, CDN distribution, and recommendation ML serving billions of views.",
  difficulty: 'Advanced',
  category: 'Video Streaming',
  estimatedTime: '~32 mins',
  nodeCount: 11,
  stepCount: 11,
  icon: 'Video',
  color: '#ff0000',
  tags: ['Transcoding', 'CDN', 'Pipeline', 'ML', 'Storage'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "YouTube's client is the web browser and mobile app. It handles adaptive bitrate video playback, switching quality automatically based on your connection speed every 10 seconds.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "The client's adaptive bitrate player is what makes YouTube work on both 4K TVs and slow mobile connections. Understanding the client shapes every downstream architectural decision.",
      messages: [
        { type: 'guide', content: "Welcome to the YouTube Architecture tutorial. 2.7 billion users, 500 hours of video uploaded every minute." },
        { type: 'guide', content: "YouTube's client handles adaptive bitrate playback — it monitors your bandwidth every 10 seconds and switches video quality automatically. That quality switch is a new CDN request to a different file segment." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Client added. Now the delivery layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "YouTube's client switches video quality every 10 seconds. How does adaptive bitrate streaming work?",
      defaultSuggestions: ["What is adaptive bitrate streaming?", "What is DASH?", "How does YouTube handle buffering?"],
    },
    {
      id: 2,
      title: 'Add CDN for Video Delivery',
      explanation: "YouTube's CDN serves video segments to viewers. It caches the most popular videos at edge locations worldwide. A video that goes viral is served from edge nodes, never hitting YouTube's origin servers.",
      action: 'Press ⌘K, search for "CDN", and add it to the canvas. Then connect Client → CDN.',
      searchHint: 'CDN',
      why: "Without a CDN, every video request would travel to Google's data centers. At 1 billion hours of video watched daily, that would be impossible. CDNs make global video delivery economically viable.",
      messages: [
        { type: 'guide', content: "1 billion hours of video watched daily. How does YouTube serve all that without crushing their servers?" },
        { type: 'guide', content: "The CDN caches popular videos at edge locations worldwide. When a video goes viral, it's served from the nearest edge node — never hitting YouTube's origin. Cache hit rates for popular content exceed 95%." },
        { type: 'guide', content: 'Press ⌘K, search for "CDN", add it, then connect: Client → CDN.' },
      ],
      validation: {
        requiredNodes: ['cdn'],
        requiredEdges: [{ from: 'Web', to: 'CDN' }],
        successMessage: "CDN added and connected. Now the API layer.",
        errorMessage: 'Add a CDN and connect it from the Client.',
      },
      openingMessage: "YouTube's CDN serves 1 billion hours of video daily. Connect Client → CDN — video bytes flow from the user to the nearest edge node.",
      defaultSuggestions: ["How does CDN caching work for video?", "What is a cache hit rate?", "How does YouTube handle cache invalidation?"],
    },
    {
      id: 3,
      title: 'Add API Gateway',
      explanation: "All non-video requests — search, comments, likes, subscriptions, recommendations — flow through the API Gateway. It routes requests to the correct microservice and enforces rate limits.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas. Then connect Client → API Gateway.',
      searchHint: 'API Gateway',
      why: "YouTube has dozens of microservices. The API Gateway is the single entry point that abstracts this complexity and provides a consistent interface for the client.",
      messages: [
        { type: 'guide', content: "Video bytes go through the CDN. Everything else — search, comments, likes — goes through the API Gateway." },
        { type: 'guide', content: "The gateway routes requests to the right service: search queries go to the search service, comment posts go to the comment service, recommendation requests go to the ML service." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", add it, then connect: Client → API Gateway.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [{ from: 'Web', to: 'API Gateway' }],
        successMessage: "API Gateway added and connected. Now distribute the traffic.",
        errorMessage: 'Add an API Gateway and connect it from the Client.',
      },
      openingMessage: "The API Gateway routes all non-video requests. Connect Client → API Gateway alongside the CDN connection.",
      defaultSuggestions: ["What requests go through the API Gateway?", "How does YouTube handle search?", "What is request routing?"],
    },
    {
      id: 4,
      title: 'Add Load Balancer',
      explanation: "YouTube's API Gateway routes to a Load Balancer that distributes requests across thousands of application servers. YouTube handles over 500 million API requests per day.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas. Then connect API Gateway → Load Balancer.',
      searchHint: 'Load Balancer',
      why: "YouTube sees massive traffic spikes when popular videos are published. The load balancer enables horizontal scaling and ensures no single server becomes a bottleneck.",
      messages: [
        { type: 'guide', content: "500 million API requests per day need to be distributed across thousands of servers." },
        { type: 'guide', content: "The Load Balancer handles this distribution. When MrBeast posts a new video and millions of people click simultaneously, the load balancer spreads those requests across the fleet." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", add it, then connect: API Gateway → Load Balancer.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [{ from: 'API Gateway', to: 'Load Balancer' }],
        successMessage: "Load balancer added. Now the upload pipeline.",
        errorMessage: 'Add a Load Balancer and connect it from the API Gateway.',
      },
      openingMessage: "YouTube handles 500 million API requests per day. Connect API Gateway → Load Balancer to distribute the traffic.",
      defaultSuggestions: ["What is horizontal scaling?", "How does YouTube handle traffic spikes?", "What is auto-scaling?"],
    },
    {
      id: 5,
      title: 'Add Upload Service',
      explanation: "YouTube chunks large video files during upload. If your connection drops, the upload resumes from the last successful chunk. A 4K 1-hour video can be 50GB+ — chunked upload is essential.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas. Then connect Load Balancer → Upload Service (Microservice).',
      searchHint: 'Microservice',
      why: "Resumable chunked uploads are critical for large video files. Without chunking, a dropped connection on a 50GB upload would require starting over. The Upload Service manages chunk tracking and assembly.",
      messages: [
        { type: 'guide', content: "500 hours of video uploaded every minute. Each upload needs to be reliable — a 4K 1-hour video can be 50GB." },
        { type: 'guide', content: "The Upload Service uses chunked uploads. The video is split into chunks on the client. If the connection drops, only the last chunk needs to be re-sent. The service tracks which chunks have been received." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add it, then connect: Load Balancer → Upload Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Microservice' }],
        successMessage: "Upload Service added. Now the transcoding pipeline.",
        errorMessage: 'Add a Microservice (Upload Service) and connect it from the Load Balancer.',
      },
      openingMessage: "YouTube uses chunked uploads for large video files. Connect Load Balancer → Upload Service — that's where upload traffic is handled.",
      defaultSuggestions: ["What is chunked upload?", "How does YouTube handle upload failures?", "What is a resumable upload?"],
    },
    {
      id: 6,
      title: 'Add Transcoding Worker',
      explanation: "Every video is transcoded into 360p, 480p, 720p, 1080p, 1440p, 4K — plus VP9, H.264, AV1 codecs — that's 20+ versions per video. YouTube's encoding pipeline (Archer) uses per-title encoding for optimal quality.",
      action: 'Press ⌘K, search for "Worker / Background Job", and add it to the canvas. Then connect Upload Service → Transcoding Worker.',
      searchHint: 'Worker / Background Job',
      why: "Without transcoding, YouTube could only serve one quality level. The 20+ versions per video is what enables adaptive bitrate streaming across every device and connection speed.",
      messages: [
        { type: 'guide', content: "Every uploaded video needs to be transcoded into 20+ formats before it can be watched." },
        { type: 'guide', content: "YouTube's Archer pipeline uses per-title encoding — it analyzes each scene's complexity and allocates more bits to action sequences. This produces better quality at lower file sizes, saving hundreds of millions in CDN costs annually." },
        { type: 'guide', content: 'Press ⌘K, search for "Worker / Background Job", add it, then connect: Upload Service → Transcoding Worker.' },
      ],
      validation: {
        requiredNodes: ['worker_job'],
        requiredEdges: [{ from: 'Microservice', to: 'Worker' }],
        successMessage: "Transcoding Worker added. Now the storage layer.",
        errorMessage: 'Add a Worker / Background Job (Transcoding) and connect it from the Upload Service.',
      },
      openingMessage: "YouTube creates 20+ versions of every video. Connect Upload Service → Transcoding Worker — uploads trigger the transcoding pipeline.",
      defaultSuggestions: ["What is video transcoding?", "What is per-title encoding?", "What codecs does YouTube use?"],
    },
    {
      id: 7,
      title: 'Add Object Storage',
      explanation: "All raw and transcoded video files are stored in object storage. YouTube stores over 1 exabyte of video content — every title in every resolution, codec, and language combination.",
      action: 'Press ⌘K, search for "Object Storage", and add it to the canvas. Then connect Transcoding Worker → Object Storage.',
      searchHint: 'Object Storage',
      why: "Object storage is the only viable option for exabyte-scale video storage. It's infinitely scalable, durable, and cheap per GB — perfect for binary video files.",
      messages: [
        { type: 'guide', content: "After transcoding, all 20+ versions of every video need to be stored. That's object storage." },
        { type: 'guide', content: "YouTube stores over 1 exabyte of video — that's 1 million terabytes. Object storage (like Google Cloud Storage) handles this at 11 nines of durability. The CDN pulls from here to serve viewers." },
        { type: 'guide', content: 'Press ⌘K, search for "Object Storage", add it, then connect: Transcoding Worker → Object Storage.' },
      ],
      validation: {
        requiredNodes: ['object_storage'],
        requiredEdges: [{ from: 'Worker', to: 'Object' }],
        successMessage: "Object Storage added. Now the streaming service.",
        errorMessage: 'Add an Object Storage and connect it from the Transcoding Worker.',
      },
      openingMessage: "YouTube stores 1 exabyte of video. Connect Transcoding Worker → Object Storage — finished video files land here.",
      defaultSuggestions: ["What is object storage?", "How does YouTube organize video files?", "What is 11 nines durability?"],
    },
    {
      id: 8,
      title: 'Add Streaming Service',
      explanation: "The Streaming Service generates time-limited signed URLs for video segments. The CDN uses these URLs to fetch from Object Storage. Signed URLs expire after 60 seconds, preventing unauthorized sharing.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas. Then connect Object Storage → Streaming Service.',
      searchHint: 'Microservice',
      why: "Signed URLs are the elegant solution to video DRM — they grant temporary access to specific video segments without making files publicly accessible or proxying every byte through YouTube's servers.",
      messages: [
        { type: 'guide', content: "How does YouTube prevent people from downloading videos directly from storage? Signed URLs." },
        { type: 'guide', content: "The Streaming Service generates time-limited URLs for each video segment. The URL expires in 60 seconds — long enough to watch, too short to share. The CDN fetches segments using these URLs." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add another one for the Streaming Service, then connect: Object Storage → Streaming Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Object', to: 'Microservice' }],
        successMessage: "Streaming Service added. Now the recommendation engine.",
        errorMessage: 'Add a second Microservice (Streaming Service) and connect it from Object Storage.',
      },
      openingMessage: "YouTube uses signed URLs for video DRM. Connect Object Storage → Streaming Service — the streaming service gates access to video files.",
      defaultSuggestions: ["What is a signed URL?", "How does YouTube DRM work?", "What is adaptive bitrate streaming?"],
    },
    {
      id: 9,
      title: 'Add Recommendation Service',
      explanation: "YouTube's recommendation model is a two-stage system — first a candidate generation model narrows billions of videos to hundreds, then a ranking model scores those hundreds. 70% of watch time comes from recommendations.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas. Then connect Load Balancer → Recommendation Service.',
      searchHint: 'Microservice',
      why: "Recommendations drive 70% of YouTube's watch time. The two-stage architecture (candidate generation + ranking) is necessary because scoring all 800 million videos for every user would be computationally impossible.",
      messages: [
        { type: 'guide', content: "70% of what you watch on YouTube comes from recommendations, not search. That's the Recommendation Service." },
        { type: 'guide', content: "It's a two-stage system: first, a candidate generation model narrows 800 million videos to ~500 candidates for you. Then a ranking model scores those 500 using deep learning. The top results appear on your homepage." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add another one for the Recommendation Service, then connect: Load Balancer → Recommendation Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Microservice' }],
        successMessage: "Recommendation Service added. Now the metadata database.",
        errorMessage: 'Add a third Microservice (Recommendation Service) and connect it from the Load Balancer.',
      },
      openingMessage: "YouTube's two-stage recommendation system narrows 800M videos to 500 candidates. Connect Load Balancer → Recommendation Service.",
      defaultSuggestions: ["How does YouTube's recommendation work?", "What is candidate generation?", "What signals does YouTube use for ranking?"],
    },
    {
      id: 10,
      title: 'Add NoSQL Database',
      explanation: "YouTube stores video metadata (titles, descriptions, tags, view counts) and comments in a NoSQL database. View counts alone require billions of increments per day — NoSQL handles this write throughput.",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas. Then connect Upload Service → NoSQL Database.',
      searchHint: 'NoSQL Database',
      why: "Video metadata has a flexible schema — different video types have different fields. NoSQL's flexible document model handles this without requiring schema migrations every time YouTube adds a new video feature.",
      messages: [
        { type: 'guide', content: "Every video has metadata — title, description, tags, view count, likes. That's stored in a NoSQL database." },
        { type: 'guide', content: "View counts alone require billions of atomic increments per day. YouTube uses a counter service backed by NoSQL to handle this write throughput without locking rows in a SQL table." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", add it, then connect: Upload Service → NoSQL Database.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [{ from: 'Microservice', to: 'NoSQL' }],
        successMessage: "NoSQL Database added. Final step — observability.",
        errorMessage: 'Add a NoSQL Database and connect it from the Upload Service.',
      },
      openingMessage: "YouTube increments view counts billions of times per day. Connect Upload Service → NoSQL Database to persist video metadata.",
      defaultSuggestions: ["How does YouTube count views?", "What is a counter service?", "How does YouTube handle comment spam?"],
    },
    {
      id: 11,
      title: 'Add Observability',
      explanation: "YouTube monitors video start times, buffering rates, transcoding queue depth, and recommendation click-through rates in real time. A spike in buffering rate indicates CDN or transcoding issues.",
      action: 'Press ⌘K, search for "Metrics Collector", and add it to the canvas. Then search for "Logger" and add that too. Connect both from the Upload Service.',
      searchHint: 'Metrics Collector',
      why: "Video quality metrics directly impact user retention. A 1-second increase in video start time reduces views by 5%. Observability lets YouTube detect and fix quality degradation before users notice.",
      messages: [
        { type: 'guide', content: "Final step — observability. A 1-second increase in video start time reduces views by 5% at YouTube's scale." },
        { type: 'guide', content: "The Metrics Collector tracks video start times, buffering rates, and transcoding queue depth. The Logger captures detailed traces for debugging specific playback failures." },
        { type: 'guide', content: 'Press ⌘K, search for "Metrics Collector", add it. Then search for "Logger" and add that too. Connect both from the Upload Service.' },
      ],
      validation: {
        requiredNodes: ['metrics_collector', 'logger'],
        requiredEdges: [
          { from: 'Microservice', to: 'Metrics' },
          { from: 'Microservice', to: 'Logger' },
        ],
        successMessage: "🎉 Tutorial complete! You've designed YouTube's core architecture from scratch.",
        errorMessage: 'Add both Metrics Collector and Logger and connect them from the Upload Service.',
      },
      openingMessage: "A 1-second video start delay reduces views by 5%. Connect Upload Service → Metrics Collector and Upload Service → Logger.",
      defaultSuggestions: ["What metrics does YouTube track?", "What is video start time?", "How does YouTube detect CDN issues?"],
    },
  ],
};
