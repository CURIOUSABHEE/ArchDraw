import { step, level, tutorial, component, edge, msg } from '@/lib/tutorial/factories';
import { buildAction, buildFirstStepAction, buildCelebration, buildOpeningL1, buildOpeningL2, buildOpeningL3 } from '@/lib/tutorial/defaults';
import type { Tutorial } from '@/lib/tutorial/types';

const l1 = level({
  level: 1,
  title: 'The Foundation',
  subtitle: 'Build a working photo platform in 8 steps',
  description: 'The minimum viable Instagram — a system that can receive a photo, store it, and serve it globally. Both clients, CDN, API gateway, upload pipeline, and databases.',
  estimatedTime: '~15 mins',
  unlocks: 'Production Layer',
  contextMessage: "Let's build Instagram from scratch. Level 1 is the foundation — 8 components that handle photo uploads, serve content via CDN, and store it all. By the end you have a system that can actually receive a post and display it to users.",
  steps: [
    step({
      id: 1,
      title: 'Add Mobile Client',
      explanation: 'The Mobile Client is the iOS or Android app where users upload photos, scroll their feed, and interact with content. 95% of Instagram usage is mobile — it is the primary client.',
      action: buildFirstStepAction('Mobile'),
      why: 'Every architecture starts from the user. Instagram is fundamentally a mobile-first product — the mobile app handles 95% of all uploads.',
      component: component('client_mobile', 'Mobile'),
      openingMessage: buildOpeningL1('Instagram', 'Mobile Client', 'capture photos and videos, display the feed, stories, and reels, and handle all user interactions on iOS and Android', "This is the camera, the scroll, and the double-tap — everything the user touches is the mobile client.", 'Mobile'),
      celebrationMessage: buildCelebration('Mobile Client', 'nothing yet', 'Instagram has 2 billion monthly active users with 95% accessing via mobile app. Over 100 million photos and videos are uploaded daily from mobile devices alone.', 'Web Client'),
      connectingMessage: '',
      messages: [
        msg("Instagram's mobile app is one of the most downloaded apps in history. The iOS and Android versions are completely separate codebases but connect to the same backend."),
        msg("Press ⌘K and search for 'Mobile' to add the primary client to the canvas."),
      ],
      requiredNodes: ['client_mobile'],
      requiredEdges: [],
      successMessage: 'Mobile Client added. Now the Web Client.',
      errorMessage: 'Add a Mobile client node to the canvas using ⌘K → search "Mobile".',
    }),
    step({
      id: 2,
      title: 'Add Web Client',
      explanation: "Instagram's Web Client is the browser-based version used primarily for browsing and messaging — most users view content here but fewer than 5% upload from the web.",
      action: buildFirstStepAction('Web'),
      why: 'Instagram launched as mobile-only in 2010. The web version came 2 years later. Architecturally, mobile remains the primary client with web as a secondary entry point.',
      component: component('client_web', 'Web'),
      openingMessage: buildOpeningL1('Instagram', 'Web Client', 'provide a browser-based version of Instagram for viewing feeds and sending messages but with limited upload capability', "The web version is Instagram's secondary entrance — most people browse here but almost nobody posts from here.", 'Web'),
      celebrationMessage: buildCelebration('Web Client', 'nothing yet', "Instagram's web version gets 500 million visits monthly but accounts for less than 5% of uploads. Most web users are casual browsers not content creators.", 'CDN'),
      connectingMessage: '',
      messages: [
        msg("Instagram launched as mobile-only in 2010. The web version came 2 years later. This is why mobile is still the primary client architecturally."),
        msg("Press ⌘K and search for 'Web' to add the secondary client."),
      ],
      requiredNodes: ['client_web'],
      requiredEdges: [],
      successMessage: 'Web Client added. Now the CDN.',
      errorMessage: 'Add a Web client node using ⌘K → search "Web".',
    }),
    step({
      id: 3,
      title: 'Add CDN',
      explanation: 'The CDN serves photos, videos, and static assets from edge servers physically close to each user worldwide. Instead of fetching every photo from US servers, the CDN puts copies in every city.',
      action: buildAction('CDN', 'Mobile', 'CDN', 'photos and videos being served from the nearest edge server instead of a central origin'),
      why: "Without a CDN, a user in Mumbai would wait 800ms for each photo to load from US servers instead of 40ms from a local edge. Instagram serves over 100 petabytes of media daily.",
      component: component('cdn', 'CDN'),
      openingMessage: buildOpeningL1('Instagram', 'CDN', 'serve photos, videos, and static assets from edge servers physically close to each user worldwide', "Instead of fetching every photo from Instagram's servers in the US, the CDN puts copies everywhere — like having a mini Instagram warehouse in every city.", 'CDN'),
      celebrationMessage: buildCelebration('CDN', 'both clients', 'Instagram serves over 100 petabytes of media daily through its CDN. Without it a user in Mumbai would wait 800ms for each photo to load from US servers instead of 40ms from a local edge.', 'API Gateway'),
      connectingMessage: 'CDN is on the canvas. Connect Mobile Client → CDN first. Then connect Web Client → CDN. Both clients fetch all media through the CDN — photos, videos, stories, and profile pictures all come from the nearest edge server, not from Instagram\'s origin servers directly.',
      messages: [
        msg("Instagram uses multiple CDN providers — Meta's own CDN plus Akamai and Cloudflare as fallbacks. If one CDN has an outage another takes over automatically."),
        msg("Press ⌘K and search for 'CDN' and press Enter to add it, then connect both Mobile and Web clients to it."),
      ],
      requiredNodes: ['cdn'],
      requiredEdges: [edge('client_mobile', 'cdn'), edge('client_web', 'cdn')],
      successMessage: 'CDN added and connected to both clients. Now the API Gateway.',
      errorMessage: 'Add a CDN connected from both Mobile and Web clients.',
    }),
    step({
      id: 4,
      title: 'Add API Gateway',
      explanation: 'The API Gateway handles all non-media API requests — feed loading, post creation, likes, comments, follows — and routes them to the correct services.',
      action: buildAction('API Gateway', 'Mobile', 'API Gateway', 'all API requests being routed through a single controlled entry point'),
      why: "The CDN handles media files. The API Gateway handles everything else — every tap, every like, every comment goes through here. Instagram uses GraphQL since 2012, one of the earliest large-scale adoptions.",
      component: component('api_gateway', 'API Gateway'),
      openingMessage: buildOpeningL1('Instagram', 'API Gateway', 'handle all non-media API requests — feed loading, post creation, likes, comments, follows — and route them to the correct services', "The CDN handles media files. The API Gateway handles everything else — every tap, every like, every comment goes through here.", 'API Gateway'),
      celebrationMessage: buildCelebration('API Gateway', 'both clients', "Instagram's API Gateway processes over 500,000 API requests per second at peak. It enforces rate limits, handles GraphQL query parsing, and routes to 100+ downstream microservices.", 'Load Balancer'),
      connectingMessage: 'API Gateway is on the canvas. Connect Mobile Client → API Gateway first. Then connect Web Client → API Gateway. Both clients send API calls — feed requests, post actions, profile updates — through the same gateway. Only media delivery goes through the CDN instead.',
      messages: [
        msg("Instagram uses GraphQL for its API since 2012 — one of the earliest large-scale GraphQL adoptions. This lets the mobile app request exactly the fields it needs, reducing data transfer by 30-40%."),
        msg("Press ⌘K and search for 'API Gateway' and press Enter to add it, then connect both clients to it."),
      ],
      requiredNodes: ['api_gateway'],
      requiredEdges: [edge('client_mobile', 'api_gateway'), edge('client_web', 'api_gateway')],
      successMessage: 'API Gateway added and connected to both clients. Now the Load Balancer.',
      errorMessage: 'Add an API Gateway connected from both Mobile and Web clients.',
    }),
    step({
      id: 5,
      title: 'Add Load Balancer',
      explanation: 'The Load Balancer distributes the 500,000 requests per second across thousands of application servers so no single server gets overwhelmed. Like a maitre d\' at a restaurant with 100 tables.',
      action: buildAction('Load Balancer', 'API Gateway', 'Load Balancer', 'API traffic being distributed across thousands of servers so no single server gets overwhelmed during traffic spikes'),
      why: "When Beyoncé posts a photo, Instagram experiences a traffic spike 10x the normal load within seconds. The load balancer automatically distributes this across the server fleet without any manual intervention.",
      component: component('load_balancer', 'Load Balancer'),
      openingMessage: buildOpeningL1('Instagram', 'Load Balancer', 'distribute the 500,000 requests per second across thousands of application servers so no single server gets overwhelmed', "Like a maitre d' at a restaurant with 100 tables — they direct each incoming guest to an available table rather than everyone crowding the same table.", 'Load Balancer'),
      celebrationMessage: buildCelebration('Load Balancer', 'API Gateway', "When Beyoncé posts a photo, Instagram experiences a traffic spike 10x the normal load within seconds. The load balancer automatically distributes this across the server fleet without any manual intervention.", 'Upload Service'),
      connectingMessage: 'Load Balancer is on the canvas. Connect API Gateway → Load Balancer. Hover over API Gateway and drag to Load Balancer. All API traffic is now distributed across Instagram\'s server fleet before reaching any specific service.',
      messages: [
        msg("Instagram uses Layer 7 load balancing — it reads the request content to route different endpoints to specialized server pools. Upload requests go to upload servers, feed requests to feed servers."),
        msg("Press ⌘K and search for 'Load Balancer' and press Enter to add it, then connect API Gateway → Load Balancer."),
      ],
      requiredNodes: ['load_balancer'],
      requiredEdges: [edge('api_gateway', 'load_balancer')],
      successMessage: 'Load Balancer added and connected. Now the Upload Service.',
      errorMessage: 'Add a Load Balancer connected from the API Gateway.',
    }),
    step({
      id: 6,
      title: 'Add Upload Service',
      explanation: 'The Upload Service receives photo and video uploads from the mobile client, validates file types and sizes, and hands off to the media processing pipeline. Think of it as the receiving dock at a warehouse.',
      action: buildAction('Upload Service', 'Load Balancer', 'Upload Service', 'upload requests being routed specifically to the upload service for processing'),
      why: 'The Upload Service does not process the media itself — it just receives, validates, and stores the raw file. Processing happens asynchronously in a separate pipeline to avoid blocking the upload.',
      component: component('upload_service', 'Upload Service'),
      openingMessage: buildOpeningL1('Instagram', 'Upload Service', 'receive photo and video uploads from the mobile client, validate file types and sizes, and hand off to the media processing pipeline', "Think of it as the receiving dock at a warehouse — it accepts incoming packages, checks they are valid, and sends them to the right processing station.", 'Upload Service'),
      celebrationMessage: buildCelebration('Upload Service', 'Load Balancer', "Instagram's Upload Service handles files up to 4GB for Reels. It uses chunked upload — a 1GB video is split into 5MB chunks. If the connection drops, upload resumes from the last successful chunk rather than starting over.", 'Object Storage'),
      connectingMessage: 'Upload Service is on the canvas. Connect Load Balancer → Upload Service. Hover over Load Balancer and drag to Upload Service. Upload requests are now routed specifically to the upload service — not mixed with feed or profile requests.',
      messages: [
        msg("The Upload Service does not process the media itself — it just receives, validates, and stores the raw file. Processing happens asynchronously in a separate pipeline to avoid blocking the upload."),
        msg("Press ⌘K and search for \"Upload Service\" and press Enter to add it, then connect Load Balancer → Upload Service."),
      ],
      requiredNodes: ['upload_service'],
      requiredEdges: [edge('load_balancer', 'upload_service')],
      successMessage: 'Upload Service added and connected. Now the Object Storage.',
      errorMessage: 'Add an Upload Service connected from the Load Balancer.',
    }),
    step({
      id: 7,
      title: 'Add Object Storage',
      explanation: 'Object Storage stores every photo and video ever uploaded to Instagram — billions of files at petabyte scale. An infinite hard drive that never fills up and never loses a file.',
      action: buildAction('Object Storage (S3-like)', 'Upload Service', 'Object Storage', 'raw photo and video files being stored durably and served to the CDN on demand'),
      why: 'Photos are binary blobs — they do not belong in a database. Object storage is infinitely scalable and designed for large binary files. Meta built Haystack specifically for Instagram\'s scale.',
      component: component('object_storage', 'Object Storage (S3-like)'),
      openingMessage: buildOpeningL1('Instagram', 'Object Storage', 'store every photo and video ever uploaded to Instagram — billions of files at petabyte scale', "An infinite hard drive that never fills up and never loses a file — every photo uploaded since 2010 is still retrievable in milliseconds.", 'Object Storage (S3-like)'),
      celebrationMessage: buildCelebration('Object Storage', 'Upload Service', 'Instagram stores over 50 billion photos. Meta\'s object storage system stores multiple copies of every file across different data centers. A single photo might exist in 3 geographic locations to guarantee it is never lost.', 'NoSQL Database'),
      connectingMessage: 'Object Storage is on the canvas. Connect Upload Service → Object Storage. Hover over Upload Service and drag to Object Storage. Raw uploaded files land here immediately after upload. This is S3-compatible storage — infinitely scalable, 99.999999999% durability.',
      messages: [
        msg("Meta built their own object storage system called Haystack specifically for Instagram's scale. Traditional filesystems could not handle billions of small files efficiently."),
        msg("Press ⌘K and search for \"Object Storage (S3-like)\" and press Enter to add it, then connect Upload Service → Object Storage."),
      ],
      requiredNodes: ['object_storage'],
      requiredEdges: [edge('upload_service', 'object_storage')],
      successMessage: 'Object Storage added and connected. Now the NoSQL Database.',
      errorMessage: 'Add Object Storage connected from the Upload Service.',
    }),
    step({
      id: 8,
      title: 'Add NoSQL Database',
      explanation: 'The NoSQL Database stores post metadata — captions, hashtags, like counts, comment counts, post timestamps — separately from the actual media files. Object Storage holds the photo; NoSQL holds everything about it.',
      action: buildAction('NoSQL Database', 'Upload Service', 'NoSQL Database', 'post metadata being stored separately from media files with massive write throughput'),
      why: "Instagram uses Cassandra for post metadata — it handles massive write throughput and never has a single point of failure since data is distributed across many nodes.",
      component: component('nosql_db', 'NoSQL Database'),
      openingMessage: buildOpeningL1('Instagram', 'NoSQL Database', 'store post metadata — captions, hashtags, like counts, comment counts, post timestamps — separately from the actual media files', "Object Storage holds the photo itself. NoSQL Database holds everything you know ABOUT the photo — who posted it, when, what the caption says, how many likes it has.", 'NoSQL Database'),
      celebrationMessage: buildCelebration('NoSQL Database', 'Upload Service', 'Instagram\'s NoSQL database stores metadata for over 50 billion posts. Cassandra handles writes of 1 million+ posts per hour during peak times because it scales horizontally without a single point of write contention.', 'Auth Service in Level 2'),
      connectingMessage: 'NoSQL Database is on the canvas. Connect Upload Service → NoSQL Database. When a photo uploads, two things happen simultaneously: the file goes to Object Storage and the metadata goes to NoSQL Database — both connections are created by the Upload Service.',
      messages: [
        msg("Instagram uses Cassandra for post metadata — it handles massive write throughput and never has a single point of failure since data is distributed across many nodes."),
        msg("Press ⌘K and search for \"NoSQL Database\" and press Enter to add it, then connect Upload Service → NoSQL Database."),
      ],
      requiredNodes: ['nosql_db'],
      requiredEdges: [edge('upload_service', 'nosql_db')],
      successMessage: 'Level 1 complete! You have a working photo platform foundation.',
      errorMessage: 'Add a NoSQL Database connected from the Upload Service.',
    }),
  ],
});

const l2 = level({
  level: 2,
  title: 'Production Ready',
  subtitle: 'Add auth, feed, caching, streaming, and observability',
  description: 'Your Instagram foundation handles uploads. Now add what Meta actually runs in production — auth, personalized feed generation, Redis caching, Kafka event streaming, background workers, and full observability.',
  estimatedTime: '~20 mins',
  unlocks: 'Expert Architecture',
  prerequisite: 'Builds on your Level 1 diagram',
  contextMessage: 'Your Instagram foundation handles uploads and serves media. Now add what Meta actually runs in production — auth, feed generation, caching, event streaming, background processing, and full observability. 10 more components on your existing diagram.',
  steps: [
    step({
      id: 9,
      title: 'Add Auth Service',
      explanation: 'The Auth Service validates OAuth tokens and session cookies for 2 billion users across mobile, web, and the Instagram API used by third-party apps.',
      action: buildAction('Auth Service (JWT)', 'Load Balancer', 'Auth Service', 'every request being validated before reaching any backend service'),
      why: 'Without auth, anyone could post as any user — no authentication means no private accounts, no DMs, no ability to know who posted what.',
      component: component('auth_service', 'Auth Service (JWT)', 'Auth Service (JWT)'),
      openingMessage: buildOpeningL2('Instagram', 'Auth Service', 'validates OAuth tokens and session cookies for 2 billion users across mobile, web, and the Instagram API used by third-party apps', 'anyone could post as any user — no authentication means no private accounts, no DMs, no ability to know who posted what', 'Auth Service'),
      celebrationMessage: buildCelebration('Auth Service', 'Load Balancer', "Instagram's Auth Service validates over 500 million session tokens per hour. Meta uses a distributed token validation system so no single auth server is a bottleneck — tokens are validated at the edge in under 2ms.", 'SQL Database'),
      connectingMessage: 'Auth Service is on the canvas. Connect Load Balancer → Auth Service. Every request to any Instagram endpoint is authenticated before reaching any service. The Load Balancer routes to Auth Service first for all non-CDN traffic.',
      messages: [
        msg("Every request to any Instagram endpoint is authenticated before reaching any service. The Load Balancer routes to Auth Service first for all non-CDN traffic."),
        msg("Press ⌘K and search for \"Auth Service\" and press Enter to add it, then connect Load Balancer → Auth Service."),
      ],
      requiredNodes: ['auth_service'],
      requiredEdges: [edge('load_balancer', 'auth_service')],
      successMessage: 'Auth Service added and connected. Now the SQL Database.',
      errorMessage: 'Add an Auth Service connected from the Load Balancer.',
    }),
    step({
      id: 10,
      title: 'Add SQL Database',
      explanation: 'The SQL Database stores user accounts, profile information, follow relationships, and subscription data with ACID transaction guarantees.',
      action: buildAction('SQL Database', 'Auth Service', 'SQL Database', 'user accounts and relationships being stored with strong consistency guarantees'),
      why: 'A follow operation could succeed on one server and fail silently on another without SQL. Users would appear and disappear from follower lists randomly.',
      component: component('sql_db', 'SQL Database'),
      openingMessage: buildOpeningL2('Instagram', 'SQL Database', 'stores user accounts, profile information, follow relationships, and subscription data with ACID transaction guarantees', 'a follow operation could succeed on one server and fail silently on another — users would appear and disappear from follower lists randomly', 'SQL Database'),
      celebrationMessage: buildCelebration('SQL Database', 'Auth Service', "Instagram sharded their PostgreSQL database across 12 shards in 2012 when they were acquired by Facebook with only 13 employees. Each shard handled a subset of users — a pattern that scaled them from 30 million to 1 billion users.", 'Feed Service'),
      connectingMessage: 'SQL Database is on the canvas. Connect Auth Service → SQL Database. User accounts and relationships require strong consistency — SQL guarantees that a follow relationship is either fully committed or fully rolled back, never in a partial state.',
      messages: [
        msg("User accounts and relationships require strong consistency — SQL guarantees that a follow relationship is either fully committed or fully rolled back, never in a partial state."),
        msg("Press ⌘K and search for \"SQL Database\" and press Enter to add it, then connect Auth Service → SQL Database."),
      ],
      requiredNodes: ['sql_db'],
      requiredEdges: [edge('auth_service', 'sql_db')],
      successMessage: 'SQL Database added and connected. Now the Feed Service.',
      errorMessage: 'Add a SQL Database connected from the Auth Service.',
    }),
    step({
      id: 11,
      title: 'Add Feed Service',
      explanation: 'The Feed Service assembles the personalized home feed for each user by merging posts from all followed accounts and ranking them using an ML model.',
      action: buildAction('Feed Service', 'Load Balancer', 'Feed Service', 'personalized feeds being assembled and ranked using ML for every user'),
      why: "Without the Feed Service, Instagram would show posts chronologically from all followed accounts. For users following 1000 accounts this would show the wrong content to the wrong people at the wrong time.",
      component: component('feed_service', 'Feed Service'),
      openingMessage: buildOpeningL2('Instagram', 'Feed Service', 'assembles the personalized home feed for each user by merging posts from all followed accounts and ranking them using a ML model', 'without it, Instagram would have to show posts chronologically from all followed accounts — for users following 1000 accounts this would show the wrong content to the wrong people at the wrong time', 'Feed Service'),
      celebrationMessage: buildCelebration('Feed Service', 'Load Balancer', "Instagram's feed ranking model considers thousands of signals per post — relationship strength, engagement velocity, content type, time decay. The entire feed for one user is assembled and ranked in under 200ms.", 'In-Memory Cache'),
      connectingMessage: 'Feed Service is on the canvas. Connect Load Balancer → Feed Service. When a user opens Instagram the Load Balancer routes the feed request here. This service assembles and ranks the personalized feed.',
      messages: [
        msg("When a user opens Instagram the Load Balancer routes the feed request here. This service assembles and ranks the personalized feed."),
        msg("Press ⌘K and search for \"Feed Service\" and press Enter to add it, then connect Load Balancer → Feed Service."),
      ],
      requiredNodes: ['feed_service'],
      requiredEdges: [edge('load_balancer', 'feed_service')],
      successMessage: 'Feed Service added and connected. Now the In-Memory Cache.',
      errorMessage: 'Add a Feed Service connected from the Load Balancer.',
    }),
    step({
      id: 12,
      title: 'Add In-Memory Cache',
      explanation: 'The In-Memory Cache pre-computes and caches the feed for each user so opening the app shows content instantly rather than computing the feed on every open.',
      action: buildAction('In-Memory Cache', 'Feed Service', 'In-Memory Cache', 'pre-computed feeds being served in under 1ms instead of recomputing from scratch'),
      why: "Without cache, every time a user opened Instagram it would take 2-3 seconds to compute their personalized feed from scratch — unacceptable for a mobile app where users expect instant content.",
      component: component('in_memory_cache', 'In-Memory Cache'),
      openingMessage: buildOpeningL2('Instagram', 'In-Memory Cache', 'pre-computes and caches the feed for each user so opening the app shows content instantly rather than computing the feed on every open', 'every time a user opened Instagram it would take 2-3 seconds to compute their personalized feed from scratch — unacceptable for a mobile app where users expect instant content', 'In-Memory Cache'),
      celebrationMessage: buildCelebration('In-Memory Cache', 'Feed Service', 'Instagram caches the top 300 posts for each user\'s feed in Redis. This cache is refreshed every 30 minutes or when new posts from close contacts arrive. Reading from cache takes 1ms vs 200ms for a fresh feed computation.', 'Kafka Streaming'),
      connectingMessage: 'In-Memory Cache is on the canvas. Connect Feed Service → In-Memory Cache. Pre-computed feeds are stored here. When a user opens Instagram the cached feed loads in milliseconds. The Feed Service regenerates the cache in the background while the user is already scrolling.',
      messages: [
        msg("Pre-computed feeds are stored here. When a user opens Instagram the cached feed loads in milliseconds. The Feed Service regenerates the cache in the background while the user is already scrolling."),
        msg("Press ⌘K and search for \"In-Memory Cache\" and press Enter to add it, then connect Feed Service → In-Memory Cache."),
      ],
      requiredNodes: ['in_memory_cache'],
      requiredEdges: [edge('feed_service', 'in_memory_cache')],
      successMessage: 'In-Memory Cache added and connected. Now Kafka Streaming.',
      errorMessage: 'Add an In-Memory Cache connected from the Feed Service.',
    }),
    step({
      id: 13,
      title: 'Add Kafka Streaming',
      explanation: 'Kafka streams every upload event, like, comment, and follow to all downstream services that need to react to these events — notifications, feed updates, analytics, ML training.',
      action: buildAction('Kafka / Streaming', 'Upload Service', 'Kafka Streaming', 'upload events being published to Kafka so downstream services consume independently'),
      why: 'Without Kafka, the Upload Service would need to directly call the notification service, feed update service, analytics service, and ML pipeline synchronously — coupling everything together and creating cascading failures.',
      component: component('kafka_streaming', 'Kafka / Streaming', 'Kafka / Streaming'),
      openingMessage: buildOpeningL2('Instagram', 'Kafka Streaming', 'streams every upload event, like, comment, and follow to all downstream services that need to react to these events', 'the Upload Service would need to directly call the notification service, feed update service, analytics service, and ML pipeline synchronously — coupling everything together and creating cascading failures', 'Kafka'),
      celebrationMessage: buildCelebration('Kafka', 'Upload Service', 'Instagram processes over 1 billion Kafka events per day. A single post upload triggers events consumed by 12+ downstream services — notification delivery, feed invalidation, hashtag indexing, spam detection, and ML feature extraction.', 'Worker / Background Job'),
      connectingMessage: 'Kafka is on the canvas. Connect Upload Service → Kafka. When a post is uploaded, an event is published to Kafka. Every service that cares about new posts — notifications, feed updates, analytics, ML training — consumes from Kafka independently.',
      messages: [
        msg("When a post is uploaded, an event is published to Kafka. Every service that cares about new posts — notifications, feed updates, analytics, ML training — consumes from Kafka independently."),
        msg("Press ⌘K and search for \"Kafka / Streaming\" and press Enter to add it, then connect Upload Service → Kafka Streaming."),
      ],
      requiredNodes: ['kafka_streaming'],
      requiredEdges: [edge('upload_service', 'kafka_streaming')],
      successMessage: 'Kafka Streaming added and connected. Now the Worker / Background Job.',
      errorMessage: 'Add Kafka Streaming connected from the Upload Service.',
    }),
    step({
      id: 14,
      title: 'Add Worker / Background Job',
      explanation: 'The Worker processes async tasks like media transcoding, thumbnail generation, hashtag indexing, and feed cache invalidation that do not need to happen during the upload request.',
      action: buildAction('Worker / Background Job', 'Kafka / Streaming', 'Worker / Background Job', 'thumbnail generation and media processing happening asynchronously after upload'),
      why: "Without workers, every photo upload would wait for thumbnails to be generated and hashtags to be indexed before completing — uploads would take 30 seconds instead of 2 seconds.",
      component: component('worker_job', 'Worker / Background Job'),
      openingMessage: buildOpeningL2('Instagram', 'Worker / Background Job', 'processes async tasks like media transcoding, thumbnail generation, hashtag indexing, and feed cache invalidation that do not need to happen during the upload request', 'every photo upload would wait for thumbnails to be generated and hashtags to be indexed before completing — uploads would take 30 seconds instead of 2 seconds', 'Worker'),
      celebrationMessage: buildCelebration('Worker', 'Kafka', 'Instagram generates 5 thumbnail sizes for every photo — 150x150, 320x320, 480x480, 640x640, 1080x1080. A video gets transcoded into 8 different formats and 4 bitrates. All 36 files are generated asynchronously by workers after upload.', 'Search Engine'),
      connectingMessage: 'Worker is on the canvas. Connect Kafka → Worker. Upload events from Kafka trigger background jobs. Media transcoding, thumbnail generation, and hashtag indexing all run here asynchronously while the user sees their post immediately.',
      messages: [
        msg("Upload events from Kafka trigger background jobs. Media transcoding, thumbnail generation, and hashtag indexing all run here asynchronously while the user sees their post immediately."),
        msg("Press ⌘K and search for \"Worker\" and press Enter to add it, then connect Kafka Streaming → Worker / Background Job."),
      ],
      requiredNodes: ['worker_job'],
      requiredEdges: [edge('kafka_streaming', 'worker_job')],
      successMessage: 'Worker / Background Job added and connected. Now the Search Engine.',
      errorMessage: 'Add a Worker / Background Job connected from Kafka Streaming.',
    }),
    step({
      id: 15,
      title: 'Add Search Engine',
      explanation: 'The Search Engine indexes all user profiles, hashtags, and locations so users can search and discover content in real-time as they type.',
      action: buildAction('Search Engine', 'Load Balancer', 'Search Engine', 'hashtag and user search being handled by a dedicated search index'),
      why: "Without it, the Explore page and hashtag pages would not exist — users could only see content from accounts they already follow with no way to discover new creators.",
      component: component('search_engine', 'Search Engine'),
      openingMessage: buildOpeningL2('Instagram', 'Search Engine', 'indexes all user profiles, hashtags, and locations so users can search and discover content in real-time as they type', 'the Explore page and hashtag pages would not exist — users could only see content from accounts they already follow with no way to discover new creators', 'Search Engine'),
      celebrationMessage: buildCelebration('Search Engine', 'Load Balancer', "Instagram's Explore page uses search and ML ranking to show content from accounts you don't follow. It serves 200 million accounts daily and drives 50% of new creator discoveries. Built on Elasticsearch with custom ML ranking.", 'Logger'),
      connectingMessage: 'Search Engine is on the canvas. Connect Load Balancer → Search Engine. Search requests are routed directly to the search engine — separate from the feed service because search has different performance characteristics and scaling requirements.',
      messages: [
        msg("Search requests are routed directly to the search engine — separate from the feed service because search has different performance characteristics and scaling requirements."),
        msg("Press ⌘K and search for \"Search Engine\" and press Enter to add it, then connect Load Balancer → Search Engine."),
      ],
      requiredNodes: ['search_engine'],
      requiredEdges: [edge('load_balancer', 'search_engine')],
      successMessage: 'Search Engine added and connected. Now the Logger.',
      errorMessage: 'Add a Search Engine connected from the Load Balancer.',
    }),
    step({
      id: 16,
      title: 'Add Logger',
      explanation: 'The Logger captures structured logs from every service for debugging, audit trails, and feed algorithm improvement.',
      action: buildAction('Logger', 'Feed Service', 'Logger', 'request traces and feed ranking signals being captured for debugging'),
      why: "When feed ranking breaks and users see irrelevant content there is no way to debug which signal caused the wrong ranking decision without logs.",
      component: component('logger', 'Logger'),
      openingMessage: buildOpeningL2('Instagram', 'Logger', 'captures structured logs from every service for debugging, audit trails, and feed algorithm improvement', 'when feed ranking breaks and users see irrelevant content there is no way to debug which signal caused the wrong ranking decision', 'Logger'),
      celebrationMessage: buildCelebration('Logger', 'Feed Service', "Instagram logs every feed ranking decision with the top 10 signals that influenced it. This log data feeds back into ML training — the algorithm learns from what users engage with vs what they scroll past.", 'Metrics Collector'),
      connectingMessage: 'Logger is on the canvas. Connect Feed Service → Logger. Feed requests are logged with the full ranking signals used — this data is used to improve the ML model and debug ranking issues.',
      messages: [
        msg("Feed requests are logged with the full ranking signals used — this data is used to improve the ML model and debug ranking issues."),
        msg("Press ⌘K and search for \"Logger\" and press Enter to add it, then connect Feed Service → Logger."),
      ],
      requiredNodes: ['logger'],
      requiredEdges: [edge('feed_service', 'logger')],
      successMessage: 'Logger added and connected. Now the Metrics Collector.',
      errorMessage: 'Add a Logger connected from the Feed Service.',
    }),
    step({
      id: 17,
      title: 'Add Metrics Collector',
      explanation: 'The Metrics Collector aggregates real-time metrics on upload success rates, feed load times, story view counts, and API error rates across all services.',
      action: buildAction('Metrics Collector', 'Logger', 'Metrics Collector', 'real-time performance metrics being aggregated for alerting and dashboards'),
      why: "Instagram engineers would not know that uploads were failing for Android users in Southeast Asia until thousands of support tickets arrived hours later without metrics.",
      component: component('metrics_collector', 'Metrics Collector'),
      openingMessage: buildOpeningL2('Instagram', 'Metrics Collector', 'aggregates real-time metrics on upload success rates, feed load times, story view counts, and API error rates across all services', 'Instagram engineers would not know that uploads were failing for Android users in Southeast Asia until thousands of support tickets arrived hours later', 'Metrics Collector'),
      celebrationMessage: buildCelebration('Metrics Collector', 'Logger', 'Instagram monitors over 500 custom metrics in real-time. A drop in Stories completion rate below 85% triggers an automated alert within 60 seconds — before any user complains.', 'Dashboard'),
      connectingMessage: 'Metrics Collector is on the canvas. Connect Logger → Metrics Collector. Logs feed into metric aggregation — error rates, latency percentiles, and throughput numbers are computed from log streams in real-time.',
      messages: [
        msg("Logs feed into metric aggregation — error rates, latency percentiles, and throughput numbers are computed from log streams in real-time."),
        msg("Press ⌘K and search for \"Metrics Collector\" and press Enter to add it, then connect Logger → Metrics Collector."),
      ],
      requiredNodes: ['metrics_collector'],
      requiredEdges: [edge('logger', 'metrics_collector')],
      successMessage: 'Metrics Collector added and connected. Now the Dashboard.',
      errorMessage: 'Add a Metrics Collector connected from the Logger.',
    }),
    step({
      id: 18,
      title: 'Add Dashboard',
      explanation: "The Dashboard provides Meta's engineering and product teams with real-time visibility into Instagram's health across all regions and features.",
      action: buildAction('Dashboard (Grafana)', 'Metrics Collector', 'Dashboard', 'system health metrics being visualized for 24/7 operational monitoring'),
      why: "During major incidents engineers fly blind without a dashboard — no visibility into which region is affected, which service is failing, or how many users are impacted.",
      component: component('dashboard', 'Dashboard (Grafana)', 'Dashboard (Grafana)'),
      openingMessage: buildOpeningL2('Instagram', 'Dashboard', "provides Meta's engineering and product teams with real-time visibility into Instagram's health across all regions and features", 'during major incidents engineers fly blind — no visibility into which region is affected, which service is failing, or how many users are impacted', 'Dashboard'),
      celebrationMessage: buildCelebration('Dashboard', 'Metrics Collector', "Instagram's ops team monitors upload success rates, feed P99 latency, and Stories view counts on live dashboards during product launches. When a new feature ships to 1% of users, they watch these dashboards for 30 minutes before expanding the rollout.", 'Level 3 — Expert Architecture'),
      connectingMessage: 'Dashboard is on the canvas. Connect Metrics Collector → Dashboard. Aggregated metrics flow to the dashboard where engineers monitor platform health 24/7 across every region Instagram operates in.',
      messages: [
        msg("Aggregated metrics flow to the dashboard where engineers monitor platform health 24/7 across every region Instagram operates in."),
        msg("Press ⌘K and search for \"Dashboard (Grafana)\" and press Enter to add it, then connect Metrics Collector → Dashboard."),
      ],
      requiredNodes: ['dashboard'],
      requiredEdges: [edge('metrics_collector', 'dashboard')],
      successMessage: 'Level 2 complete! Your Instagram architecture is production-ready.',
      errorMessage: 'Add a Dashboard connected from the Metrics Collector.',
    }),
  ],
});

const l3 = level({
  level: 3,
  title: 'Expert Architecture',
  subtitle: 'Design like a Meta senior engineer',
  description: 'Good architecture stores photos. Expert architecture handles the follower graph at 2B users, ML recommendations, fan-out for celebrity accounts, multi-channel notifications, and security infrastructure.',
  estimatedTime: '~25 mins',
  prerequisite: 'Builds on your Level 2 diagram',
  contextMessage: "You have production Instagram. Level 3 adds the systems that power Instagram at true Meta scale — ML feed ranking, fan-out architecture for celebrity accounts, multi-channel notifications, the Explore recommendation engine, and security infrastructure. 10 more components. This is what Meta's senior engineers actually built.",
  steps: [
    step({
      id: 19,
      title: 'Add GraphQL Federation Gateway',
      explanation: "The GraphQL Federation Gateway aggregates multiple backend services — User Service, Media Service, Feed Service — into a single unified GraphQL API. Clients query one endpoint; the gateway fans out to the correct services and composes the response.",
      action: buildAction('GraphQL Federation Gateway', 'Load Balancer', 'GraphQL Federation Gateway', 'multiple backend services being aggregated into a single unified GraphQL API for client queries'),
      why: "Without federation, clients must make separate REST calls to User Service, Media Service, and Feed Service — causing chatty client-server interactions and versioning nightmares. The gateway composes everything server-side.",
      component: component('graphql_federation_gateway', 'GraphQL Federation Gateway'),
      openingMessage: buildOpeningL3('Instagram', 'GraphQL Federation Gateway', 'federated GraphQL architecture that aggregates multiple backend services into a single unified API — clients query one endpoint while the gateway fans out to User Service, Media Service, and Feed Service', "Without federation, clients must make separate REST calls to User Service, Media Service, and Feed Service — causing chatty client-server interactions and versioning nightmares. The gateway composes everything server-side.", 'GraphQL Federation Gateway'),
      celebrationMessage: buildCelebration('GraphQL Federation Gateway', 'Load Balancer', "Instagram's mobile app makes a single GraphQL request to fetch profile, posts, and follower counts. The Federation Gateway fans this out to User Service, Feed Service, and Media Service in parallel, composes the response, and returns it in one round trip.", 'CDC Connector'),
      connectingMessage: 'GraphQL Federation Gateway is on the canvas. Connect Load Balancer → GraphQL Federation Gateway. Connect GraphQL Federation Gateway → User Service, → Feed Service, and → Media Service. The gateway receives client queries and fans them out to the appropriate services.',
      messages: [
        msg("The GraphQL Federation Gateway receives client queries and fans them out to User Service, Feed Service, and Media Service in parallel, then composes a unified response."),
        msg("Press ⌘K and search for \"GraphQL Federation Gateway\" and press Enter to add it, then connect Load Balancer → GraphQL Federation Gateway and to the services."),
      ],
      requiredNodes: ['graphql_federation_gateway'],
      requiredEdges: [edge('load_balancer', 'graphql_federation_gateway')],
      successMessage: 'GraphQL Federation Gateway added and connected. Now the CDC Connector.',
      errorMessage: 'Add a GraphQL Federation Gateway connected from the Load Balancer.',
    }),
    step({
      id: 20,
      title: 'Add CDC Connector',
      explanation: "The CDC Connector (Debezium) captures real-time changes from the User Service's follower graph database and streams them to downstream consumers — Cache, Search Index, and Data Warehouse — without tight coupling.",
      action: buildAction('CDC Connector (Debezium)', 'SQL Database', 'CDC Connector', 'database change events being streamed to downstream consumers in real-time without tight coupling'),
      why: "Without CDC, every service that needs follower graph data must query the User Service database directly — creating coupling and single points of failure. CDC decouples producers from consumers using event streaming.",
      component: component('cdc_connector', 'CDC Connector (Debezium)'),
      openingMessage: buildOpeningL3('Instagram', 'CDC Connector', 'Change Data Capture using Debezium to stream database changes from the User Service to downstream consumers — Cache, Search Index, and Data Warehouse', "Without CDC, every service that needs follower graph data must query the User Service database directly — creating coupling and single points of failure. CDC decouples producers from consumers using event streaming.", 'CDC Connector'),
      celebrationMessage: buildCelebration('CDC Connector', 'SQL Database', "When Cristiano Ronaldo gains 10 million followers, the CDC Connector streams those database changes to the follower cache, search index, and recommendation system within seconds. Each downstream system processes the event independently — no tightly coupled queries.", 'CQRS Command Handler'),
      connectingMessage: 'CDC Connector is on the canvas. Connect SQL Database → CDC Connector. Then connect CDC Connector → Cache and → Search Engine. Changes to the follower graph are streamed to downstream systems in real-time.',
      messages: [
        msg("The CDC Connector captures every follower graph change and streams it to downstream consumers. When a celebrity gains 1 million followers, all caches and indexes are updated within seconds without tight coupling."),
        msg("Press ⌘K and search for \"CDC Connector\" and press Enter to add it, then connect SQL Database → CDC Connector → Cache and Search Engine."),
      ],
      requiredNodes: ['cdc_connector'],
      requiredEdges: [edge('sql_db', 'cdc_connector')],
      successMessage: 'CDC Connector added and connected. Now the CQRS Command Handler.',
      errorMessage: 'Add a CDC Connector connected from the SQL Database.',
    }),
    step({
      id: 21,
      title: 'Add CQRS Command Handler',
      explanation: "The CQRS Command Handler separates write operations (post creation, follow, like) from read operations (feed retrieval, profile fetch). Writes go through the command path with validation; reads come from optimized read replicas with denormalized views.",
      action: buildAction('CQRS Command Handler', 'Feed Service', 'CQRS Command Handler', 'write operations being separated from read operations for optimized performance on both paths'),
      why: "A single model serving both reads and writes creates contention — high read traffic slows down writes and vice versa. CQRS separates these paths so they can scale independently.",
      component: component('cqrs_command_handler', 'CQRS Command Handler'),
      openingMessage: buildOpeningL3('Instagram', 'CQRS Command Handler', 'Command Query Responsibility Segregation separating write operations (post, follow, like) from optimized read paths (feed, profile)', "A single model serving both reads and writes creates contention — high read traffic slows down writes and vice versa. CQRS separates these paths so they can scale independently.", 'CQRS Command Handler'),
      celebrationMessage: buildCelebration('CQRS Command Handler', 'Feed Service', "Instagram's CQRS separation means 100 million daily post creations and 50 billion daily feed reads operate on different infrastructure. Writes go through strict validation; reads hit denormalized read replicas optimized for feed queries.", 'Notification Service'),
      connectingMessage: 'CQRS Command Handler is on the canvas. Connect Feed Service → CQRS Command Handler. Write operations (post, follow, like) are routed through the command handler while reads continue to use the existing read path.',
      messages: [
        msg("The CQRS Command Handler receives write operations and routes them through strict validation and event sourcing. Read operations bypass this and hit optimized read replicas directly."),
        msg("Press ⌘K and search for \"CQRS Command Handler\" and press Enter to add it, then connect Feed Service → CQRS Command Handler."),
      ],
      requiredNodes: ['cqrs_command_handler'],
      requiredEdges: [edge('feed_service', 'cqrs_command_handler')],
      successMessage: 'CQRS Command Handler added and connected. Now the Notification Service.',
      errorMessage: 'Add a CQRS Command Handler connected from the Feed Service.',
    }),
    step({
      id: 22,
      title: 'Add Notification Service',
      explanation: 'The Notification Service is an event-driven notification orchestrator that consumes Kafka events and routes to the correct delivery channel — push, in-app, or email.',
      action: buildAction('Notification Service', 'Kafka / Streaming', 'Notification Service', 'Kafka events being consumed and routed to the correct delivery channel based on notification type and user preferences'),
      why: 'A like notification from a close friend should arrive in under 1 second. A marketing email about account activity can wait 5 minutes. Different delivery priorities require routing logic that separates urgent from non-urgent notifications.',
      component: component('notification_service', 'Notification Service'),
      openingMessage: buildOpeningL3('Instagram', 'Notification Service', 'event-driven notification orchestration that consumes Kafka events and routes to the correct delivery channel — push, in-app, or email', 'A like notification from a close friend should arrive in under 1 second. A marketing email about account activity can wait 5 minutes. Different delivery priorities require routing logic that separates urgent from non-urgent notifications.', 'Notification Service'),
      celebrationMessage: buildCelebration('Notification Service', 'Kafka', 'Instagram delivers over 1 billion push notifications daily. The Notification Service applies user preferences — some users mute likes, others mute follows — before routing each notification to its delivery channel.', 'CQRS Query Handler'),
      connectingMessage: 'Notification Service is on the canvas. Connect Kafka → Notification Service. Like, comment, follow, and mention events flow from Kafka to the Notification Service which decides the delivery channel and priority for each notification type.',
      messages: [
        msg("Like, comment, follow, and mention events flow from Kafka to the Notification Service which decides the delivery channel and priority for each notification type."),
        msg("Press ⌘K and search for \"Notification Service\" and press Enter to add it, then connect Kafka Streaming → Notification Service."),
      ],
      requiredNodes: ['notification_service'],
      requiredEdges: [edge('kafka_streaming', 'notification_service')],
      successMessage: 'Notification Service added and connected. Now the CQRS Query Handler.',
      errorMessage: 'Add a Notification Service connected from Kafka Streaming.',
    }),
    step({
      id: 23,
      title: 'Add CQRS Query Handler',
      explanation: "The CQRS Query Handler serves optimized read queries from denormalized read replicas. The read model is built asynchronously from the command side — feeds are pre-computed and cached rather than computed on every read.",
      action: buildAction('CQRS Query Handler', 'Read Replica', 'CQRS Query Handler', 'optimized read queries being served from denormalized read replicas'),
      why: "Computing a user's feed by querying the post database on every read is O(n) where n is the number of followed users. Pre-computing and caching the feed makes reads O(1) — constant time regardless of how many people you follow.",
      component: component('cqrs_query_handler', 'CQRS Query Handler'),
      openingMessage: buildOpeningL3('Instagram', 'CQRS Query Handler', 'optimized read path from denormalized read replicas — feeds are pre-computed and cached rather than computed on every read', "Computing a user's feed by querying the post database on every read is O(n) where n is the number of followed users. Pre-computing and caching the feed makes reads O(1) — constant time regardless of how many people you follow.", 'CQRS Query Handler'),
      celebrationMessage: buildCelebration('CQRS Query Handler', 'Read Replica', "Instagram's read replicas serve 50 billion feed reads daily with sub-50ms latency. The query handler hits read replicas with pre-computed feed views — no joins, no aggregations on the hot path.", 'Recommendation Service'),
      connectingMessage: 'CQRS Query Handler is on the canvas. Connect Read Replica → CQRS Query Handler. Then connect CQRS Query Handler → Feed Service. Read queries for feeds and profiles are served from denormalized views on read replicas.',
      messages: [
        msg("The CQRS Query Handler serves read queries from denormalized read replicas. Feed reads hit pre-computed views — no expensive joins or aggregations on the critical path."),
        msg("Press ⌘K and search for \"CQRS Query Handler\" and press Enter to add it, then connect Read Replica → CQRS Query Handler → Feed Service."),
      ],
      requiredNodes: ['cqrs_query_handler'],
      requiredEdges: [edge('feed_service', 'cqrs_query_handler')],
      successMessage: 'CQRS Query Handler added and connected. Now the Recommendation Service.',
      errorMessage: 'Add a CQRS Query Handler connected from the Feed Service.',
    }),
    step({
      id: 24,
      title: 'Add Recommendation Service',
      explanation: 'The Recommendation Service implements a two-stage recommendation pipeline — candidate generation narrows billions of posts to hundreds, then a deep learning ranker scores and orders those hundreds.',
      action: buildAction('Recommendation Service', 'Feed Service', 'Recommendation Service', 'two-stage ML pipeline narrowing candidate posts and ranking them for personalized recommendations'),
      why: "Running the full ranking model on all 50 billion posts for every feed request is computationally impossible. Two-stage retrieval makes it tractable — fast ANN search generates candidates in 20ms, then the ranker scores them in 50ms.",
      component: component('recommendation_service', 'Recommendation Service'),
      openingMessage: buildOpeningL3('Instagram', 'Recommendation Service', 'two-stage recommendation pipeline — candidate generation narrows billions of posts to hundreds, then a deep learning ranker scores and orders those hundreds', "Running the full ranking model on all 50 billion posts for every feed request is computationally impossible. Two-stage retrieval makes it tractable — fast ANN search generates candidates in 20ms, then the ranker scores them in 50ms.", 'Recommendation Service'),
      celebrationMessage: buildCelebration('Recommendation Service', 'Feed Service', "Instagram's recommendation model uses over 500 billion parameters and is trained on trillions of user interactions. It predicts the probability of 12 different actions — like, comment, share, save, profile visit — for every candidate post.", 'Read Replica'),
      connectingMessage: 'Recommendation Service is on the canvas. Connect Feed Service → Recommendation Service. When assembling a feed, the Feed Service queries the Recommendation Service for Explore page content and suggested accounts to mix into the home feed.',
      messages: [
        msg("When assembling a feed, the Feed Service queries the Recommendation Service for Explore page content and suggested accounts to mix into the home feed."),
        msg("Press ⌘K and search for \"Recommendation Service\" and press Enter to add it, then connect Feed Service → Recommendation Service."),
      ],
      requiredNodes: ['recommendation_service'],
      requiredEdges: [edge('feed_service', 'recommendation_service')],
      successMessage: 'Recommendation Service added and connected. Now the Read Replica.',
      errorMessage: 'Add a Recommendation Service connected from the Feed Service.',
    }),
    step({
      id: 25,
      title: 'Add Read Replica',
      explanation: "The Read Replica is a read-only copy of the main database that serves query traffic. Instagram uses read replicas for every service that needs database access — it offloads read traffic from the primary and reduces replication lag to under 100ms.",
      action: buildAction('Read Replica', 'NoSQL Database', 'Read Replica', 'read queries being served from replicas instead of the primary database'),
      why: "Instagram's primary database handles 5 million writes per second during peak hours. Without replicas, read queries would contend with writes and latency would spike. Replicas offload all read traffic.",
      component: component('read_replica', 'Read Replica'),
      openingMessage: buildOpeningL3('Instagram', 'Read Replica', 'read-only database copies that offload query traffic from the primary database with replication lag under 100ms', "Instagram's primary database handles 5 million writes per second during peak hours. Without replicas, read queries would contend with writes and latency would spike. Replicas offload all read traffic.", 'Read Replica'),
      celebrationMessage: buildCelebration('Read Replica', 'NoSQL Database', "Instagram runs hundreds of read replicas across multiple regions. Every database read — profile, feed, comment — hits a replica. Replication lag is measured in milliseconds using synchronous WAL shipping.", 'Data Warehouse'),
      connectingMessage: 'Read Replica is on the canvas. Connect NoSQL Database → Read Replica. Then connect Read Replica → CQRS Query Handler. All read queries are served from replicas — the primary only handles writes.',
      messages: [
        msg("The Read Replica serves all read queries. Replication from primary to replica uses write-ahead log (WAL) shipping with lag under 100ms. Reads never contend with the high-throughput write path."),
        msg("Press ⌘K and search for \"Read Replica\" and press Enter to add it, then connect NoSQL Database → Read Replica → CQRS Query Handler."),
      ],
      requiredNodes: ['read_replica'],
      requiredEdges: [edge('nosql_db', 'read_replica')],
      successMessage: 'Read Replica added and connected. Now the Data Warehouse.',
      errorMessage: 'Add a Read Replica connected from the NoSQL Database.',
    }),
    step({
      id: 26,
      title: 'Add Data Warehouse',
      explanation: 'The Data Warehouse is an append-only analytical store fed by Kafka that stores all engagement events for ML training, business analytics, and creator insights.',
      action: buildAction('Data Warehouse', 'Kafka / Streaming', 'Data Warehouse', 'engagement events flowing into columnar storage for ML training and business analytics'),
      why: 'The NoSQL database optimizes for write throughput on individual posts. The data warehouse optimizes for analytical queries — how many Reels did users in Brazil watch this week? These different access patterns require completely different systems.',
      component: component('data_warehouse', 'Data Warehouse'),
      openingMessage: buildOpeningL3('Instagram', 'Data Warehouse', 'append-only analytical store fed by Kafka that stores all engagement events for ML training, business analytics, and creator insights', 'The NoSQL database optimizes for write throughput on individual posts. The data warehouse optimizes for analytical queries — how many Reels did users in Brazil watch this week? These different access patterns require completely different systems.', 'Data Warehouse'),
      celebrationMessage: buildCelebration('Data Warehouse', 'Kafka', "Instagram's data warehouse processes over 1 petabyte of new engagement data daily. Creator Studio uses this warehouse to show creators their reach, engagement rate, and audience demographics updated every 24 hours.", 'SLO/SLI Tracker'),
      connectingMessage: 'Data Warehouse is on the canvas. Connect Kafka → Data Warehouse. Every engagement event — view, like, share, save — flows from Kafka into the warehouse where data scientists run analysis and ML engineers extract training data.',
      messages: [
        msg("Every engagement event — view, like, share, save — flows from Kafka into the warehouse where data scientists run analysis and ML engineers extract training data."),
        msg("Press ⌘K and search for \"Data Warehouse\" and press Enter to add it, then connect Kafka Streaming → Data Warehouse."),
      ],
      requiredNodes: ['data_warehouse'],
      requiredEdges: [edge('kafka_streaming', 'data_warehouse')],
      successMessage: 'Data Warehouse added and connected. Now the SLO/SLI Tracker.',
      errorMessage: 'Add a Data Warehouse connected from Kafka Streaming.',
    }),
    step({
      id: 27,
      title: 'Add SLO/SLI Tracker',
      explanation: "The SLO/SLI Tracker monitors Service Level Objectives and Indicators — Feed availability must be 99.99%, Feed P95 latency under 200ms, Upload success rate above 99.5%. When SLIs breach SLO thresholds, the Error Budget Monitor calculates remaining error budget.",
      action: buildAction('SLO/SLI Tracker', 'Metrics Collector', 'SLO/SLI Tracker', 'service level objectives being tracked and error budgets being calculated in real-time'),
      why: "Without SLOs, engineers debate what 'fast enough' means. With SLOs and error budgets, you know exactly how much unreliability you can afford. When the error budget hits zero, feature development pauses for reliability work.",
      component: component('slo_sli_tracker', 'SLO/SLI Tracker'),
      openingMessage: buildOpeningL3('Instagram', 'SLO/SLI Tracker', 'Service Level Objectives and Indicators tracked in real-time — Feed availability 99.99%, Feed P95 latency under 200ms, Upload success rate above 99.5%', "Without SLOs, engineers debate what 'fast enough' means. With SLOs and error budgets, you know exactly how much unreliability you can afford. When the error budget hits zero, feature development pauses for reliability work.", 'SLO/SLI Tracker'),
      celebrationMessage: buildCelebration('SLO/SLI Tracker', 'Metrics Collector', "Instagram's SLO for feed availability is 99.99% — allowing only 52 minutes of downtime per year. Their error budget for feed latency is consumed at 5% per month. When error budget burns down to 20%, feature freezes trigger automatically.", 'Error Budget Monitor'),
      connectingMessage: 'SLO/SLI Tracker is on the canvas. Connect Metrics Collector → SLO/SLI Tracker. Real-time metrics are evaluated against SLO thresholds — availability, latency, and error rates are tracked continuously.',
      messages: [
        msg("The SLO/SLI Tracker evaluates real-time metrics against defined SLOs. When availability drops below 99.99% or latency exceeds 200ms P95, the error budget begins depleting."),
        msg("Press ⌘K and search for \"SLO/SLI Tracker\" and press Enter to add it, then connect Metrics Collector → SLO/SLI Tracker."),
      ],
      requiredNodes: ['slo_sli_tracker'],
      requiredEdges: [edge('metrics_collector', 'slo_sli_tracker')],
      successMessage: 'SLO/SLI Tracker added and connected. Now the Error Budget Monitor.',
      errorMessage: 'Add an SLO/SLI Tracker connected from the Metrics Collector.',
    }),
    step({
      id: 28,
      title: 'Add Error Budget Monitor',
      explanation: "The Error Budget Monitor calculates remaining error budget based on SLO violations and automatically triggers alerts and feature freezes when budgets deplete. When 80% of the monthly error budget is consumed, new feature rollouts are paused.",
      action: buildAction('Error Budget Monitor', 'SLO/SLI Tracker', 'Error Budget Monitor', 'error budgets being calculated and feature rollouts being automatically paused when budgets deplete'),
      why: "Without error budgets, reliability work competes with feature work and always loses. Error budget policies make reliability mandatory — when budget is depleted, features stop shipping until reliability improves.",
      component: component('error_budget_monitor', 'Error Budget Monitor'),
      openingMessage: buildOpeningL3('Instagram', 'Error Budget Monitor', 'error budget calculation with automated feature freeze triggers when budgets deplete — 80% budget consumed pauses new rollouts', "Without error budgets, reliability work competes with feature work and always loses. Error budget policies make reliability mandatory — when budget is depleted, features stop shipping until reliability improves.", 'Error Budget Monitor'),
      celebrationMessage: buildCelebration('Error Budget Monitor', 'SLO/SLI Tracker', "Instagram's error budget policy means reliability is not optional. When feed error budget hits zero, all feed-related feature flags are frozen and on-call engineers focus entirely on reliability until the budget recovers. You have completed the full Instagram expert architecture.", ''),
      connectingMessage: 'Error Budget Monitor is on the canvas. Connect SLO/SLI Tracker → Error Budget Monitor. Error budgets are calculated continuously — when 80% is consumed, automated alerts fire and feature rollouts pause automatically.',
      messages: [
        msg("The Error Budget Monitor calculates remaining budget based on SLO violations. When 80% is consumed, automated alerts fire and feature rollouts pause until reliability improves."),
        msg("Press ⌘K and search for \"Error Budget Monitor\" and press Enter to add it, then connect SLO/SLI Tracker → Error Budget Monitor."),
      ],
      requiredNodes: ['error_budget_monitor'],
      requiredEdges: [edge('slo_sli_tracker', 'error_budget_monitor')],
      successMessage: "Expert architecture complete! You have designed Instagram like a Meta senior engineer.",
      errorMessage: 'Add an Error Budget Monitor connected from the SLO/SLI Tracker.',
    }),
  ],
});

export const instagramTutorial: Tutorial = tutorial({
  id: 'instagram-architecture',
  title: 'How to Design Instagram Architecture',
  description: 'Instagram serves 2B+ monthly active users sharing 100M+ photos daily. Learn how CDN, distributed storage, ML recommendations, and the follower graph work at global scale.',
  difficulty: 'Intermediate',
  category: 'Social Media',
  isLive: false,
  icon: 'Camera',
  color: '#E1306C',
  tags: ['CDN', 'Social Graph', 'Media Storage', 'ML Recommendations', 'Microservices'],
  estimatedTime: '~60 mins',
  levels: [l1, l2, l3],
});
