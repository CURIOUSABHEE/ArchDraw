import type { TutorialData } from './index';

export const spotifyTutorial: TutorialData = {
  id: 'spotify-architecture',
  title: 'How to Design Spotify Architecture',
  description: 'Build a music streaming platform for 600 million users. Learn audio transcoding, CDN delivery, offline sync, recommendation algorithms, and real-time listening sessions.',
  difficulty: 'Advanced',
  category: 'Audio Streaming',
  estimatedTime: '~30 mins',
  nodeCount: 11,
  stepCount: 11,
  icon: 'Music',
  color: '#1db954',
  tags: ['Audio CDN', 'Offline', 'Recommendations', 'Transcoding'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "Spotify's client is the web app, desktop app, and mobile app. The client handles adaptive audio quality selection, local playback queue management, and offline playback from downloaded tracks.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "Spotify's client is responsible for selecting audio quality based on connection speed and user preference. The client-side intelligence is what makes Spotify work on both WiFi and 2G.",
      messages: [
        { type: 'guide', content: "Welcome to the Spotify Architecture tutorial. 600 million users, 100 million songs and podcasts, and a recommendation system that feels like it reads your mind." },
        { type: 'guide', content: "Spotify's client selects audio quality automatically: 24kbps on 2G, 96kbps on 3G, 160kbps on WiFi, 320kbps for Premium users. The client also manages the local playback queue and offline downloads." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Client added. Now the audio delivery layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "Spotify selects audio quality based on connection speed. How does the client detect available bandwidth?",
      defaultSuggestions: ["How does Spotify select audio quality?", "What is adaptive streaming?", "How does Spotify work offline?"],
    },
    {
      id: 2,
      title: 'Add Audio CDN',
      explanation: "Spotify's Audio CDN delivers audio files from edge locations close to listeners. Every song is cached at hundreds of edge nodes worldwide. A popular song in Brazil is served from a São Paulo edge node, not a Stockholm origin server.",
      action: 'Press ⌘K, search for "Audio CDN", and add it to the canvas. Then connect Client → Audio CDN.',
      searchHint: 'Audio CDN',
      why: "Audio streaming requires low latency to prevent buffering. CDN edge nodes reduce the distance audio data travels, cutting latency from 200ms to under 20ms for most users.",
      messages: [
        { type: 'guide', content: "Spotify uses multiple CDN providers simultaneously — not just one. A song request goes to whichever CDN has the lowest latency for your location." },
        { type: 'guide', content: "The Audio CDN caches the most popular songs at edge nodes. The top 10,000 songs account for 80% of streams — these are pre-cached everywhere. Long-tail songs are fetched from origin on first request and cached for subsequent ones." },
        { type: 'guide', content: 'Press ⌘K, search for "Audio CDN", add it, then connect: Client → Audio CDN.' },
      ],
      validation: {
        requiredNodes: ['audio_cdn'],
        requiredEdges: [{ from: 'Web', to: 'Audio CDN' }],
        successMessage: "Audio CDN added. Now the API layer.",
        errorMessage: 'Add an Audio CDN and connect it from the Client.',
      },
      openingMessage: "Spotify uses multiple CDN providers simultaneously. Connect Client → Audio CDN — audio bytes flow from the user to the nearest edge node.",
      defaultSuggestions: ["How does Spotify choose CDN providers?", "What is CDN multi-homing?", "How does Spotify cache audio?"],
    },
    {
      id: 3,
      title: 'Add API Gateway',
      explanation: "All non-audio requests — search, playlist management, social features, recommendations — flow through the API Gateway. Audio bytes go through the CDN; everything else goes through the gateway.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas. Then connect Client → API Gateway.',
      searchHint: 'API Gateway',
      why: "Separating audio delivery (CDN) from API traffic (gateway) allows each to scale independently. Audio traffic is 100x larger by volume but doesn't need the same processing as API requests.",
      messages: [
        { type: 'guide', content: "Audio bytes go through the CDN. Everything else — search, playlists, recommendations — goes through the API Gateway." },
        { type: 'guide', content: "The API Gateway routes requests to the right microservice: search queries go to the search service, playlist updates go to the playlist service, recommendation requests go to the ML inference service." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", add it, then connect: Client → API Gateway.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [{ from: 'Web', to: 'API Gateway' }],
        successMessage: "API Gateway added. Now distribute the traffic.",
        errorMessage: 'Add an API Gateway and connect it from the Client.',
      },
      openingMessage: "Spotify separates audio delivery from API traffic. Connect Client → API Gateway for all non-audio requests.",
      defaultSuggestions: ["What is the difference between CDN and API traffic?", "How does Spotify handle search?", "What is request routing?"],
    },
    {
      id: 4,
      title: 'Add Load Balancer',
      explanation: "Spotify's Load Balancer distributes API requests across thousands of microservice instances. Spotify uses a microservices architecture with 800+ services — the load balancer routes to the right cluster.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas. Then connect API Gateway → Load Balancer.',
      searchHint: 'Load Balancer',
      why: "Spotify has 800+ microservices. The load balancer must route requests to the correct service cluster while handling traffic spikes — like when a new album drops and millions of users search simultaneously.",
      messages: [
        { type: 'guide', content: "Spotify has 800+ microservices. The load balancer routes each request to the right service cluster." },
        { type: 'guide', content: "When Taylor Swift drops a new album, millions of users search simultaneously. The load balancer auto-scales the search service cluster while other services remain unaffected." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", add it, then connect: API Gateway → Load Balancer.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [{ from: 'API Gateway', to: 'Load Balancer' }],
        successMessage: "Load Balancer added. Now the auth layer.",
        errorMessage: 'Add a Load Balancer and connect it from the API Gateway.',
      },
      openingMessage: "Spotify has 800+ microservices. Connect API Gateway → Load Balancer to route to the right service cluster.",
      defaultSuggestions: ["How does Spotify manage 800 microservices?", "What is service discovery?", "How does Spotify handle album release spikes?"],
    },
    {
      id: 5,
      title: 'Add Auth Service',
      explanation: "Spotify authenticates users via email/password or OAuth (Facebook, Google, Apple). The Auth Service also manages device authentication — a Premium account can be active on multiple devices simultaneously.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas. Then connect Load Balancer → Auth Service.',
      searchHint: 'Auth Service',
      why: "Spotify's auth must handle the Spotify Connect feature — controlling playback on one device from another. This requires device-level authentication tokens, not just user tokens.",
      messages: [
        { type: 'guide', content: "Spotify's auth handles both user authentication and device authentication for Spotify Connect." },
        { type: 'guide', content: "Spotify Connect lets you control your TV's Spotify from your phone. The Auth Service issues device tokens so your phone can send commands to your TV's Spotify session. Both devices are authenticated independently." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", add it, then connect: Load Balancer → Auth Service.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Auth' }],
        successMessage: "Auth added. Now the playlist service.",
        errorMessage: 'Add an Auth Service and connect it from the Load Balancer.',
      },
      openingMessage: "Spotify Connect lets you control playback across devices. Connect Load Balancer → Auth Service — all requests are validated here.",
      defaultSuggestions: ["What is Spotify Connect?", "How does Spotify handle multi-device auth?", "What is a device token?"],
    },
    {
      id: 6,
      title: 'Add Playlist Service',
      explanation: "The Playlist Service manages user playlists, collaborative playlists, and algorithmic playlists like Discover Weekly. It handles the complex logic of playlist ordering, collaborative editing, and playlist-to-track relationships.",
      action: 'Press ⌘K, search for "Playlist Service", and add it to the canvas. Then connect Auth Service → Playlist Service.',
      searchHint: 'Playlist Service',
      why: "Playlists are Spotify's core engagement mechanism. The Playlist Service must handle collaborative editing (multiple users adding tracks simultaneously) without conflicts.",
      messages: [
        { type: 'guide', content: "Playlists are Spotify's core engagement feature. The Playlist Service manages everything from personal playlists to collaborative ones." },
        { type: 'guide', content: "Collaborative playlists use operational transforms — if two users add a track at the same position simultaneously, both tracks are added without conflict. Spotify also generates algorithmic playlists like Discover Weekly here." },
        { type: 'guide', content: 'Press ⌘K, search for "Playlist Service", add it, then connect: Auth Service → Playlist Service.' },
      ],
      validation: {
        requiredNodes: ['playlist_service'],
        requiredEdges: [{ from: 'Auth', to: 'Playlist' }],
        successMessage: "Playlist Service added. Now the transcoding pipeline.",
        errorMessage: 'Add a Playlist Service and connect it from the Auth Service.',
      },
      openingMessage: "Spotify's collaborative playlists use operational transforms. Connect Auth Service → Playlist Service — authenticated playlist requests flow here.",
      defaultSuggestions: ["What is a collaborative playlist?", "How does Spotify handle playlist conflicts?", "What is Discover Weekly?"],
    },
    {
      id: 7,
      title: 'Add Audio Transcoder',
      explanation: "Every song uploaded to Spotify is transcoded into 5 quality levels: 24kbps, 96kbps, 160kbps, 320kbps, and lossless. That's 500 million files for 100 million tracks. The transcoder runs as background workers.",
      action: 'Press ⌘K, search for "Audio Transcoder", and add it to the canvas. Then connect Playlist Service → Audio Transcoder.',
      searchHint: 'Audio Transcoder',
      why: "Without transcoding, Spotify could only serve one quality level. The 5 quality levels are what enable Spotify to work on both 2G connections and audiophile setups.",
      messages: [
        { type: 'guide', content: "Every song on Spotify exists in 5 quality levels. The Audio Transcoder creates all of them from the master file." },
        { type: 'guide', content: "When a label uploads a master WAV file, the Audio Transcoder creates 5 versions: 24kbps (mobile data saver), 96kbps (normal), 160kbps (high), 320kbps (very high), and lossless FLAC. That's 500 million files total." },
        { type: 'guide', content: 'Press ⌘K, search for "Audio Transcoder", add it, then connect: Playlist Service → Audio Transcoder.' },
      ],
      validation: {
        requiredNodes: ['audio_transcoder'],
        requiredEdges: [{ from: 'Playlist', to: 'Audio' }],
        successMessage: "Audio Transcoder added. Now the recommendation engine.",
        errorMessage: 'Add an Audio Transcoder and connect it from the Playlist Service.',
      },
      openingMessage: "Spotify creates 5 quality versions of every song. Connect Playlist Service → Audio Transcoder — playlist-level metadata triggers transcoding jobs.",
      defaultSuggestions: ["What is audio transcoding?", "What is lossless audio?", "How does Spotify select audio quality?"],
    },
    {
      id: 8,
      title: 'Add Recommendation Service',
      explanation: "Spotify's Discover Weekly uses collaborative filtering — finding users with similar taste, then recommending what they liked that you haven't heard. It runs every Monday morning as a batch ML job processing 30 billion listening events.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas. Then connect Auth Service → Recommendation Service (Microservice).',
      searchHint: 'Microservice',
      why: "Recommendations drive engagement. Discover Weekly has a 40% save rate — users save 40% of recommended songs to their library. This is extraordinarily high for a recommendation system.",
      messages: [
        { type: 'guide', content: "Discover Weekly feels like Spotify reads your mind. It uses collaborative filtering across 30 billion listening events." },
        { type: 'guide', content: "The algorithm finds users with similar taste to you, then recommends songs they loved that you haven't heard. It runs every Monday morning as a batch job — not real-time. The 40% save rate makes it one of the most accurate recommendation systems ever built." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add it, then connect: Auth Service → Recommendation Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Auth', to: 'Microservice' }],
        successMessage: "Recommendation Service added. Now offline sync.",
        errorMessage: 'Add a Microservice (Recommendation Service) and connect it from the Auth Service.',
      },
      openingMessage: "Discover Weekly has a 40% save rate. Connect Auth Service → Recommendation Service — personalization requests flow here.",
      defaultSuggestions: ["What is collaborative filtering?", "How does Spotify's recommendation work?", "What is a listening event?"],
    },
    {
      id: 9,
      title: 'Add Offline Sync Service',
      explanation: "When you download a song, the encrypted audio file is stored on your device. The encryption key is stored server-side. When your subscription expires, the key is revoked and downloads become unplayable.",
      action: 'Press ⌘K, search for "Offline Sync Service", and add it to the canvas. Then connect Auth Service → Offline Sync Service.',
      searchHint: 'Offline Sync Service',
      why: "Offline sync is a Premium feature that drives subscription revenue. The server-side key model ensures downloaded songs stop working when a subscription lapses — protecting label licensing agreements.",
      messages: [
        { type: 'guide', content: "Spotify's offline downloads use a clever DRM model: the audio file is on your device, but the decryption key is on Spotify's servers." },
        { type: 'guide', content: "The Offline Sync Service manages download state, syncs playlist changes to offline devices, and revokes decryption keys when subscriptions expire. This is how Spotify honors its licensing agreements with labels." },
        { type: 'guide', content: 'Press ⌘K, search for "Offline Sync Service", add it, then connect: Auth Service → Offline Sync Service.' },
      ],
      validation: {
        requiredNodes: ['offline_sync'],
        requiredEdges: [{ from: 'Auth', to: 'Offline' }],
        successMessage: "Offline Sync Service added. Now the data layer.",
        errorMessage: 'Add an Offline Sync Service and connect it from the Auth Service.',
      },
      openingMessage: "Spotify stores the decryption key server-side. Connect Auth Service → Offline Sync Service — offline download permissions are validated at auth time.",
      defaultSuggestions: ["What is DRM?", "How does Spotify offline work?", "What happens when a subscription expires?"],
    },
    {
      id: 10,
      title: 'Add NoSQL Database',
      explanation: "Spotify stores user listening history, playlist data, and song metadata in a NoSQL database. Listening history is the training data for recommendations — billions of play events per day.",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas. Then connect Playlist Service → NoSQL Database.',
      searchHint: 'NoSQL Database',
      why: "Listening history has a flexible schema — different event types (play, skip, save, share) have different fields. NoSQL handles this without schema migrations as Spotify adds new event types.",
      messages: [
        { type: 'guide', content: "Spotify stores billions of listening events per day. This data is the foundation of the recommendation system." },
        { type: 'guide', content: "Every play, skip, save, and share is stored as an event. The Recommendation Service processes these events weekly to update Discover Weekly. NoSQL's flexible schema handles the different event types without migrations." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", add it, then connect: Playlist Service → NoSQL Database.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [{ from: 'Playlist', to: 'NoSQL' }],
        successMessage: "NoSQL Database added. Final step — master audio storage.",
        errorMessage: 'Add a NoSQL Database and connect it from the Playlist Service.',
      },
      openingMessage: "Spotify stores every play, skip, and save event. Connect Playlist Service → NoSQL Database — playlist and listening data is persisted here.",
      defaultSuggestions: ["What listening events does Spotify track?", "How does Spotify use skip data?", "What is event-driven ML?"],
    },
    {
      id: 11,
      title: 'Add Object Storage',
      explanation: "Master audio files and all transcoded versions are stored in Object Storage. Spotify stores 500 million audio files — the master WAV plus 5 quality versions for each of 100 million tracks.",
      action: 'Press ⌘K, search for "Object Storage", and add it to the canvas. Then connect Audio Transcoder → Object Storage.',
      searchHint: 'Object Storage',
      why: "Object storage is the only viable option for 500 million audio files. It's infinitely scalable, durable, and cheap per GB — the CDN pulls from here to serve listeners.",
      messages: [
        { type: 'guide', content: "Final step — where the actual audio files live. 500 million files in Object Storage." },
        { type: 'guide', content: "Object Storage holds the master WAV files and all 5 transcoded versions. The Audio CDN pulls from here to cache popular songs at edge nodes. The Offline Sync Service downloads encrypted versions to user devices." },
        { type: 'guide', content: 'Press ⌘K, search for "Object Storage", add it, then connect: Audio Transcoder → Object Storage.' },
      ],
      validation: {
        requiredNodes: ['object_storage'],
        requiredEdges: [{ from: 'Audio', to: 'Object' }],
        successMessage: "🎉 Tutorial complete! You've designed Spotify's core architecture from scratch.",
        errorMessage: 'Add an Object Storage and connect it from the Audio Transcoder.',
      },
      openingMessage: "Spotify stores 500 million audio files in Object Storage. Connect Audio Transcoder → Object Storage — transcoded files land here for CDN distribution.",
      defaultSuggestions: ["How does Spotify organize audio files?", "What is CDN pre-warming?", "How does Spotify handle new song uploads?"],
    },
  ],
};
