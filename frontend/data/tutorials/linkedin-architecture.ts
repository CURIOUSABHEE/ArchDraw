import type { TutorialData } from './chatgpt-architecture';

export const linkedinTutorial: TutorialData = {
  id: 'linkedin-architecture',
  title: 'How to Design LinkedIn Architecture',
  description: 'Build a professional social network for 1 billion members. Learn social graph traversal, feed ranking, connection degrees, job matching, and real-time messaging at scale.',
  difficulty: 'Advanced',
  category: 'Social Network',
  estimatedTime: '~30 mins',
  nodeCount: 11,
  stepCount: 11,
  icon: 'Linkedin',
  color: '#0a66c2',
  tags: ['Social Graph', 'Feed Ranking', 'Graph DB', 'Connections'],
  steps: [
    {
      id: 1,
      title: 'Start with the Web Client',
      explanation: "LinkedIn's web client handles the feed, profile pages, job search, and messaging. It's a React SPA that renders the feed, connection suggestions, and job recommendations — all personalized per user.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "The web client is where 1 billion members spend their time. It must render a personalized feed, connection suggestions, and job recommendations — all different for every user.",
      messages: [
        { type: 'guide', content: "Welcome to the LinkedIn Architecture tutorial. 1 billion members, 40 million job postings, and a social graph with billions of connections." },
        { type: 'guide', content: "LinkedIn's hardest problem: when you search for '2nd-degree connections who work at Google', LinkedIn must traverse a graph of billions of edges in milliseconds. That requires a purpose-built graph database." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Web Client added. Now the API layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "LinkedIn must traverse billions of graph edges in milliseconds. How does a graph database make that possible?",
      defaultSuggestions: ["What is a social graph?", "How does LinkedIn find 2nd-degree connections?", "What is graph traversal?"],
    },
    {
      id: 2,
      title: 'Add API Gateway',
      explanation: "All client requests flow through the API Gateway. LinkedIn uses a gateway that handles authentication, rate limiting, and routing to the correct microservice — whether that's the feed service, search service, or messaging service.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas.',
      searchHint: 'API Gateway',
      why: "LinkedIn has dozens of microservices. The API Gateway is the single entry point that routes requests, enforces rate limits, and handles auth token validation before any service sees the request.",
      messages: [
        { type: 'guide', content: "All LinkedIn requests flow through the API Gateway — feed loads, profile views, job searches, and messages." },
        { type: 'guide', content: "The gateway validates your auth token, checks rate limits (LinkedIn throttles aggressive scrapers), and routes to the right service. A profile view goes to the profile service; a job search goes to the search service." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [],
        successMessage: "API Gateway added. Now distribute the load.",
        errorMessage: 'Add an API Gateway node to the canvas.',
      },
      openingMessage: "LinkedIn throttles aggressive scrapers at the gateway. How does rate limiting protect the social graph?",
      defaultSuggestions: ["How does LinkedIn prevent scraping?", "What is rate limiting?", "How does API routing work?"],
    },
    {
      id: 3,
      title: 'Add Load Balancer',
      explanation: "LinkedIn's Load Balancer distributes traffic across service instances. LinkedIn uses consistent hashing to route requests for the same user to the same service instance — this improves cache hit rates for user-specific data.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.',
      searchHint: 'Load Balancer',
      why: "Consistent hashing means user A's requests always go to the same server. That server caches user A's connection graph in memory, making subsequent requests much faster.",
      messages: [
        { type: 'guide', content: "LinkedIn uses consistent hashing in its load balancer — your requests tend to go to the same server instance." },
        { type: 'guide', content: "This matters because the server can cache your connection graph in memory. If your requests were random, every request would need a cold graph lookup. Consistent hashing turns cold lookups into warm cache hits." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [],
        successMessage: "Load Balancer added. Now authentication.",
        errorMessage: 'Add a Load Balancer node to the canvas.',
      },
      openingMessage: "Consistent hashing routes the same user to the same server. How does that improve cache performance?",
      defaultSuggestions: ["What is consistent hashing?", "How does sticky sessions work?", "What is a warm cache?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: "LinkedIn's Auth Service handles OAuth login (Google, Microsoft), email/password, and SSO for enterprise accounts. It issues JWTs with short expiry and refresh tokens for long-lived sessions.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas.',
      searchHint: 'Auth Service',
      why: "LinkedIn has enterprise SSO requirements — companies configure SAML/OIDC so employees log in with their corporate credentials. The Auth Service must support both consumer OAuth and enterprise SSO.",
      messages: [
        { type: 'guide', content: "LinkedIn's auth handles both consumer logins (Google OAuth) and enterprise SSO (SAML for corporate accounts)." },
        { type: 'guide', content: "Enterprise customers configure their identity provider (Okta, Azure AD) to authenticate employees. The Auth Service validates the SAML assertion and issues a LinkedIn JWT. This is how 'Sign in with your company account' works." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [],
        successMessage: "Auth Service added. Now the core of LinkedIn — the graph.",
        errorMessage: 'Add an Auth Service node to the canvas.',
      },
      openingMessage: "LinkedIn supports enterprise SSO via SAML. How does that differ from standard OAuth?",
      defaultSuggestions: ["What is SAML?", "What is enterprise SSO?", "How does OAuth differ from SAML?"],
    },
    {
      id: 5,
      title: 'Add Feed Ranker',
      explanation: "LinkedIn's Feed Ranker scores every post for every user using a machine learning model. It considers: your relationship to the author, post engagement velocity, content type preference, and recency. The top-scored posts appear in your feed.",
      action: 'Press ⌘K, search for "Feed Ranker", and add it to the canvas.',
      searchHint: 'Feed Ranker',
      why: "Without ranking, your feed would be a chronological firehose. The Feed Ranker filters thousands of eligible posts down to the 20 most relevant — the ones you're most likely to engage with.",
      messages: [
        { type: 'guide', content: "LinkedIn's Feed Ranker scores thousands of candidate posts and picks the 20 most relevant for your feed." },
        { type: 'guide', content: "The model considers: how close you are to the author (1st degree > 2nd degree), how fast the post is getting likes, whether you prefer articles vs. job updates, and how old the post is. A viral post from a 2nd-degree connection can outrank a stale post from a 1st-degree connection." },
        { type: 'guide', content: 'Press ⌘K, search for "Feed Ranker", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['feed_ranker'],
        requiredEdges: [],
        successMessage: "Feed Ranker added. Now the social graph database.",
        errorMessage: 'Add a Feed Ranker node to the canvas.',
      },
      openingMessage: "LinkedIn's feed ranker scores thousands of posts per user. What signals matter most for ranking?",
      defaultSuggestions: ["How does LinkedIn rank feed posts?", "What is engagement velocity?", "How does connection degree affect ranking?"],
    },
    {
      id: 6,
      title: 'Add Graph Database',
      explanation: "LinkedIn's Graph Database stores the social graph — who is connected to whom, who follows whom, and company-to-employee relationships. LinkedIn built their own graph database called LinkedIn Graph (based on Voldemort) to handle billions of nodes and edges.",
      action: 'Press ⌘K, search for "Graph Database", and add it to the canvas.',
      searchHint: 'Graph Database',
      why: "Relational databases are terrible at graph traversal. Finding all 2nd-degree connections requires a JOIN of a JOIN — exponentially expensive. A graph database stores relationships as first-class citizens, making traversal O(connections) instead of O(table size).",
      messages: [
        { type: 'guide', content: "The Graph Database is LinkedIn's most critical infrastructure. It stores billions of connections and enables degree-of-separation queries in milliseconds." },
        { type: 'guide', content: "In a relational DB, finding 2nd-degree connections requires: SELECT friends of friends WHERE NOT already connected. That's a self-join on a billion-row table — catastrophically slow. A graph DB traverses edges directly: start at your node, hop to neighbors, hop again. O(connections) not O(table size)." },
        { type: 'guide', content: 'Press ⌘K, search for "Graph Database", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['graph_database'],
        requiredEdges: [],
        successMessage: "Graph Database added. Now the search layer.",
        errorMessage: 'Add a Graph Database node to the canvas.',
      },
      openingMessage: "Graph databases traverse edges directly instead of joining tables. Why is that so much faster for social graphs?",
      defaultSuggestions: ["What is a graph database?", "How does LinkedIn store connections?", "What is graph traversal complexity?"],
    },
    {
      id: 7,
      title: 'Add Search Service',
      explanation: "LinkedIn's Search Service powers people search, job search, and company search. It uses Elasticsearch under the hood with custom ranking that boosts results based on your network — a 1st-degree connection named 'John Smith' ranks above a stranger named 'John Smith'.",
      action: 'Press ⌘K, search for "Search Engine", and add it to the canvas.',
      searchHint: 'Search Engine',
      why: "LinkedIn search is network-aware. The same query returns different results for different users based on their connections. This requires combining full-text search scores with graph proximity scores.",
      messages: [
        { type: 'guide', content: "LinkedIn's search is network-aware — the same query returns different results for different users." },
        { type: 'guide', content: "When you search 'software engineer at Google', LinkedIn combines text relevance (does the profile match?) with network proximity (are they in your network?). A 2nd-degree connection ranks above a stranger with the same title. This requires the Search Service to query both Elasticsearch and the Graph Database." },
        { type: 'guide', content: 'Press ⌘K, search for "Search Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['search_engine'],
        requiredEdges: [],
        successMessage: "Search Service added. Now the timeline service.",
        errorMessage: 'Add a Search Service node to the canvas.',
      },
      openingMessage: "LinkedIn combines text relevance with network proximity in search. How do you merge two different scoring systems?",
      defaultSuggestions: ["How does LinkedIn rank search results?", "What is network-aware search?", "How does Elasticsearch work?"],
    },
    {
      id: 8,
      title: 'Add Timeline Service',
      explanation: "The Timeline Service aggregates posts from your connections and follows, applies the Feed Ranker scores, and assembles your feed. It uses a pull model — when you open LinkedIn, it fetches recent posts from your connections and ranks them in real-time.",
      action: 'Press ⌘K, search for "Timeline Service", and add it to the canvas.',
      searchHint: 'Timeline Service',
      why: "LinkedIn uses a pull model (not push) because the average user has 500 connections. Pushing every post to every follower's feed would be 500x write amplification. Pull is cheaper for LinkedIn's connection density.",
      messages: [
        { type: 'guide', content: "The Timeline Service assembles your feed on demand using a pull model." },
        { type: 'guide', content: "When you open LinkedIn, the Timeline Service fetches recent posts from your 500 connections, passes them to the Feed Ranker, and returns the top 20. LinkedIn chose pull over push because 500 connections × millions of posts = too many writes. Twitter uses push for celebrities (fan-out on write) but LinkedIn's connection graph is denser." },
        { type: 'guide', content: 'Press ⌘K, search for "Timeline Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['timeline_service'],
        requiredEdges: [],
        successMessage: "Timeline Service added. Now the message queue.",
        errorMessage: 'Add a Timeline Service node to the canvas.',
      },
      openingMessage: "LinkedIn uses pull for feeds while Twitter uses push for celebrities. When does each model make sense?",
      defaultSuggestions: ["What is fan-out on read vs write?", "Why does LinkedIn use pull model?", "How does feed assembly work?"],
    },
    {
      id: 9,
      title: 'Add Message Queue',
      explanation: "LinkedIn's Message Queue (Kafka) handles async events: new post published, connection request sent, job application submitted. These events fan out to multiple consumers — the notification service, the analytics pipeline, and the feed indexer.",
      action: 'Press ⌘K, search for "Message Queue", and add it to the canvas.',
      searchHint: 'Message Queue',
      why: "When you post on LinkedIn, 10 things need to happen: notify your connections, index the post for search, update analytics, check for spam, etc. Kafka decouples the post action from all downstream processing.",
      messages: [
        { type: 'guide', content: "LinkedIn actually built Kafka — it was created by LinkedIn engineers and open-sourced in 2011." },
        { type: 'guide', content: "When you post on LinkedIn, a single event goes to Kafka. Multiple consumers process it independently: the notification service sends alerts to your connections, the search indexer makes it searchable, the spam detector checks for violations, the analytics pipeline records the event. All decoupled, all async." },
        { type: 'guide', content: 'Press ⌘K, search for "Message Queue", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['message_queue'],
        requiredEdges: [],
        successMessage: "Message Queue added. Now the data layer.",
        errorMessage: 'Add a Message Queue node to the canvas.',
      },
      openingMessage: "LinkedIn invented Kafka. How does event streaming decouple the post action from downstream processing?",
      defaultSuggestions: ["What is Apache Kafka?", "What is event-driven architecture?", "How does fan-out work with Kafka?"],
    },
    {
      id: 10,
      title: 'Add NoSQL Database',
      explanation: "LinkedIn stores profiles, posts, job listings, and activity data in NoSQL databases. Profile data is document-oriented — each profile has a flexible schema with different sections for different users (some have patents, some have publications).",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.',
      searchHint: 'NoSQL Database',
      why: "LinkedIn profiles have wildly different schemas — a researcher has publications, a designer has a portfolio, an executive has board memberships. NoSQL's flexible schema handles this without ALTER TABLE migrations.",
      messages: [
        { type: 'guide', content: "LinkedIn profiles are stored as documents in NoSQL — each profile is a JSON document with different fields per user." },
        { type: 'guide', content: "A researcher's profile has publications and patents. A designer's has portfolio links. An executive's has board memberships. In a relational DB, you'd need nullable columns for every possible field — or complex joins. NoSQL stores each profile as a self-contained document." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [],
        successMessage: "NoSQL Database added. Final step — the cache layer.",
        errorMessage: 'Add a NoSQL Database node to the canvas.',
      },
      openingMessage: "LinkedIn profiles have wildly different schemas. How does document storage handle that better than relational?",
      defaultSuggestions: ["What is document storage?", "How does NoSQL handle schema flexibility?", "What is LinkedIn's data model?"],
    },
    {
      id: 11,
      title: 'Add In-Memory Cache',
      explanation: "LinkedIn caches connection graphs, profile data, and feed results in an in-memory cache. Your connection list is cached so degree-of-separation queries don't hit the graph database on every request.",
      action: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas.',
      searchHint: 'In-Memory Cache',
      why: "Graph traversal is expensive even with a graph database. Caching your first-degree connections in memory means 'people you may know' suggestions can be computed without a live graph query.",
      messages: [
        { type: 'guide', content: "The In-Memory Cache stores your connection graph so LinkedIn doesn't traverse the graph database on every page load." },
        { type: 'guide', content: "Your 500 connections are cached in memory. When LinkedIn computes 'people you may know', it uses the cached graph to find friends-of-friends without hitting the graph database. Cache invalidation happens when you add or remove a connection." },
        { type: 'guide', content: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['in_memory_cache'],
        requiredEdges: [],
        successMessage: "🎉 Tutorial complete! You've designed LinkedIn's core architecture from scratch.",
        errorMessage: 'Add an In-Memory Cache node to the canvas.',
      },
      openingMessage: "LinkedIn caches your connection graph in memory. How does cache invalidation work when you add a new connection?",
      defaultSuggestions: ["How does LinkedIn cache connections?", "What is cache invalidation?", "How does 'people you may know' work?"],
    },
  ],
};
