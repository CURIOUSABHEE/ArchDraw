import type { TutorialData } from './chatgpt-architecture';

export const twitterTutorial: TutorialData = {
  id: 'twitter-architecture',
  title: 'How to Design Twitter/X Architecture',
  description: 'Solve the fan-out problem serving 500M users. Build tweet delivery, real-time feeds, trending topics, and a timeline that scales to millions of concurrent reads.',
  difficulty: 'Advanced',
  category: 'Social Media',
  estimatedTime: '~32 mins',
  nodeCount: 11,
  stepCount: 11,
  icon: 'Twitter',
  color: '#1da1f2',
  tags: ['Fan-out', 'Timeline', 'Trending', 'Cache', 'Graph'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "Twitter's client is the web app and mobile app. Users compose tweets, scroll their timeline, and receive real-time notifications. The client maintains a WebSocket connection for live updates.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "Every architecture starts from the user. Twitter's web and mobile clients define the two core flows: writing tweets (low volume, high fanout) and reading timelines (extremely high volume).",
      messages: [
        { type: 'guide', content: "Welcome to the Twitter/X Architecture tutorial. 500 million users, 500 million tweets per day, and the hardest fan-out problem in social media." },
        { type: 'guide', content: "Twitter's client handles two very different flows: writing a tweet (rare, but triggers massive fan-out) and reading your timeline (happens constantly, must be instant)." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Client added. Now the entry layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "Twitter has two flows: writing tweets and reading timelines. Why are they architecturally so different?",
      defaultSuggestions: ["What is the fan-out problem?", "How does Twitter's timeline work?", "What comes after the client?"],
    },
    {
      id: 2,
      title: 'Add the API Gateway',
      explanation: "All requests from Twitter clients flow through an API Gateway. It handles OAuth authentication, rate limiting (300 requests per 15 minutes for free tier), and routes to the correct backend service.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas.',
      searchHint: 'API Gateway',
      why: "Twitter's API Gateway enforces rate limits that protect the backend from abuse. Without it, a single bot could flood the tweet service with millions of requests.",
      messages: [
        { type: 'guide', content: "Every tweet, like, and timeline request hits the API Gateway first. It's the enforcer of Twitter's rate limits." },
        { type: 'guide', content: "Twitter's API Gateway validates OAuth tokens, enforces per-user rate limits (300 requests/15 min), and routes to the right service — tweet service, timeline service, search, etc." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [],
        successMessage: "API Gateway added. Now distribute the traffic.",
        errorMessage: 'Add an API Gateway node to the canvas.',
      },
      openingMessage: "Twitter's API Gateway enforces rate limits. How does it prevent bots from abusing the platform?",
      defaultSuggestions: ["What is OAuth?", "How does Twitter rate limiting work?", "What is a rate limit window?"],
    },
    {
      id: 3,
      title: 'Add Load Balancer',
      explanation: "Twitter's API Gateway routes to a Load Balancer that distributes requests across thousands of application servers. Twitter handles 6,000 tweets per second at peak.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.',
      searchHint: 'Load Balancer',
      why: "At 6,000 tweets per second, no single server can handle the write load. The load balancer enables horizontal scaling across Twitter's massive server fleet.",
      messages: [
        { type: 'guide', content: "6,000 tweets per second at peak. The load balancer distributes this across thousands of servers." },
        { type: 'guide', content: "Twitter uses consistent hashing in their load balancer — a user's requests always route to the same server cluster, which helps with connection state and caching." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [],
        successMessage: "Load Balancer added. Now the auth layer.",
        errorMessage: 'Add a Load Balancer node to the canvas.',
      },
      openingMessage: "Twitter handles 6,000 tweets per second. How does consistent hashing help with load balancing?",
      defaultSuggestions: ["What is consistent hashing?", "How does Twitter scale horizontally?", "What is a health check?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: "Twitter uses OAuth 2.0 for authentication. The Auth Service validates tokens, checks account status (suspended, verified, etc.), and enforces per-account rate limits.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas.',
      searchHint: 'Auth Service',
      why: "Auth is critical for Twitter's trust and safety. A suspended account must be blocked at the auth layer before any tweet reaches the fan-out pipeline.",
      messages: [
        { type: 'guide', content: "Before any tweet is processed, the user must be authenticated and their account status checked." },
        { type: 'guide', content: "The Auth Service validates OAuth tokens and checks account status — suspended accounts are blocked here, before any fan-out happens. This prevents banned users from reaching millions of followers." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [],
        successMessage: "Auth added. Now the tweet creation service.",
        errorMessage: 'Add an Auth Service node to the canvas.',
      },
      openingMessage: "Twitter checks account status at auth time. Why is it important to block suspended accounts before fan-out?",
      defaultSuggestions: ["What is OAuth 2.0?", "How does Twitter handle account suspension?", "What is a verified account?"],
    },
    {
      id: 5,
      title: 'Add Tweet Service',
      explanation: "The Tweet Service handles tweet creation and storage. Every tweet gets a Snowflake ID — a 64-bit integer encoding timestamp, datacenter ID, and sequence number. This gives chronological ordering without a central counter.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas.',
      searchHint: 'Microservice',
      why: "Snowflake IDs are Twitter's solution to distributed ID generation. They're sortable by time, unique across datacenters, and generated without any central coordination.",
      messages: [
        { type: 'guide', content: "The Tweet Service creates and stores tweets. Every tweet gets a Snowflake ID — Twitter's invention for distributed unique IDs." },
        { type: 'guide', content: "A Snowflake ID is a 64-bit integer: 41 bits for timestamp (milliseconds since 2010), 10 bits for machine ID, 12 bits for sequence. This means 4,096 unique IDs per millisecond per machine — no central counter needed." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Tweet Service added. Now the fan-out layer.",
        errorMessage: 'Add a Microservice (Tweet Service) to the canvas.',
      },
      openingMessage: "Twitter's Snowflake ID encodes timestamp + machine ID + sequence. Why is that better than auto-increment?",
      defaultSuggestions: ["What is a Snowflake ID?", "Why not use UUID?", "How does Twitter store tweets?"],
    },
    {
      id: 6,
      title: 'Add Fan-out Service',
      explanation: "When Elon Musk tweets, 150 million followers need to see it. The Fan-out Service uses a hybrid model: regular users get push fan-out (tweet written to all follower timelines immediately), celebrities use pull fan-out (followers fetch on demand).",
      action: 'Press ⌘K, search for "Fan-out Service", and add it to the canvas.',
      searchHint: 'Fan-out Service',
      why: "Push fan-out for 150M followers would require 150M writes per tweet. Twitter's hybrid model caps the write amplification by using pull for celebrity accounts.",
      messages: [
        { type: 'guide', content: "Here's the hardest problem in Twitter's architecture: when a celebrity tweets, how do you deliver it to 150 million followers in under 5 seconds?" },
        { type: 'guide', content: "Twitter uses hybrid fan-out. Regular users (<1M followers): push model — tweet is written to all follower timeline caches immediately. Celebrities (>1M followers): pull model — followers fetch their tweets on demand when opening the app." },
        { type: 'guide', content: 'Press ⌘K, search for "Fan-out Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['fanout_service'],
        requiredEdges: [],
        successMessage: "Fan-out Service added. Now the timeline layer.",
        errorMessage: 'Add a Fan-out Service node to the canvas.',
      },
      openingMessage: "Twitter uses hybrid fan-out — push for regular users, pull for celebrities. What's the threshold and why?",
      defaultSuggestions: ["What is push vs pull fan-out?", "How does Twitter handle celebrity accounts?", "What is write amplification?"],
    },
    {
      id: 7,
      title: 'Add Timeline Service',
      explanation: "The Timeline Service assembles and serves your personalized feed. Your timeline is pre-computed and stored in Redis. Reading it is a single Redis read — not a database query across millions of tweets.",
      action: 'Press ⌘K, search for "Timeline Service", and add it to the canvas.',
      searchHint: 'Timeline Service',
      why: "Pre-computing timelines in Redis is what makes Twitter load instantly. Without it, every timeline request would require joining millions of tweets from thousands of followed accounts.",
      messages: [
        { type: 'guide', content: "Your Twitter timeline loads in under 100ms. How? It's pre-computed and cached in Redis." },
        { type: 'guide', content: "The Timeline Service maintains a Redis sorted set per user — a list of tweet IDs ordered by time. When you open Twitter, it reads the top 200 tweet IDs from Redis and fetches their content. No database query needed." },
        { type: 'guide', content: 'Press ⌘K, search for "Timeline Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['timeline_service'],
        requiredEdges: [],
        successMessage: "Timeline Service added. Now trending topics.",
        errorMessage: 'Add a Timeline Service node to the canvas.',
      },
      openingMessage: "Twitter pre-computes timelines in Redis sorted sets. How does that make timeline reads O(1)?",
      defaultSuggestions: ["What is a Redis sorted set?", "How does Twitter's timeline work?", "What is pre-computation?"],
    },
    {
      id: 8,
      title: 'Add Trending Service',
      explanation: "The Trending Service computes trending topics in real-time using sliding window counters. Every hashtag gets a counter per geographic region, updated every 30 seconds. Topics trending in NYC may not trend in Tokyo.",
      action: 'Press ⌘K, search for "Trending Service", and add it to the canvas.',
      searchHint: 'Trending Service',
      why: "Trending topics are computed from billions of tweets using approximate counting algorithms. Exact counts would require too much memory — Twitter uses Count-Min Sketch for space-efficient frequency estimation.",
      messages: [
        { type: 'guide', content: "How does Twitter know what's trending? Every hashtag gets a sliding window counter per geographic region." },
        { type: 'guide', content: "The Trending Service uses Count-Min Sketch — a probabilistic data structure that estimates hashtag frequency using a fraction of the memory exact counting would require. Updated every 30 seconds, localized by region." },
        { type: 'guide', content: 'Press ⌘K, search for "Trending Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['trending_service'],
        requiredEdges: [],
        successMessage: "Trending Service added. Now the social graph.",
        errorMessage: 'Add a Trending Service node to the canvas.',
      },
      openingMessage: "Twitter uses Count-Min Sketch for trending. Why not just count exactly?",
      defaultSuggestions: ["What is Count-Min Sketch?", "How does Twitter localize trends?", "What is a sliding window counter?"],
    },
    {
      id: 9,
      title: 'Add Graph Database',
      explanation: "Twitter's social graph — who follows whom — is stored in a Graph Database. With 500M users and billions of follow relationships, graph traversal (finding mutual followers, suggested follows) requires a database optimized for relationship queries.",
      action: 'Press ⌘K, search for "Graph Database", and add it to the canvas.',
      searchHint: 'Graph Database',
      why: "SQL JOINs across billions of follow relationships would be impossibly slow. Graph databases store relationships as first-class citizens, making traversal queries orders of magnitude faster.",
      messages: [
        { type: 'guide', content: "Twitter has billions of follow relationships. Storing them in SQL and doing JOINs would be too slow." },
        { type: 'guide', content: "Twitter built FlockDB — a custom graph database optimized for social graph traversal. It stores directed edges (A follows B) and supports fast queries like 'who follows this user' and 'who does this user follow'." },
        { type: 'guide', content: 'Press ⌘K, search for "Graph Database", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['graph_database'],
        requiredEdges: [],
        successMessage: "Graph Database added. Now tweet storage.",
        errorMessage: 'Add a Graph Database node to the canvas.',
      },
      openingMessage: "Twitter built FlockDB for social graph storage. What makes graph databases faster than SQL for relationship queries?",
      defaultSuggestions: ["What is FlockDB?", "How does Twitter store follow relationships?", "What is graph traversal?"],
    },
    {
      id: 10,
      title: 'Add NoSQL Database',
      explanation: "Tweets themselves are stored in a NoSQL database (Twitter uses Manhattan, their own distributed key-value store). Each tweet is a document with text, media URLs, engagement counts, and metadata.",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.',
      searchHint: 'NoSQL Database',
      why: "Tweets have a flexible schema — different tweet types (text, image, video, poll, thread) have different fields. NoSQL's flexible document model handles this without schema migrations.",
      messages: [
        { type: 'guide', content: "Tweets are stored in a NoSQL database. Twitter built Manhattan — their own distributed key-value store — for this." },
        { type: 'guide', content: "Manhattan stores tweets by Snowflake ID. Lookups are O(1) by ID. The flexible schema handles all tweet types: text, images, polls, threads, spaces — each with different fields." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [],
        successMessage: "NoSQL Database added. Final step — the timeline cache.",
        errorMessage: 'Add a NoSQL Database node to the canvas.',
      },
      openingMessage: "Twitter built Manhattan for tweet storage. Why build a custom database instead of using Cassandra or DynamoDB?",
      defaultSuggestions: ["What is Manhattan?", "How does Twitter store tweet content?", "What is a key-value store?"],
    },
    {
      id: 11,
      title: 'Add In-Memory Cache',
      explanation: "Twitter caches pre-computed timelines, tweet content, and user profiles in Redis. The timeline cache holds the top 800 tweet IDs per user. Cache hit rate exceeds 99% — almost no timeline reads touch the database.",
      action: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas.',
      searchHint: 'In-Memory Cache',
      why: "Without the timeline cache, every feed load would require reading from the NoSQL database and assembling tweets from thousands of followed accounts. Redis makes it a single sub-millisecond read.",
      messages: [
        { type: 'guide', content: "Final step — the cache that makes everything fast. Twitter stores pre-computed timelines in Redis." },
        { type: 'guide', content: "Each user has a Redis sorted set holding their top 800 tweet IDs. Timeline reads hit Redis 99%+ of the time — the database is almost never touched for feed reads. This is what makes Twitter feel instant." },
        { type: 'guide', content: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['in_memory_cache'],
        requiredEdges: [],
        successMessage: "🎉 Tutorial complete! You've designed Twitter's core architecture from scratch.",
        errorMessage: 'Add an In-Memory Cache node to the canvas.',
      },
      openingMessage: "Twitter's timeline cache has a 99%+ hit rate. What happens on a cache miss?",
      defaultSuggestions: ["What is a cache miss?", "How does Twitter handle cache eviction?", "What is a Redis sorted set?"],
    },
  ],
};
