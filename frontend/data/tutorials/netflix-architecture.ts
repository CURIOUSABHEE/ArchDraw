import { step, level, tutorial, component, edge, msg } from '@/lib/tutorial/factories';
import { buildAction, buildFirstStepAction, buildCelebration, buildOpeningL1, buildOpeningL2, buildOpeningL3 } from '@/lib/tutorial/defaults';
import type { Tutorial } from '@/lib/tutorial/types';

const l1 = level({
  level: 1,
  title: 'The Foundation',
  subtitle: 'Build a working Netflix in 7 steps',
  description: 'The minimum viable Netflix — a system that can actually stream video to a user. Client, DNS, CDN, API gateway, load balancer, streaming service, and object storage. 7 components, 6 connections.',
  estimatedTime: '~20 mins',
  unlocks: 'Production Layer',
  contextMessage: 'Level 1: The foundation. What does a system need to stream video to 270 million subscribers?',
  steps: [
    step({
      id: 1,
      title: 'Add the Client',
      explanation: "Netflix's client runs on 2,000+ device types — Smart TVs, phones, tablets, browsers, and game consoles. Each implements adaptive bitrate streaming, switching video quality every few seconds based on available bandwidth.",
      action: buildFirstStepAction('Web'),
      why: "The client is the entry point for every stream, search, and recommendation request. Netflix has to support wildly different clients simultaneously — from a 4K TV with 50Mbps to a phone on 3G. Every architectural decision downstream is shaped by this diversity.",
      component: component('client_web', 'Web'),
      openingMessage: buildOpeningL1('Netflix', 'Web Client', 'run on 2,000+ device types simultaneously — Smart TVs, phones, tablets, browsers — implementing adaptive bitrate streaming that switches quality every few seconds', "Every architectural decision downstream is shaped by this diversity — from a 4K TV with 50Mbps to a phone on 3G.", 'Web'),
      celebrationMessage: buildCelebration('Web Client', 'the canvas', 'Netflix has 270 million subscribers across 190 countries, all connecting through clients on hundreds of different device types.', 'DNS and the CDN'),
      messages: [
        msg("Welcome to the Netflix Architecture tutorial. 270 million subscribers, 190 countries, 1 billion hours of content streamed per month."),
        msg("Netflix's client implements adaptive bitrate streaming — it monitors your bandwidth every few seconds and switches quality automatically. That quality switch is a new CDN request to a different encoded file."),
        msg('Press ⌘K, search for "Web", and add it to the canvas.'),
      ],
      requiredNodes: ['client_web'],
      requiredEdges: [],
      successMessage: 'Client added. Now the routing layer.',
      errorMessage: 'Add a Web client node to the canvas first.',
    }),
    step({
      id: 2,
      title: 'Add DNS and CDN',
      explanation: "Netflix's Open Connect CDN has 15,000+ servers physically embedded inside ISPs worldwide. 94% of Netflix traffic is served from these edge nodes — your video might come from a server in the same building as your router.",
      action: buildAction('DNS', 'Web', 'DNS', 'every Netflix request being routed to the nearest edge node before anything else happens'),
      why: "Without DNS-based routing, every user would hit the same data center regardless of location. Without the CDN, every video byte would travel from Netflix's origin servers — impossible at 270M subscribers.",
      component: component('dns', 'DNS'),
      openingMessage: "Netflix's CDN serves 94% of traffic without hitting origin. How does DNS route you to the nearest edge node?",
      celebrationMessage: buildCelebration('DNS and CDN', 'Client', "Netflix's Open Connect CDN has over 15,000 server locations embedded inside ISPs globally — it's one of the largest private CDNs ever built.", 'the API Gateway and Load Balancer'),
      connectingMessage: "DNS is on the canvas. Now connect Web → DNS. Hover over the Web node until a circle appears on its edge, then drag to DNS. This connection means every Netflix request first hits DNS, which uses Anycast routing to direct you to the nearest Open Connect CDN appliance.",
      messages: [
        msg("94% of Netflix traffic never reaches their origin servers. It's served from 15,000+ CDN nodes embedded inside ISPs worldwide."),
        msg("DNS uses Anycast routing — the same domain resolves to different IPs depending on where you are, directing you to the nearest Open Connect appliance. Your video might come from a server in the same building as your router."),
      ],
      requiredNodes: ['dns'],
      requiredEdges: [edge('client_web', 'dns')],
      successMessage: 'DNS and CDN added. Now the API layer.',
      errorMessage: 'Add a DNS node and connect it from the Client.',
    }),
    step({
      id: 3,
      title: 'Add API Gateway and Load Balancer',
      explanation: "Netflix's Zuul API Gateway handles all non-video requests — login, search, watchlist, recommendations, account settings. It routes, authenticates, and rate-limits billions of API calls per day. The Load Balancer distributes those requests across thousands of application servers.",
      action: buildAction('API Gateway', 'DNS', 'API Gateway', 'all dynamic requests being routed through a single controlled entry point'),
      why: "The CDN handles video bytes. The API Gateway handles everything else. Without it, each of Netflix's hundreds of microservices would need to implement auth and rate limiting independently.",
      component: component('api_gateway', 'API Gateway'),
      openingMessage: "Netflix's Zuul gateway handles 2 billion API requests per day. What does it do that the CDN can't?",
      celebrationMessage: buildCelebration('API Gateway', 'DNS', "Netflix's API Gateway processes over 2 billion requests per day and routes them to hundreds of microservices — all through a single entry point.", 'the Auth Service'),
      connectingMessage: "API Gateway is on the canvas. Now connect DNS → API Gateway. Hover over DNS until a circle appears on its edge, then drag to API Gateway. This connection means all non-video requests — login, search, watchlist — flow from DNS through the Zuul gateway before reaching any backend service.",
      messages: [
        msg("The CDN handles video bytes. Everything else — login, search, your watchlist — goes through the API Gateway."),
        msg("Netflix's Zuul gateway processes over 2 billion API requests per day. When Netflix enforced password-sharing rules, the gateway was where device fingerprinting logic was added — one enforcement point for every request."),
      ],
      requiredNodes: ['api_gateway'],
      requiredEdges: [edge('dns', 'api_gateway')],
      successMessage: 'API Gateway added. Now the auth layer.',
      errorMessage: 'Add an API Gateway and connect it from DNS.',
    }),
    step({
      id: 4,
      title: 'Add Auth Service',
      explanation: "Netflix's Auth Service validates OAuth tokens, checks subscription plans, and counts active streams in real time. It enforces concurrent stream limits — if you're on a 2-stream plan and try to start a 3rd, auth blocks it before the request reaches any other service.",
      action: buildAction('Auth Service (JWT)', 'API Gateway', 'Auth Service', 'every request being validated for subscription plan and active stream count before reaching any backend service'),
      why: "Auth is the enforcement layer for Netflix's entire subscription model. Centralizing it means one change — like the password-sharing crackdown — applies everywhere instantly without touching any downstream service.",
      component: component('auth_service', 'Auth Service (JWT)', 'Auth Service (JWT)'),
      openingMessage: "Netflix validates auth tokens in under 5ms at peak. How does it enforce concurrent stream limits in real time?",
      celebrationMessage: buildCelebration('Auth Service', 'API Gateway', "Netflix's Auth Service validates tokens in under 5ms at peak load across 270 million subscribers. It's one of the highest-stakes auth systems ever built.", 'the User Service and SQL Database'),
      connectingMessage: "Auth Service is on the canvas. Now connect API Gateway → Auth Service. Hover over API Gateway until a circle appears on its edge, then drag to Auth Service. This connection means every request is validated for subscription plan and active stream count before reaching any backend service.",
      messages: [
        msg("Netflix's Auth Service validates tokens in under 5ms at peak load across 270 million subscribers. It checks your OAuth token, subscription plan, and active stream count in real time."),
        msg("The password-sharing enforcement works entirely here — it checks whether the requesting device is in the account's registered household on every single play request."),
      ],
      requiredNodes: ['auth_service'],
      requiredEdges: [edge('api_gateway', 'auth_service')],
      successMessage: 'Auth Service added. Now the user data layer.',
      errorMessage: 'Add an Auth Service and connect it from the API Gateway.',
    }),
    step({
      id: 5,
      title: 'Add Load Balancer',
      explanation: "Netflix's Load Balancer distributes API requests across thousands of application servers. It enables horizontal scaling — when a new season of Stranger Things drops and traffic spikes 10x, new servers spin up and the load balancer routes to them automatically.",
      action: buildAction('Load Balancer', 'API Gateway', 'Load Balancer', 'traffic being distributed across thousands of application servers to enable horizontal scaling'),
      why: "Netflix sees massive traffic spikes when popular content drops. The load balancer is what makes horizontal scaling possible — add more servers without changing anything else in the architecture.",
      component: component('load_balancer', 'Load Balancer'),
      openingMessage: "Netflix traffic spikes 10x when a new season drops. How does the load balancer handle that without downtime?",
      celebrationMessage: buildCelebration('Load Balancer', 'API Gateway', "Netflix's load balancers handle over 1 trillion requests per day — the same horizontal scaling pattern applies across every major distributed system.", 'the Streaming Service and Object Storage'),
      connectingMessage: "Load Balancer is on the canvas. Now connect API Gateway → Load Balancer. Hover over API Gateway until a circle appears on its edge, then drag to Load Balancer. This connection means API traffic is distributed across thousands of application servers — when Stranger Things drops and traffic spikes 10x, new servers spin up and the balancer routes to them automatically.",
      messages: [
        msg("When a new season of Stranger Things drops, Netflix traffic spikes 10x. The Load Balancer distributes requests across thousands of servers — new instances spin up automatically and the balancer routes to them."),
        msg("Connect: API Gateway → Load Balancer."),
      ],
      requiredNodes: ['load_balancer'],
      requiredEdges: [edge('api_gateway', 'load_balancer')],
      successMessage: 'Load Balancer added. Now the streaming layer.',
      errorMessage: 'Add a Load Balancer and connect it from the API Gateway.',
    }),
    step({
      id: 6,
      title: 'Add Streaming Service',
      explanation: "The Streaming Service generates time-limited pre-signed URLs for video segments — a URL that grants access to a specific chunk for only 60 seconds. The CDN uses these URLs to fetch from Object Storage. If someone copies the URL, it expires before they can share it.",
      action: buildAction('Microservice', 'Load Balancer', 'Streaming Service', 'time-limited pre-signed URLs being generated so the CDN can securely fetch video from object storage'),
      why: "Pre-signed URLs are the elegant solution to video DRM — they grant temporary access without making files publicly accessible or proxying every byte through Netflix's servers. Without them, Netflix would either expose all video files or add massive latency.",
      component: component('microservice', 'Microservice'),
      openingMessage: "Netflix stores 120+ versions of every title. How do pre-signed URLs prevent unauthorized downloads?",
      celebrationMessage: buildCelebration('Streaming Service', 'Load Balancer', "Netflix stores every title in 120+ different formats — every resolution, codec, and bitrate combination. That's petabytes of video in object storage.", 'the Transcoding Worker'),
      connectingMessage: "Streaming Service is on the canvas. Now connect Load Balancer → Streaming Service. Hover over Load Balancer until a circle appears on its edge, then drag to Streaming Service. This connection means load-balanced requests reach the service that generates 60-second pre-signed URLs for CDN video delivery.",
      messages: [
        msg("The Streaming Service generates pre-signed URLs that expire in 60 seconds. The CDN uses these to fetch video from object storage. If someone copies the URL, it expires before they can share it — elegant DRM without proxying every byte."),
        msg("Connect: Load Balancer → Streaming Service."),
      ],
      requiredNodes: ['microservice'],
      requiredEdges: [edge('load_balancer', 'microservice')],
      successMessage: 'Streaming Service added. Now the storage layer.',
      errorMessage: 'Add a Microservice (Streaming Service) and connect it from the Load Balancer.',
    }),
    step({
      id: 7,
      title: 'Add Object Storage',
      explanation: "Object Storage holds petabytes of encoded video files. Netflix stores every title in 120+ different formats — every resolution (480p through 4K HDR), every codec (H.264, VP9, AV1), every bitrate. Object storage is the only viable option at this scale.",
      action: buildAction('Object Storage', 'Streaming Service', 'Object Storage', 'petabytes of encoded video files being stored durably and served to the CDN on demand'),
      why: "Object storage is infinitely scalable, 11 nines durable, and cheap per GB — the only viable option for petabyte-scale video. The CDN pulls from here using the pre-signed URLs the Streaming Service generates.",
      component: component('object_storage', 'Object Storage', 'Object Storage (S3-like)'),
      openingMessage: "Netflix stores petabytes of video in 120+ formats per title. Why is object storage the only viable option at this scale?",
      celebrationMessage: buildCelebration('Object Storage', 'Streaming Service', "Netflix stores over 1 exabyte of video content — every title in every resolution, codec, and language combination. Object storage handles this at 11 nines of durability.", 'Level 2'),
      connectingMessage: "Object Storage is on the canvas. Now connect Streaming Service → Object Storage. Hover over Streaming Service until a circle appears on its edge, then drag to Object Storage. This connection means the Streaming Service fetches video segments from object storage to generate pre-signed CDN URLs.",
      messages: [
        msg("Netflix stores every title in 120+ encoded versions — every resolution, codec, and bitrate combination. That's petabytes of video in object storage."),
        msg("Connect: Streaming Service → Object Storage."),
      ],
      requiredNodes: ['object_storage'],
      requiredEdges: [edge('microservice', 'object_storage')],
      successMessage: 'Level 1 complete. You have a working Netflix streaming foundation.',
      errorMessage: 'Add Object Storage and connect it from the Streaming Service.',
    }),
  ],
});

const l2 = level({
  level: 2,
  title: 'Production Ready',
  subtitle: 'Add recommendations, Kafka, and data storage',
  description: 'Your Netflix foundation streams video. Now add what makes Netflix actually Netflix: ML-powered recommendations, Kafka event streaming, and the data layer that stores viewing history for 270 million users.',
  estimatedTime: '~20 mins',
  unlocks: 'Expert Architecture',
  prerequisite: 'Builds on your Level 1 diagram',
  contextMessage: 'Level 2: Production Ready. Add the recommendation engine, Kafka event streaming, and data storage.',
  steps: [
    step({
      id: 8,
      title: 'Add Recommendation Service',
      explanation: "Netflix's recommendation algorithm drives 80% of what people watch. The Recommendation Service uses a two-stage system: a candidate generation model narrows 15,000+ titles to ~500 candidates, then a ranking model scores those 500 using deep learning. Redis caches pre-computed recommendations for all 270M+ users.",
      action: buildAction('Recommendation Service', 'Load Balancer', 'Recommendation Service', '80% of Netflix viewing being driven by personalized recommendations computed from viewing history'),
      why: "Recommendations drive 80% of Netflix viewing. Without them, users spend an average of 90 seconds searching before giving up — which is why Netflix considers the recommendation system core to retention, not a nice-to-have.",
      component: component('recommendation_service', 'Recommendation Service'),
      openingMessage: "Netflix's recommendation algorithm drives 80% of viewing. How does the two-stage candidate generation and ranking system work?",
      celebrationMessage: buildCelebration('Recommendation Service', 'Load Balancer', "Netflix's recommendation models consider your viewing history, time of day, device type, and what similar users watched — all computed in real time. The models are retrained weekly on billions of viewing events.", 'the Kafka event streaming layer'),
      connectingMessage: "Recommendation Service is on the canvas. Now connect Load Balancer → Recommendation Service. Hover over Load Balancer until a circle appears on its edge, then drag to Recommendation Service. This connection means load-balanced requests reach the two-stage ML system that drives 80% of what Netflix subscribers watch.",
      messages: [
        msg("Level 2 — Production Ready. 80% of what you watch on Netflix comes from recommendations, not search. That single statistic explains why Netflix invests more in ML infrastructure than almost any other feature."),
        msg("The two-stage system: candidate generation narrows 15,000+ titles to ~500 for you. Then a ranking model scores those 500 using your viewing history, time of day, and what similar users watched."),
      ],
      requiredNodes: ['recommendation_service'],
      requiredEdges: [edge('load_balancer', 'recommendation_service')],
      successMessage: 'Recommendation Service added. Now the event streaming layer.',
      errorMessage: 'Add a Recommendation Service and connect it from the Load Balancer.',
    }),
    step({
      id: 9,
      title: 'Add Kafka Streaming',
      explanation: "Netflix's Kafka cluster processes billions of events per day — every play, pause, seek, skip intro, and abandon event from every subscriber. These events feed the recommendation system, analytics, and content acquisition teams in near real-time.",
      action: buildAction('Kafka / Streaming', 'Streaming Service', 'Kafka Streaming', 'billions of viewing events per day being streamed to the recommendation system and analytics pipeline'),
      why: "Without Kafka, viewing events would require synchronous calls to every downstream system — slowing down playback. Kafka decouples event producers from consumers, so a recommendation model retraining job never impacts stream start time.",
      component: component('kafka_streaming', 'Kafka / Streaming', 'Kafka / Streaming'),
      openingMessage: "Netflix processes billions of viewing events per day through Kafka. What happens to those events downstream?",
      celebrationMessage: buildCelebration('Kafka Streaming', 'Streaming Service', "Netflix processes billions of viewing events per day through Kafka. Netflix's content team uses this data to decide which shows to renew and which to cancel — the same infrastructure that powers recommendations also powers billion-dollar content decisions.", 'the data storage layer'),
      connectingMessage: "Kafka Streaming is on the canvas. Now connect Streaming Service → Kafka Streaming. Hover over Streaming Service until a circle appears on its edge, then drag to Kafka Streaming. This connection means every play, pause, and skip event flows from the streaming service into Kafka, feeding the recommendation system and content analytics in near real-time.",
      messages: [
        msg("Netflix processes billions of viewing events per day through Kafka. Every time you skip an intro, that signal reaches the recommendation model within minutes."),
        msg("Netflix's content team uses this data to decide which shows to renew and which to cancel. Kafka makes that data available in near real-time rather than next-day batch reports."),
      ],
      requiredNodes: ['kafka_streaming'],
      requiredEdges: [edge('microservice', 'kafka_streaming')],
      successMessage: 'Kafka added. Now the data storage layer.',
      errorMessage: 'Add Kafka Streaming and connect it from the Streaming Service.',
    }),
    step({
      id: 10,
      title: 'Add Data Warehouse',
      explanation: "Netflix uses a data warehouse for aggregate analytics — completion rates, regional popularity, cohort retention. Unlike Cassandra (per-user lookups), the data warehouse is optimized for scanning billions of rows to answer questions like 'what percentage of users finished Stranger Things S4 within 48 hours?'",
      action: buildAction('Data Warehouse', 'Kafka Streaming', 'Data Warehouse', 'aggregate viewing analytics being stored for content investment decisions and ML model training'),
      why: "Netflix's data warehouse processes petabytes of viewing data to guide the $17 billion they spend on content annually. Every renewal and cancellation decision is backed by this data.",
      component: component('data_warehouse', 'Data Warehouse'),
      openingMessage: "Netflix uses both Cassandra and a data warehouse. Why two storage systems for the same viewing data?",
      celebrationMessage: buildCelebration('Data Warehouse', 'Kafka Streaming', "Netflix's data warehouse processes petabytes of viewing data to guide the $17 billion they spend on content annually. Every renewal and cancellation decision is backed by this data.", 'the NoSQL Database'),
      connectingMessage: "Data Warehouse is on the canvas. Now connect Kafka Streaming → Data Warehouse. Hover over Kafka Streaming until a circle appears on its edge, then drag to Data Warehouse. This connection means viewing events flow from Kafka into columnar storage optimized for the aggregate analytics that guide Netflix's $17 billion content budget.",
      messages: [
        msg("Netflix uses Cassandra for per-user viewing history — fast single-user lookups. They use a data warehouse for aggregate analytics — columnar storage makes scanning billions of rows 100x faster."),
        msg("Netflix's data warehouse guides the $17 billion they spend on content annually. Every renewal and cancellation decision is backed by this data."),
      ],
      requiredNodes: ['data_warehouse'],
      requiredEdges: [edge('kafka_streaming', 'data_warehouse')],
      successMessage: 'Data Warehouse added. Now the NoSQL layer.',
      errorMessage: 'Add a Data Warehouse and connect it from Kafka Streaming.',
    }),
    step({
      id: 11,
      title: 'Add NoSQL Database',
      explanation: "Netflix uses Cassandra for per-user viewing history — what you watched, when, and for how long. Cassandra can write a billion records per day with single-digit millisecond latency, making it the right choice for high-throughput per-user data.",
      action: buildAction('NoSQL Database', 'Kafka Streaming', 'NoSQL Database', 'per-user viewing history being stored with single-digit millisecond write latency at billion-record-per-day scale'),
      why: "Viewing history requires fast per-user lookups — 'what did user X watch last?' That's a single key lookup, which is exactly what Cassandra is optimized for. The data warehouse handles the aggregate queries.",
      component: component('nosql_db', 'NoSQL Database'),
      openingMessage: "Netflix writes a billion viewing records per day. Why Cassandra instead of PostgreSQL?",
      celebrationMessage: buildCelebration('NoSQL Database', 'Kafka Streaming', "Netflix uses Cassandra for viewing history because it can write a billion records per day with single-digit millisecond latency. The same data feeds both the recommendation system and the data warehouse.", 'Level 3'),
      connectingMessage: "NoSQL Database is on the canvas. Now connect Kafka Streaming → NoSQL Database. Hover over Kafka Streaming until a circle appears on its edge, then drag to NoSQL Database. This connection means viewing events stream from Kafka into Cassandra, where they're stored as per-user history for fast single-key lookups by the recommendation system.",
      messages: [
        msg("Netflix uses Cassandra for viewing history — it can write a billion records per day with single-digit millisecond latency. Fast per-user lookups, not aggregate queries."),
        msg("Connect: Kafka Streaming → NoSQL Database."),
      ],
      requiredNodes: ['nosql_db'],
      requiredEdges: [edge('kafka_streaming', 'nosql_db')],
      successMessage: 'Level 2 complete. Netflix is production-ready.',
      errorMessage: 'Add a NoSQL Database and connect it from Kafka Streaming.',
    }),
    step({
      id: 12,
      title: 'Add Worker / Background Job',
      explanation: "Netflix's transcoding pipeline uses background workers to process async tasks like thumbnail generation, subtitle extraction, and video quality analysis. Workers consume from Kafka and run independently of the streaming path.",
      action: buildAction('Worker / Background Job', 'Kafka / Streaming', 'Worker / Background Job', 'async tasks like thumbnail generation being processed without blocking the streaming path'),
      why: "Thumbnail generation and subtitle extraction are CPU-intensive tasks that should never block a user's stream request. Background workers process these asynchronously from Kafka events.",
      component: component('worker_job', 'Worker / Background Job'),
      openingMessage: buildOpeningL2('Netflix', 'Worker / Background Job', 'processes async tasks like thumbnail generation and subtitle extraction from Kafka events', 'Without background workers, CPU-intensive tasks would block the streaming path.', 'Worker / Background Job'),
      celebrationMessage: buildCelebration('Worker / Background Job', 'Kafka / Streaming', "Netflix processes thousands of hours of new content per day. Background workers handle thumbnail generation, subtitle extraction, and quality analysis — all asynchronously from Kafka events.", 'the Logger'),
      connectingMessage: "Worker / Background Job is on the canvas. Now connect Kafka Streaming → Worker / Background Job. Hover over Kafka Streaming until a circle appears on its edge, then drag to Worker / Background Job. This connection means CPU-intensive tasks like thumbnail generation and subtitle extraction are consumed from Kafka asynchronously, never blocking the streaming path.",
      messages: [
        msg("Netflix's background workers consume from Kafka and process async tasks — thumbnail generation, subtitle extraction, video quality analysis. These never block a user's stream request."),
        msg("Connect: Kafka Streaming → Worker / Background Job."),
      ],
      requiredNodes: ['worker_job'],
      requiredEdges: [edge('kafka_streaming', 'worker_job')],
      successMessage: 'Background processing added. Now logging.',
      errorMessage: 'Add a Worker / Background Job and connect it from Kafka Streaming.',
    }),
    step({
      id: 13,
      title: 'Add Logger',
      explanation: "The Logger captures structured logs for every request — which service handled it, how long it took, and any errors. Netflix engineers can trace any request from client to CDN to every backend service.",
      action: buildAction('Logger', 'Streaming Service', 'Logger', 'structured logs being captured for debugging and audit trails across all microservices'),
      why: "A Netflix stream start request touches 10-20 microservices. Without structured logging, debugging a 2-second stream start delay requires checking 20 separate systems manually.",
      component: component('logger', 'Logger'),
      openingMessage: buildOpeningL2('Netflix', 'Logger', 'captures structured logs for every request across 700+ microservices', 'Without logs, debugging a slow stream start requires checking 20 separate systems manually.', 'Logger'),
      celebrationMessage: buildCelebration('Logger', 'Streaming Service', "Netflix processes terabytes of logs per day across thousands of microservices. Structured logging is what makes operating at this scale manageable.", 'the Metrics Collector'),
      connectingMessage: "Logger is on the canvas. Now connect Streaming Service → Logger. Hover over Streaming Service until a circle appears on its edge, then drag to Logger. This connection means every stream request — which service handled it, how long it took, any errors — is captured as a structured log entry for debugging across Netflix's 700+ microservices.",
      messages: [
        msg("Netflix's Logger captures structured logs for every request — service name, latency, error code, user ID. Engineers can trace any request from client to CDN to every backend service."),
        msg("Connect: Streaming Service → Logger."),
      ],
      requiredNodes: ['logger'],
      requiredEdges: [edge('microservice', 'logger')],
      successMessage: 'Logging added. Now metrics.',
      errorMessage: 'Add a Logger and connect it from the Streaming Service.',
    }),
    step({
      id: 14,
      title: 'Add Metrics Collector',
      explanation: "The Metrics Collector aggregates real-time performance data — stream start success rate, rebuffering rate, API error rate, and CDN hit rate. Netflix's on-call engineers watch these dashboards 24/7.",
      action: buildAction('Metrics Collector', 'Logger', 'Metrics Collector', 'real-time performance metrics being aggregated for alerting and dashboards'),
      why: "Logs are for debugging individual issues. Metrics spot trends — a 0.1% drop in stream start success rate triggers an alert within 60 seconds, before users start complaining.",
      component: component('metrics_collector', 'Metrics Collector'),
      openingMessage: buildOpeningL2('Netflix', 'Metrics Collector', 'aggregates stream start success rate and rebuffering rate in real time', 'Without metrics, you find out about outages from Twitter instead of your own systems.', 'Metrics Collector'),
      celebrationMessage: buildCelebration('Metrics Collector', 'Logger', "Netflix's on-call engineers watch dashboards showing stream start success rate across every region in real time. A 0.1% drop triggers an alert within 60 seconds.", 'Level 3'),
      connectingMessage: "Metrics Collector is on the canvas. Now connect Logger → Metrics Collector. Hover over Logger until a circle appears on its edge, then drag to Metrics Collector. This connection means structured log data flows into the metrics aggregation layer, where stream start success rate and rebuffering rate are tracked in real time across all regions.",
      messages: [
        msg("Netflix's Metrics Collector tracks stream start success rate, rebuffering rate, and API error rate across every region in real time. A 0.1% drop triggers an alert within 60 seconds."),
        msg("Connect: Logger → Metrics Collector. That's Level 2 — production ready!"),
      ],
      requiredNodes: ['metrics_collector'],
      requiredEdges: [edge('logger', 'metrics_collector')],
      successMessage: 'Level 2 complete. Netflix is production-ready.',
      errorMessage: 'Add a Metrics Collector and connect it from the Logger.',
    }),
  ],
});

const l3 = level({
  level: 3,
  title: 'Expert Architecture',
  subtitle: 'Design like a Netflix senior engineer',
  description: 'Good architecture streams video. Expert architecture handles ML recommendations, global replication, chaos engineering, and adaptive bitrate streaming.',
  estimatedTime: '~25 mins',
  prerequisite: 'Builds on your Level 2 diagram',
  contextMessage: 'Level 3: Expert Architecture. Add the systems that make Netflix resilient at 270M subscribers.',
  steps: [
    step({
      id: 15,
      title: 'Add In-Memory Cache',
      explanation: "Netflix pre-computes recommendations for all 270M users and stores them in Redis. When you open the app, your homepage is served from cache in under 10ms — not computed fresh from the ML model. The cache hit rate for homepage recommendations exceeds 99%.",
      action: buildAction('In-Memory Cache', 'Recommendation Service', 'In-Memory Cache', 'pre-computed recommendations for 270M users being served in under 10ms without re-running the ML model'),
      why: "Running ML inference for 270 million users on every homepage load would require an impossibly large model serving fleet. Redis caches the pre-computed results so the recommendation model only runs when the cache is cold or stale — less than 1% of requests.",
      component: component('in_memory_cache', 'In-Memory Cache'),
      openingMessage: "Netflix pre-computes recommendations for 270M users. How does Redis make homepage loads instant?",
      celebrationMessage: buildCelebration('In-Memory Cache', 'Recommendation Service', "Netflix's Redis cache holds pre-computed recommendations for all 270 million users — the cache hit rate for homepage recommendations exceeds 99%. Almost no request ever reaches the ML model directly.", 'the ML Service'),
      connectingMessage: "In-Memory Cache is on the canvas. Now connect Recommendation Service to In-Memory Cache. Hover over Recommendation Service until a circle appears on its edge, then drag to In-Memory Cache. This connection means pre-computed recommendations for all 270M users are stored in Redis, so your Netflix homepage loads in under 10ms without re-running the ML model.",
      messages: [
        msg("Netflix pre-computes recommendations for every user and stores them in Redis. When you open the app, your homepage is served from cache in under 10ms."),
        msg("Connect: Recommendation Service to In-Memory Cache."),
      ],
      requiredNodes: ['in_memory_cache'],
      requiredEdges: [edge('recommendation_service', 'in_memory_cache')],
      successMessage: 'Cache added. Now the ML service.',
      errorMessage: 'Add an In-Memory Cache and connect it from the Recommendation Service.',
    }),
    step({
      id: 16,
      title: 'Add ML Service',
      explanation: "The ML Service powers Netflix's recommendation engine — the algorithm that drives 80% of content watched. It runs ranking models trained on billions of viewing events.",
      action: buildAction('ML Service', 'NoSQL Database', 'ML Service', 'personalized content rankings being generated using ML models trained on billions of viewing events'),
      why: "80% of what people watch on Netflix comes from recommendations. Without ML ranking, users see random content and churn rates increase dramatically.",
      component: component('ml_service', 'ML Service'),
      openingMessage: buildOpeningL3('Netflix', 'ML Service', 'ranking models trained on billions of viewing events for personalized recommendations', "Without ML, users see random content.", 'ML Service'),
      celebrationMessage: buildCelebration('ML Service', 'NoSQL Database', "Netflix's recommendation engine considers 1000+ signals — viewing history, time of day, device, and similar users.", 'the Notification Service'),
      connectingMessage: "ML Service is on the canvas. Now connect NoSQL Database to ML Service. Hover over NoSQL Database until a circle appears on its edge, then drag to ML Service. This connection means the ML ranking models read per-user viewing history from Cassandra to generate the personalized scores that drive 80% of Netflix viewing.",
      messages: [
        msg("Netflix's recommendation engine considers 1000+ signals — viewing history, time of day, device type, and behavior of similar users."),
        msg("Connect: NoSQL Database to ML Service."),
      ],
      requiredNodes: ['ml_service'],
      requiredEdges: [edge('nosql_db', 'ml_service')],
      successMessage: 'ML recommendations added. Now notifications.',
      errorMessage: 'Add an ML Service connected from the NoSQL Database.',
    }),
    step({
      id: 17,
      title: 'Add Notification Service',
      explanation: 'The Notification Service delivers push notifications for new releases, continue-watching reminders, and account alerts. It handles billions of notifications per day.',
      action: buildAction('Notification Service', 'Message Queue', 'Notification Service', 'push notifications being delivered to 270M devices without blocking the streaming path'),
      why: 'Notifications drive re-engagement. A dedicated service handles delivery, retry logic, and rate limiting without blocking the main streaming path.',
      component: component('notification_service', 'Notification Service'),
      openingMessage: buildOpeningL3('Netflix', 'Notification Service', 'push notification delivery with retry logic and rate limiting', 'Without it, notification delivery would block the main streaming path.', 'Notification Service'),
      celebrationMessage: buildCelebration('Notification Service', 'Message Queue', "Netflix sends billions of notifications per day — new season alerts, continue-watching reminders, and account security alerts.", 'the SQL Database'),
      connectingMessage: "Notification Service is on the canvas. Now connect Message Queue to Notification Service. Hover over Message Queue until a circle appears on its edge, then drag to Notification Service. This connection means notification events are consumed asynchronously from the queue, so new season alerts never block the streaming path.",
      messages: [
        msg("Netflix sends billions of notifications per day to iOS (APNs), Android (FCM), and email. The Notification Service handles delivery, retry, and rate limiting."),
        msg("Connect: Message Queue to Notification Service."),
      ],
      requiredNodes: ['notification_service'],
      requiredEdges: [edge('message_queue', 'notification_service')],
      successMessage: 'Notifications added. Now relational storage.',
      errorMessage: 'Add a Notification Service connected from the Message Queue.',
    }),
    step({
      id: 18,
      title: 'Add SQL Database',
      explanation: 'The SQL Database stores structured data that needs ACID guarantees — user accounts, subscription billing, and payment records.',
      action: buildAction('SQL Database', 'Auth Service (JWT)', 'SQL Database', 'user account and billing data being stored with ACID guarantees'),
      why: "Subscription billing needs ACID transactions — you can't double-charge a user or lose a payment record. Eventual consistency is unacceptable for billing data.",
      component: component('sql_db', 'SQL Database'),
      openingMessage: buildOpeningL3('Netflix', 'SQL Database', 'ACID transactions for user accounts and subscription billing', 'Eventual consistency is unacceptable for billing data.', 'SQL Database'),
      celebrationMessage: buildCelebration('SQL Database', 'Auth Service', "Netflix uses MySQL for billing and account data. Every subscription change and payment is an ACID transaction.", 'the Search Engine'),
      connectingMessage: "SQL Database is on the canvas. Now connect Auth Service to SQL Database. Hover over Auth Service until a circle appears on its edge, then drag to SQL Database. This connection means every subscription change, payment, and account update is an ACID transaction.",
      messages: [
        msg("Netflix uses MySQL for billing and account data. Every subscription change, payment, and account update is an ACID transaction."),
        msg("Connect: Auth Service to SQL Database."),
      ],
      requiredNodes: ['sql_db'],
      requiredEdges: [edge('auth_service', 'sql_db')],
      successMessage: 'Relational storage added. Now search.',
      errorMessage: 'Add a SQL Database connected from the Auth Service.',
    }),
    step({
      id: 19,
      title: 'Add Search Engine',
      explanation: "Netflix uses Elasticsearch for title search — an inverted index enabling sub-100ms search across 15,000+ titles in 30+ languages with typo tolerance.",
      action: buildAction('Search Engine', 'Load Balancer', 'Search Engine', 'sub-100ms full-text search across 15,000+ titles in 30 languages with personalized ranking'),
      why: "NoSQL databases are optimized for key lookups, not full-text search across multilingual content. A dedicated search engine returns results in under 100ms with typo tolerance and personalized ranking.",
      component: component('search_engine', 'Search Engine'),
      openingMessage: buildOpeningL3('Netflix', 'Search Engine', 'full-text search across 15,000+ titles in 30 languages with personalized ranking', 'NoSQL databases cannot do full-text search efficiently at this scale.', 'Search Engine'),
      celebrationMessage: buildCelebration('Search Engine', 'Load Balancer', "Netflix search returns results in under 100ms across 15,000+ titles in 30 languages — powered by Elasticsearch with personalized ranking on top.", 'the Circuit Breaker'),
      connectingMessage: "Search Engine is on the canvas. Now connect Load Balancer to Search Engine. Hover over Load Balancer until a circle appears on its edge, then drag to Search Engine. This connection means search queries are load-balanced to Elasticsearch, which returns results in under 100ms with personalized ranking layered on top.",
      messages: [
        msg("Netflix search blends text relevance with personalization — the same query returns different results for different users based on their viewing history."),
        msg("Connect: Load Balancer to Search Engine."),
      ],
      requiredNodes: ['search_engine'],
      requiredEdges: [edge('load_balancer', 'search_engine')],
      successMessage: 'Search added. Now the circuit breaker.',
      errorMessage: 'Add a Search Engine connected from the Load Balancer.',
    }),
    step({
      id: 20,
      title: 'Add CDN Cache',
      explanation: "The CDN Cache pre-warms popular titles on edge nodes before users request them. Netflix analyzes viewing patterns to predict which titles will be popular in each region and proactively caches them — reducing origin server load by 60%.",
      action: buildAction('CDN Cache', 'CDN', 'CDN Cache', 'popular titles being pre-warmed on edge nodes so requests are served from cache rather than hitting origin'),
      why: "Without proactive caching, every new release causes a flash crowd at the origin. Netflix predicts popularity and pre-warms popular titles across 15,000+ edge nodes before users request them.",
      component: component('cdn_cache', 'CDN Cache'),
      openingMessage: buildOpeningL3('Netflix', 'CDN Cache', 'proactive pre-warming of popular titles across 15,000+ edge nodes based on viewing pattern prediction', 'Without it, every new release causes a flash crowd at the origin.', 'CDN Cache'),
      celebrationMessage: buildCelebration('CDN Cache', 'CDN', "Netflix's CDN Cache pre-warms popular titles across 15,000+ edge nodes. Proactive caching reduces origin server load by 60% — most viewers never hit the origin at all.", 'the Circuit Breaker'),
      connectingMessage: "CDN Cache is on the canvas. Now connect CDN to CDN Cache. Hover over CDN until a circle appears on its edge, then drag to CDN Cache. This connection means popular titles are pre-cached at edge nodes before users request them.",
      messages: [
        msg("Netflix analyzes viewing patterns to predict popularity by region. New releases are pre-warmed across edge nodes before they premiere — preventing flash crowds at the origin."),
        msg("Connect: CDN to CDN Cache."),
      ],
      requiredNodes: ['cdn_cache'],
      requiredEdges: [edge('cdn', 'cdn_cache')],
      successMessage: 'CDN Cache added. Now the circuit breaker.',
      errorMessage: 'Add a CDN Cache connected from the CDN.',
    }),
    step({
      id: 21,
      title: 'Add Circuit Breaker',
      explanation: "A circuit breaker monitors calls to a downstream service — if the failure rate exceeds a threshold, it trips and returns a fallback response immediately instead of waiting for a timeout. Netflix invented this pattern with Hystrix.",
      action: buildAction('Circuit Breaker', 'API Gateway', 'Circuit Breaker', 'cascading failures being prevented by returning fallback responses when downstream services degrade'),
      why: "A slow recommendation service would otherwise cause every homepage load to hang for 30 seconds, cascading into a full outage. The circuit breaker trips and serves pre-cached recommendations from Redis instead.",
      component: component('circuit_breaker', 'Circuit Breaker'),
      openingMessage: buildOpeningL3('Netflix', 'Circuit Breaker', 'prevents cascading failures by returning fallbacks when downstream services degrade', 'Without it, a slow recommendation service causes every homepage load to hang for 30 seconds.', 'Circuit Breaker'),
      celebrationMessage: buildCelebration('Circuit Breaker', 'API Gateway', "The circuit breaker pattern Netflix invented is now standard across the industry — every major distributed system uses it. You have completed the Netflix expert architecture.", 'completion'),
      connectingMessage: "Circuit Breaker is on the canvas. Now connect API Gateway to Circuit Breaker. Hover over API Gateway until a circle appears on its edge, then drag to Circuit Breaker. This connection means the gateway routes requests through the circuit breaker, which trips and returns cached fallbacks when any downstream service degrades.",
      messages: [
        msg("Netflix Hystrix was the original circuit breaker implementation, now an industry standard. When the circuit trips, Netflix serves pre-cached recommendations from Redis instead of waiting for the ML model."),
        msg("Connect: API Gateway to Circuit Breaker."),
      ],
      requiredNodes: ['circuit_breaker'],
      requiredEdges: [edge('api_gateway', 'circuit_breaker')],
      successMessage: 'Level 3 complete! You have designed Netflix architecture like a senior engineer.',
      errorMessage: 'Add a Circuit Breaker connected from the API Gateway.',
    }),
    step({
      id: 22,
      title: 'Add Secret Manager',
      explanation: "The Secret Manager provides centralized secrets management with automatic rotation — database credentials, CDN signing keys, and payment API tokens rotate automatically without service restarts.",
      action: buildAction('Secret Manager', 'Auth Service', 'Secret Manager', 'credentials being stored and rotated automatically without service restarts or environment variables'),
      why: "Netflix manages thousands of secrets across hundreds of services. Hardcoded credentials are a security risk — if one server is compromised, attackers get every credential. Secret Manager fetches credentials at runtime with automatic rotation.",
      component: component('secret_manager', 'Secret Manager'),
      openingMessage: buildOpeningL3('Netflix', 'Secret Manager', 'automatic credential rotation without service restarts — Netflix manages thousands of secrets across hundreds of services', 'Hardcoded credentials are a security risk — if one server is compromised, attackers get every credential.', 'Secret Manager'),
      celebrationMessage: buildCelebration('Secret Manager', 'Auth Service', "Netflix manages thousands of secrets across hundreds of services. HashiCorp Vault or AWS Secrets Manager rotates CDN signing keys, database credentials, and payment API tokens automatically every 30 days — a compromised credential is invalid within 30 days maximum. You have completed the full Netflix expert architecture.", 'completion'),
      connectingMessage: "Secret Manager is on the canvas. Now connect Auth Service to Secret Manager. Hover over Auth Service until a circle appears on its edge, then drag to Secret Manager. This connection means the auth service reads credentials from the secret manager instead of hardcoded environment variables.",
      messages: [
        msg("Netflix manages thousands of secrets across hundreds of services. HashiCorp Vault or AWS Secrets Manager rotates credentials automatically — a compromised credential is invalid within 30 days maximum."),
        msg("Connect: Auth Service to Secret Manager."),
      ],
      requiredNodes: ['secret_manager'],
      requiredEdges: [edge('auth_service', 'secret_manager')],
      successMessage: "Level 3 complete! You have designed Netflix architecture like a senior engineer.",
      errorMessage: 'Add a Secret Manager connected from the Auth Service.',
    }),
  ],
});

export const netflixTutorial: Tutorial = tutorial({
  id: 'netflix-architecture',
  title: 'How to Design Netflix Architecture',
  description: 'Build the streaming platform that serves 270M subscribers. Learn CDN-first design, distributed encoding, and ML-powered recommendations.',
  difficulty: 'Advanced',
  category: 'Streaming',
  isLive: false,
  icon: 'Play',
  color: '#E50914',
  tags: ['streaming', 'cdn', 'encoding', 'recommendations', 'scale'],
  estimatedTime: '~60 mins',
  levels: [l1, l2, l3],
});
