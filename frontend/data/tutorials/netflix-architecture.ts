import type { TutorialData } from './chatgpt-architecture';

export const netflixTutorial: TutorialData = {
  id: 'netflix-architecture',
  title: 'How to Design Netflix Architecture',
  description: "Design the world's largest streaming platform. Understand CDN routing, video transcoding pipelines, recommendation ML, and distributed systems at massive scale.",
  difficulty: 'Advanced',
  category: 'Streaming',
  estimatedTime: '~35 mins',
  nodeCount: 16,
  stepCount: 11,
  icon: 'Video',
  color: '#ef4444',
  tags: ['CDN', 'Streaming', 'ML', 'Kafka', 'Transcoding'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "Netflix runs on TVs, phones, tablets, and browsers — all connecting to the same backend. The client is the entry point for every stream, search, and recommendation request.",
      action: 'Add a "Client (Web / Mobile)" node to the canvas.',
      why: "Netflix's architecture must serve wildly different devices with different network conditions. Starting from the client reminds us that every design decision ultimately affects playback quality.",
      searchHint: 'Client',
      messages: [
        { type: 'guide', content: "Welcome to the Netflix Architecture tutorial! 🎬 We're designing one of the most complex distributed systems ever built." },
        { type: 'guide', content: "Netflix streams to 300M+ subscribers across 190 countries, on every device imaginable — TVs, phones, tablets, browsers. All connecting to the same backend." },
        { type: 'guide', content: "Starting from the client reminds us that every design decision ultimately affects playback quality. A bad CDN choice means buffering. A slow recommendation service means users can't find what to watch." },
        { type: 'guide', content: 'Add a Client (Web / Mobile) node using ⌘K.' },
      ],
      validation: {
        requiredNodes: ['client_web_mobile'],
        requiredEdges: [],
        successMessage: "Client added. Now let's handle DNS and content delivery.",
        errorMessage: 'Add a Client (Web / Mobile) node to the canvas first.',
      },
      openingMessage: "Netflix runs on every device imaginable. How does that affect the architecture decisions?",
      defaultSuggestions: ["How does Netflix handle different devices?", "What protocols does Netflix use?", "Why is Netflix so fast to load?"],
    },
    {
      id: 2,
      title: 'Add DNS + CDN',
      explanation: "When you open Netflix, DNS resolves the domain to the nearest CDN edge node. The CDN serves video chunks from a location close to you — reducing latency from 200ms to under 10ms for cached content.",
      action: 'Add a "DNS" node and a "CDN" node. Connect: Client → DNS → CDN.',
      why: "Netflix's Open Connect CDN is one of the largest in the world. Without it, every video stream would travel from a central server — impossible at 300M concurrent streams.",
      searchHint: 'DNS',
      messages: [
        { type: 'guide', content: 'Before a single frame of video plays, DNS routes your request to the nearest server.' },
        { type: 'guide', content: "Netflix's CDN (Open Connect) has servers in ISP data centers worldwide. Your video might come from a server in the same building as your router — that's why it loads so fast." },
        { type: 'guide', content: "Without a CDN, every video stream would travel from a central server. At 300M concurrent streams, that's physically impossible. The CDN is what makes Netflix work at global scale." },
        { type: 'guide', content: 'Add a DNS node and a CDN node. Connect: Client → DNS → CDN.' },
      ],
      validation: {
        requiredNodes: ['dns', 'cdn'],
        requiredEdges: [
          { from: 'Client', to: 'DNS' },
          { from: 'DNS', to: 'CDN' },
        ],
        successMessage: 'Content delivery layer added. Now add the API routing layer.',
        errorMessage: 'Add DNS and CDN nodes, then connect Client → DNS → CDN.',
      },
      openingMessage: "Netflix's Open Connect CDN is one of the largest in the world. How does DNS routing get you to the nearest server?",
      defaultSuggestions: ["How does DNS routing work?", "What is Open Connect?", "How does CDN caching work?"],
    },
    {
      id: 3,
      title: 'Add API Gateway + Load Balancer',
      explanation: 'Non-video requests — login, search, recommendations, account management — go through the API Gateway. The Load Balancer distributes these requests across hundreds of microservices.',
      action: 'Add an "API Gateway" node and a "Load Balancer" node. Connect: Client → API Gateway → Load Balancer.',
      why: 'Netflix has hundreds of microservices. The API Gateway is the single entry point that abstracts this complexity. The load balancer ensures no single service instance is overwhelmed.',
      searchHint: 'API Gateway',
      messages: [
        { type: 'guide', content: 'Video goes through the CDN, but everything else — login, search, your watchlist — goes through the API.' },
        { type: 'guide', content: 'Netflix uses Zuul as their API gateway. It handles routing, authentication, and rate limiting for billions of API calls per day.' },
        { type: 'guide', content: 'The Load Balancer distributes requests across hundreds of microservice instances. Netflix has hundreds of services — the load balancer ensures no single instance is overwhelmed.' },
        { type: 'guide', content: 'Add an API Gateway and a Load Balancer. Connect: Client → API Gateway → Load Balancer.' },
      ],
      validation: {
        requiredNodes: ['api_gateway', 'load_balancer'],
        requiredEdges: [
          { from: 'Client', to: 'API Gateway' },
          { from: 'API Gateway', to: 'Load Balancer' },
        ],
        successMessage: 'API routing layer added. Now secure it with auth.',
        errorMessage: 'Add API Gateway and Load Balancer, then connect Client → API Gateway → Load Balancer.',
      },
      openingMessage: "Netflix uses Zuul as their API gateway. What makes a good API gateway at this scale?",
      defaultSuggestions: ["What is Zuul?", "How does Netflix handle billions of API calls?", "What is circuit breaking?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: 'The Auth Service validates OAuth tokens and manages device authentication. Netflix supports up to 5 profiles per account and multiple simultaneous streams — auth must track all of them.',
      action: 'Add an "Auth Service" node and connect it from the Load Balancer.',
      why: 'Netflix has strict concurrent stream limits per plan. Auth enforces these limits in real time — if you try to stream on a 6th device, auth blocks it.',
      searchHint: 'Auth',
      messages: [
        { type: 'guide', content: 'Every request must be authenticated — Netflix needs to know who you are, what plan you have, and how many streams are active.' },
        { type: 'guide', content: 'Netflix uses OAuth 2.0. The Auth Service validates tokens, manages device sessions, and enforces concurrent stream limits — a key part of their subscription model.' },
        { type: 'guide', content: 'If you try to stream on a 6th device on a 2-stream plan, auth blocks it in real time. This is also how Netflix enforces their password-sharing crackdown.' },
        { type: 'guide', content: 'Add an Auth Service and connect: Load Balancer → Auth Service.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Auth' }],
        successMessage: 'Auth layer added. Now add user management.',
        errorMessage: 'Add an Auth Service connected from the Load Balancer.',
      },
      openingMessage: "Netflix enforces concurrent stream limits per plan. How does auth handle that in real time?",
      defaultSuggestions: ["How does Netflix enforce stream limits?", "What is OAuth 2.0?", "How did Netflix crack down on password sharing?"],
    },
    {
      id: 5,
      title: 'Add User Service + SQL Database',
      explanation: 'The User Service manages account data, profiles, and subscription plans. This data lives in a SQL database — structured, relational data that requires ACID guarantees for billing and account management.',
      action: 'Add a "Microservice" node (User Service) and a "SQL Database" node. Connect: Load Balancer → User Service → SQL Database.',
      why: 'Account and billing data must be consistent — you cannot double-charge a customer or lose a subscription record. SQL with ACID transactions is the right tool here.',
      searchHint: 'Microservice',
      messages: [
        { type: 'guide', content: 'User accounts, profiles, and billing plans need reliable, consistent storage.' },
        { type: 'guide', content: 'The User Service manages account data, profiles, and subscription plans. It uses a SQL database because this data is structured and requires ACID guarantees.' },
        { type: 'guide', content: "You cannot double-charge a customer or lose a subscription record. SQL with ACID transactions is the right tool here — consistency matters more than scale for billing data." },
        { type: 'guide', content: 'Add a Microservice (User Service) and a SQL Database. Connect: Load Balancer → User Service → SQL Database.' },
      ],
      validation: {
        requiredNodes: ['microservice', 'sql_db'],
        requiredEdges: [
          { from: 'Load Balancer', to: 'Microservice' },
          { from: 'Microservice', to: 'SQL' },
        ],
        successMessage: 'User management added. Now handle actual video streaming.',
        errorMessage: 'Add a Microservice (User Service) and SQL Database with the correct connections.',
      },
      openingMessage: "Billing data needs ACID guarantees. Why is SQL the right choice here over NoSQL?",
      defaultSuggestions: ["What are ACID transactions?", "What SQL database does Netflix use?", "How does Netflix handle billing at scale?"],
    },
    {
      id: 6,
      title: 'Add Streaming Service + Object Storage',
      explanation: 'The Streaming Service handles video playback requests — it generates signed URLs for the CDN to serve video chunks. The actual video files live in Object Storage, organized by title, quality, and codec.',
      action: 'Add another "Microservice" node (Streaming Service) and an "Object Storage" node. Connect: Load Balancer → Streaming Service → Object Storage.',
      why: 'Video files are binary blobs that can be terabytes in size. Object storage is infinitely scalable and cheap for this use case. The streaming service generates time-limited URLs so only authenticated users can access files.',
      searchHint: 'Object Storage',
      messages: [
        { type: 'guide', content: "Now for the core of Netflix — actually serving video." },
        { type: 'guide', content: 'The Streaming Service generates pre-signed URLs that the CDN uses to fetch and cache video chunks. The actual video files live in Object Storage (S3-compatible) — petabytes of content organized by title, quality, and codec.' },
        { type: 'guide', content: 'The signed URLs are time-limited — only authenticated users can access them, and they expire after a short window. This prevents unauthorized sharing of direct video links.' },
        { type: 'guide', content: 'Add a Microservice (Streaming Service) and Object Storage. Connect: Load Balancer → Streaming Service → Object Storage.' },
      ],
      validation: {
        requiredNodes: ['object_storage'],
        requiredEdges: [{ from: 'Microservice', to: 'Object Storage' }],
        successMessage: 'Video delivery layer added. Now add the transcoding pipeline.',
        errorMessage: 'Add Object Storage connected from a Microservice (Streaming Service).',
      },
      openingMessage: "Netflix stores petabytes of video in object storage. How do pre-signed URLs keep content secure?",
      defaultSuggestions: ["What are pre-signed URLs?", "How much storage does Netflix use?", "What video formats does Netflix support?"],
    },
    {
      id: 7,
      title: 'Add Encoding / Transcoding Worker',
      explanation: 'When Netflix acquires a new title, it transcodes the source file into over 1,200 versions — different resolutions (4K, 1080p, 720p), codecs (H.264, H.265, AV1), and bitrates. This is done by a fleet of background workers.',
      action: 'Add a "Worker / Background Job" node and connect it to Object Storage.',
      why: 'A single 4K source file cannot serve all devices. A 4K stream on a TV needs 15-25 Mbps. The same content on a mobile phone on 3G needs 0.5 Mbps. Transcoding creates the right version for every context.',
      searchHint: 'Worker',
      messages: [
        { type: 'guide', content: 'How does Netflix serve 4K on a TV and 480p on a slow mobile connection from the same title?' },
        { type: 'guide', content: "Netflix's encoding pipeline (called Archer) creates 1,200+ versions of every title — different resolutions, codecs (H.264, H.265, AV1), and bitrates. The streaming service picks the right version based on your device and network speed." },
        { type: 'guide', content: 'A 4K stream needs 15-25 Mbps. The same content on 3G needs 0.5 Mbps. Without transcoding, you could only serve one quality level — either everyone buffers or 4K is impossible.' },
        { type: 'guide', content: 'Add a Worker / Background Job node and connect it to Object Storage.' },
      ],
      validation: {
        requiredNodes: ['worker_job'],
        requiredEdges: [{ from: 'Worker', to: 'Object Storage' }],
        successMessage: 'Transcoding pipeline added. Now build the recommendation engine.',
        errorMessage: 'Add a Worker / Background Job connected to Object Storage.',
      },
      openingMessage: "Netflix creates 1,200+ versions of every title. Why so many, and how does the player pick the right one?",
      defaultSuggestions: ["What is adaptive bitrate streaming?", "What codecs does Netflix use?", "How long does transcoding take?"],
    },
    {
      id: 8,
      title: 'Add Recommendation Service + ML Model Server',
      explanation: "Netflix's recommendation algorithm drives 80% of content watched. The Recommendation Service queries a Model Server that runs trained ML models to score every title for every user in real time.",
      action: 'Add another "Microservice" node (Recommendation Service) and a "Model Server" node. Connect: Load Balancer → Recommendation Service → Model Server.',
      why: "Without recommendations, users spend 90 seconds searching before giving up. The algorithm reduces this to seconds by surfacing content you're likely to watch next.",
      searchHint: 'Model Server',
      messages: [
        { type: 'guide', content: "80% of what people watch on Netflix comes from recommendations, not search." },
        { type: 'guide', content: 'The Model Server runs trained collaborative filtering and deep learning models. It scores thousands of titles per user in milliseconds — figuring out what you are most likely to watch next.' },
        { type: 'guide', content: "Without recommendations, users spend 90 seconds searching before giving up and leaving. The algorithm is not just a nice feature — it's core to Netflix's retention." },
        { type: 'guide', content: 'Add a Microservice (Recommendation Service) and a Model Server. Connect: Load Balancer → Recommendation Service → Model Server.' },
      ],
      validation: {
        requiredNodes: ['model_server'],
        requiredEdges: [{ from: 'Microservice', to: 'Model Server' }],
        successMessage: 'Recommendation engine added. Now capture viewing events with Kafka.',
        errorMessage: 'Add a Model Server connected from a Microservice (Recommendation Service).',
      },
      openingMessage: "80% of Netflix views come from recommendations. What ML techniques power this?",
      defaultSuggestions: ["What is collaborative filtering?", "How does Netflix personalize thumbnails?", "How fast must recommendations be?"],
    },
    {
      id: 9,
      title: 'Add Kafka for Viewing Events',
      explanation: 'Every play, pause, seek, and stop generates a viewing event. Kafka captures these events at massive scale — billions per day — and fans them out to the recommendation system, analytics, and the data warehouse.',
      action: 'Add a "Kafka / Streaming" node and connect it from the Streaming Service Microservice.',
      why: "Viewing events are the training data for the recommendation algorithm. Without capturing every interaction, Netflix couldn't improve its models or understand what content to produce next.",
      searchHint: 'Kafka',
      messages: [
        { type: 'guide', content: 'Every time you play, pause, or skip, Netflix records it. These events are the fuel for the recommendation engine.' },
        { type: 'guide', content: 'Netflix processes billions of viewing events per day through Kafka. These events feed the recommendation models, analytics dashboards, and content acquisition decisions.' },
        { type: 'guide', content: "Viewing events are the training data for the recommendation algorithm. Without capturing every interaction, Netflix couldn't improve its models or decide which shows to produce next." },
        { type: 'guide', content: 'Add a Kafka / Streaming node and connect: Streaming Service → Kafka.' },
      ],
      validation: {
        requiredNodes: ['kafka_streaming'],
        requiredEdges: [{ from: 'Microservice', to: 'Kafka' }],
        successMessage: 'Event streaming added. Now store viewing history.',
        errorMessage: 'Add a Kafka / Streaming node connected from the Streaming Service Microservice.',
      },
      openingMessage: "Netflix processes billions of viewing events daily through Kafka. What makes Kafka reliable at this scale?",
      defaultSuggestions: ["How does Kafka guarantee delivery?", "What is a Kafka consumer group?", "How does Netflix use Kafka for recommendations?"],
    },
    {
      id: 10,
      title: 'Add Data Warehouse + NoSQL Database',
      explanation: 'The Data Warehouse stores aggregated viewing analytics for business intelligence and model training. The NoSQL Database stores individual viewing histories — what each user watched, when, and for how long.',
      action: 'Add a "Data Warehouse" node and a "NoSQL Database" node. Connect both from Kafka.',
      why: 'Viewing history is unstructured and massive — billions of records with variable schemas. NoSQL handles this better than SQL. The data warehouse handles the analytical aggregations that feed model training.',
      searchHint: 'Data Warehouse',
      messages: [
        { type: 'guide', content: 'Kafka events need to be stored in two places — individual history and aggregated analytics.' },
        { type: 'guide', content: 'Netflix uses Cassandra for viewing history (NoSQL) — billions of records like "user X watched show Y for Z minutes at timestamp T". Variable schema, massive scale, fast writes.' },
        { type: 'guide', content: 'The Data Warehouse (Hive/Spark on S3) handles analytical aggregations — "what percentage of users who started Stranger Things finished it?" — the queries that feed model training and business decisions.' },
        { type: 'guide', content: 'Add a Data Warehouse and a NoSQL Database. Connect both from Kafka.' },
      ],
      validation: {
        requiredNodes: ['data_warehouse', 'nosql_db'],
        requiredEdges: [
          { from: 'Kafka', to: 'Data Warehouse' },
          { from: 'Kafka', to: 'NoSQL' },
        ],
        successMessage: 'Data storage layer complete. Now add observability.',
        errorMessage: 'Add Data Warehouse and NoSQL Database, both connected from Kafka.',
      },
      openingMessage: "Netflix uses Cassandra for viewing history and Hive for analytics. Why two different databases?",
      defaultSuggestions: ["What is Cassandra good at?", "What is a data warehouse vs database?", "How does Netflix use viewing data?"],
    },
    {
      id: 11,
      title: 'Add Metrics Collector + Logger',
      explanation: "Netflix runs thousands of microservices. The Metrics Collector tracks service health, stream quality, and error rates. The Logger captures detailed traces for debugging. Together they enable Netflix's famous chaos engineering practices.",
      action: 'Add a "Metrics Collector" node and a "Logger" node. Connect both from the Load Balancer.',
      why: "Netflix intentionally kills random services in production (Chaos Monkey) to test resilience. Without comprehensive metrics and logging, they couldn't detect failures or understand their blast radius.",
      searchHint: 'Metrics Collector',
      messages: [
        { type: 'guide', content: 'At Netflix scale, something is always broken somewhere. Observability is what separates a 5-minute outage from a 5-hour one.' },
        { type: 'guide', content: 'The Metrics Collector tracks service health, stream quality (rebuffering rate, startup time), and error rates. The Logger captures detailed traces for debugging specific failures.' },
        { type: 'guide', content: "Netflix intentionally kills random services in production (Chaos Monkey) to test resilience. Without comprehensive metrics and logging, they couldn't detect failures or understand their blast radius." },
        { type: 'guide', content: "Add a Metrics Collector and a Logger. Connect both from the Load Balancer. �� You've designed the Netflix architecture — from CDN routing to ML recommendations to chaos engineering observability!" },
      ],
      validation: {
        requiredNodes: ['metrics_collector', 'logger'],
        requiredEdges: [
          { from: 'Load Balancer', to: 'Metrics' },
          { from: 'Load Balancer', to: 'Logger' },
        ],
        successMessage: "🎉 Tutorial complete! You've designed the Netflix streaming architecture from scratch.",
        errorMessage: 'Add Metrics Collector and Logger connected from the Load Balancer.',
      },
      openingMessage: "Netflix uses Chaos Monkey to intentionally break things in production. How does observability make that possible?",
      defaultSuggestions: ["What is Chaos Monkey?", "What metrics does Netflix track?", "What is the Netflix tech stack?"],
    },
  ],
};
