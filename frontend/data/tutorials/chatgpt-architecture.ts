import {
  createStep,
  createLevel,
  createTutorial,
  clientMatcher,
  cdnMatcher,
  gatewayMatcher,
  loadBalancerMatcher,
  serviceMatcher,
  databaseMatcher,
  cacheMatcher,
  queueMatcher,
  llmMatcher,
  authMatcher,
  vectorDbMatcher,
  embeddingMatcher,
  ragMatcher,
  observabilityMatcher,
} from '@/lib/tutorial/factories';
import {
  buildAction,
  buildFirstStepAction,
} from '@/lib/tutorial/defaults';

const l1 = createLevel({
  id: 'chatgpt-l1',
  order: 1,
  title: 'The Foundation',
  subtitle: 'Build a working AI chat in 8 steps',
  description: 'The minimum viable ChatGPT — a system that can receive a message, route it through an LLM, and return a response. Both clients, CDN, gateway, load balancer, chat service, LLM API, and a database.',
  estimatedTime: '~15 mins',
  unlocks: 'Production Layer',
  prerequisite: 'Builds on your Level 1 diagram',
  steps: [
    createStep({
      id: 'step-1-mobile',
      order: 1,
      title: 'Add the Mobile Client',
      description: 'The Mobile Client is the iOS or Android app where users type messages and read AI responses. Over 60% of ChatGPT usage comes from mobile — it is the primary client for most users.',
      explanation: "Every architecture starts from the user. Mobile is the primary client for most ChatGPT users — the web client came second, mobile came first in adoption.",
      why: "Every architecture starts from the user.",
      nodeMatcher: clientMatcher('mobile'),
      successMessage: 'Mobile Client added. Now the Web Client.',
      hints: [
        "ChatGPT's mobile app was one of the fastest-growing apps in history, reaching 1 million users in 5 days after launch.",
        buildFirstStepAction('Mobile'),
      ],
    }),
    createStep({
      id: 'step-2-web',
      order: 2,
      title: 'Add the Web Client',
      description: "The Web Client is the browser interface where users type messages and read AI responses. It is the second client entry point — architecturally identical to mobile in what it sends and receives.",
      explanation: "Both clients send the same API requests and receive the same responses. Supporting both means the architecture must be client-agnostic.",
      why: "Both clients send the same API requests.",
      nodeMatcher: clientMatcher('web'),
      successMessage: 'Web Client added. Now the CDN.',
      hints: [
        "Both clients are architecturally identical from the backend's perspective — they send the same GraphQL/REST requests and receive the same JSON responses.",
        buildFirstStepAction('Web'),
      ],
    }),
    createStep({
      id: 'step-3-cdn',
      order: 3,
      title: 'Add CDN',
      description: 'The CDN serves static assets — CSS, JavaScript, fonts — from edge servers close to each user. It also serves cached AI responses and any uploaded files like images or PDFs from DALL-E.',
      explanation: "Without a CDN, every ChatGPT user in Tokyo loads JavaScript from US servers — 300ms of extra latency on every page load.",
      why: "Without a CDN, every ChatGPT user in Tokyo loads JavaScript from US servers.",
      nodeMatcher: cdnMatcher(),
      fromMatchers: [clientMatcher('mobile'), clientMatcher('web')],
      successMessage: 'CDN added and connected to both clients. Now the API Gateway.',
      hints: [
        "The CDN serves static UI assets (HTML, CSS, JS) and cached AI responses. A repeated prompt can be served from the CDN in 10ms instead of hitting the LLM API.",
        buildAction('CDN', 'Mobile', 'CDN', 'static assets being served from the nearest edge server'),
      ],
    }),
    createStep({
      id: 'step-4-gateway',
      order: 4,
      title: 'Add API Gateway',
      description: 'The API Gateway receives every chat message sent by every user and routes it to the correct backend service. It enforces rate limits — free users get fewer requests per hour than Plus subscribers.',
      explanation: "The CDN handles static assets. The API Gateway handles everything else — every message, every response.",
      why: "Without the API Gateway, every client would need to know every backend service address.",
      nodeMatcher: gatewayMatcher(),
      fromMatchers: [clientMatcher('mobile'), clientMatcher('web'), cdnMatcher()],
      successMessage: 'API Gateway added and connected to both clients and CDN. Now the Load Balancer.',
      hints: [
        "The CDN handles static files. The API Gateway handles everything else — messages, auth tokens, file uploads.",
        buildAction('API Gateway', 'Mobile', 'API Gateway', 'all dynamic requests being routed through a single controlled entry point'),
      ],
    }),
    createStep({
      id: 'step-5-loadbalancer',
      order: 5,
      title: 'Add Load Balancer',
      description: 'The Load Balancer distributes incoming requests across multiple chat servers so no single server is overwhelmed. During ChatGPT launch week in November 2022 it hit 1 million users in 5 days.',
      explanation: "Like a traffic controller directing cars across multiple lanes — each server gets a fair share.",
      why: "Without it, one server would collapse under the load of millions of simultaneous users.",
      nodeMatcher: loadBalancerMatcher(),
      fromMatchers: [gatewayMatcher()],
      successMessage: 'Load Balancer added and connected. Now the Chat Service.',
      hints: [
        "OpenAI's load balancer routes requests to different server regions based on latency — European users get routed to European servers.",
        buildAction('Load Balancer', 'API Gateway', 'Load Balancer', 'all API traffic being distributed across multiple servers'),
      ],
    }),
    createStep({
      id: 'step-6-chat-service',
      order: 6,
      title: 'Add Chat Service',
      description: 'The Chat Service orchestrates the entire conversation flow — it receives messages, manages context, calls the LLM, and streams the response back. It is the brain of the operation.',
      explanation: "The Chat Service decides which GPT model to use based on your subscription tier before even calling the LLM API.",
      why: "It can be scaled independently from auth and storage.",
      nodeMatcher: serviceMatcher('Chat'),
      fromMatchers: [loadBalancerMatcher()],
      successMessage: 'Chat Service added and connected. Now the LLM API.',
      hints: [
        "The Chat Service decides which GPT model to use based on your subscription tier before even calling the LLM API.",
        buildAction('Microservice', 'Load Balancer', 'Chat Service', 'distributed traffic reaching the service that actually processes your conversation'),
      ],
    }),
    createStep({
      id: 'step-7-llm',
      order: 7,
      title: 'Add LLM API',
      description: 'The LLM API receives the formatted prompt and generates a response token by token using GPT. Everything else in the architecture exists to get the right prompt here and return the response.',
      explanation: "GPT-4 inference costs OpenAI approximately $0.03 per 1000 tokens.",
      why: "Everything else in the architecture exists to get the right prompt here and return the response.",
      nodeMatcher: llmMatcher(),
      toMatchers: [serviceMatcher('Chat')],
      successMessage: 'LLM API added and connected. Now the NoSQL Database.',
      hints: [
        "OpenAI runs GPT-4 on thousands of A100 GPUs. Each inference call is massively expensive compared to a normal database query.",
        buildAction('LLM API', 'Microservice', 'LLM API', 'the prompt traveling this path to get a response'),
      ],
    }),
    createStep({
      id: 'step-8-nosql',
      order: 8,
      title: 'Add NoSQL Database',
      description: 'The NoSQL Database stores every conversation — all messages, all responses, all conversation metadata per user. Every chat you have ever had with ChatGPT lives here.',
      explanation: "Conversation data is unstructured — each message can have different metadata and formats. NoSQL handles this because SQL would require a rigid schema.",
      why: "SQL would require a rigid schema that breaks every time the message format evolves.",
      nodeMatcher: databaseMatcher('nosql'),
      toMatchers: [serviceMatcher('Chat')],
      successMessage: 'Level 1 complete! You have a working AI chat foundation.',
      hints: [
        "OpenAI uses a document database so each conversation is stored as a self-contained document with all its messages and metadata.",
        buildAction('NoSQL Database', 'Microservice', 'NoSQL Database', 'conversations being persisted'),
      ],
    }),
  ],
});

const l2 = createLevel({
  id: 'chatgpt-l2',
  order: 2,
  title: 'Production Ready',
  subtitle: 'Add auth, memory, RAG, and observability',
  description: 'Your ChatGPT foundation works. Now add what OpenAI actually runs in production — auth with subscription tiers, vector memory for long conversations, RAG pipeline, streaming, and full observability. 10 more components on your existing diagram.',
  estimatedTime: '~20 mins',
  unlocks: 'Expert Architecture',
  prerequisite: 'Builds on your Level 1 diagram',
  steps: [
    createStep({
      id: 'step-9-auth',
      order: 9,
      title: 'Add Auth Service',
      description: 'The Auth Service validates JWT tokens AND checks subscription tier — free vs Plus vs Enterprise — in every request. It is what enforces who gets GPT-4 access and who gets GPT-3.5.',
      explanation: "Without it, anyone could use GPT-4 without paying and OpenAI would have no way to enforce rate limits.",
      why: "The Auth Service returns the user's model tier with every token validation.",
      nodeMatcher: authMatcher(),
      fromMatchers: [loadBalancerMatcher()],
      successMessage: 'Auth Service added and connected. Now the SQL Database.',
      hints: [
        "The Auth Service verifies identity and checks plan. Free users get GPT-3.5. Plus users get GPT-4 priority.",
        buildAction('Auth Service (JWT)', 'Load Balancer', 'Auth Service', 'every request being authenticated and subscription-checked'),
      ],
    }),
    createStep({
      id: 'step-10-sql',
      order: 10,
      title: 'Add SQL Database',
      description: 'The SQL Database stores user accounts, subscription status, billing records, and API usage quotas. This is separate from the NoSQL chat history because account data requires ACID transaction guarantees.',
      explanation: "OpenAI could not track who is a paid subscriber and the entire billing system collapses without it.",
      why: "SQL's strong consistency ensures a subscription cancellation is never double-charged.",
      nodeMatcher: databaseMatcher('sql'),
      toMatchers: [authMatcher()],
      successMessage: 'SQL Database added and connected. Now the In-Memory Cache.',
      hints: [
        "Account data needs ACID guarantees. NoSQL is eventually consistent — fine for chat. Not fine for billing.",
        buildAction('SQL Database', 'Auth Service', 'SQL Database', 'user accounts and subscription data being stored with ACID transaction guarantees'),
      ],
    }),
    createStep({
      id: 'step-11-cache',
      order: 11,
      title: 'Add In-Memory Cache',
      description: 'The In-Memory Cache stores active conversation context so the Chat Service does not re-fetch conversation history from the database on every message. Redis can serve 100,000+ requests per second at sub-millisecond latency.',
      explanation: "Every message in a long conversation would require a full database read adding 50-100ms of latency per response without it.",
      why: "Active conversations are kept in fast memory so responses feel instant.",
      nodeMatcher: cacheMatcher(),
      toMatchers: [serviceMatcher('Chat')],
      successMessage: 'In-Memory Cache added and connected. Now the Embedding Service.',
      hints: [
        "Redis returns data in under 1ms. A database returns in 5-50ms. At ChatGPT's scale that difference compounds.",
        buildAction('In-Memory Cache', 'Microservice', 'In-Memory Cache', 'active conversations being kept in fast memory'),
      ],
    }),
    createStep({
      id: 'step-12-embedding',
      order: 12,
      title: 'Add Embedding Service',
      description: "The Embedding Service converts every message into a vector representation so similar past conversations can be retrieved for context injection. OpenAI's text-embedding-ada-002 model converts text into 1536-dimensional vectors.",
      explanation: "ChatGPT would have no memory of past conversations without it — every chat would start completely fresh.",
      why: "Past conversations are converted to vectors for semantic search — not keyword matching.",
      nodeMatcher: embeddingMatcher(),
      toMatchers: [serviceMatcher('Chat')],
      successMessage: 'Embedding Service added and connected. Now the Vector Database.',
      hints: [
        "The Embedding Service converts text into dense numerical vectors. Two semantically similar sentences produce nearly identical vectors.",
        buildAction('Embedding Service', 'Microservice', 'Embedding Service', 'messages being converted to vectors for semantic search'),
      ],
    }),
    createStep({
      id: 'step-13-vector',
      order: 13,
      title: 'Add Vector Database',
      description: "The Vector Database stores billions of message embeddings and retrieves the most semantically relevant past context for any new user message in milliseconds. This is what makes long-term memory possible.",
      explanation: "Long-term memory is impossible without it — GPT-4's 128K context window would fill up within a long conversation.",
      why: "Message vectors are stored and searched by semantic similarity — not by exact text.",
      nodeMatcher: vectorDbMatcher(),
      toMatchers: [embeddingMatcher()],
      successMessage: 'Vector Database added and connected. Now the RAG Pipeline.',
      hints: [
        "The Vector Database stores your conversation embeddings. When you send a new message, it finds semantically closest past messages.",
        buildAction('Vector Database', 'Embedding Service', 'Vector Database', 'message vectors being stored and searched by semantic similarity'),
      ],
    }),
    createStep({
      id: 'step-14-rag',
      order: 14,
      title: 'Add RAG Pipeline',
      description: "The RAG Pipeline retrieves relevant past conversation chunks from the vector database and injects them into the prompt before sending to the LLM. This is what makes ChatGPT appear to remember things you said months ago.",
      explanation: "The LLM only sees the current conversation window and cannot reference anything you discussed in previous sessions without it.",
      why: "Past context is retrieved and injected into every new prompt before the LLM generates a response.",
      nodeMatcher: ragMatcher(),
      fromMatchers: [vectorDbMatcher()],
      toMatchers: [llmMatcher()],
      successMessage: 'RAG Pipeline added and connected. Now the Message Queue.',
      hints: [
        "The RAG Pipeline ties it all together: it retrieves relevant context from the vector DB and injects it into the LLM prompt.",
        buildAction('RAG Pipeline', 'Vector Database', 'RAG Pipeline', 'past context being retrieved and injected into every new prompt'),
      ],
    }),
    createStep({
      id: 'step-15-queue',
      order: 15,
      title: 'Add Message Queue',
      description: 'The Message Queue queues async tasks like usage tracking, conversation indexing, and billing updates so they do not slow down the response generation. Every token usage calculation runs asynchronously here.',
      explanation: "Every token usage calculation and billing update would run synchronously adding 50-100ms to every single response without it.",
      why: "Billing, analytics, and indexing happen asynchronously without slowing down the response.",
      nodeMatcher: queueMatcher(),
      toMatchers: [serviceMatcher('Chat')],
      successMessage: 'Message Queue added and connected. Now the Logger.',
      hints: [
        "When you get a response, 5 things happen async: save conversation, generate embeddings, log request, track usage, update metrics.",
        buildAction('Message Queue', 'Microservice', 'Message Queue', 'billing, analytics, and indexing happening asynchronously'),
      ],
    }),
    createStep({
      id: 'step-16-logger',
      order: 16,
      title: 'Add Logger',
      description: 'The Logger captures structured logs of every request including model used, tokens consumed, latency, and any errors for debugging and compliance. OpenAI must retain conversation logs for safety and policy enforcement.',
      explanation: "When something goes wrong OpenAI engineers have no visibility into what happened without it.",
      why: "Every conversation request is logged for debugging, safety review, and compliance.",
      nodeMatcher: observabilityMatcher('logger'),
      toMatchers: [serviceMatcher('Chat')],
      successMessage: 'Logger added and connected. Now the Metrics Collector.',
      hints: [
        "Something is always breaking in production. The Logger captures what happened for every single request.",
        buildAction('Logger', 'Microservice', 'Logger', 'every conversation request being logged'),
      ],
    }),
    createStep({
      id: 'step-17-metrics',
      order: 17,
      title: 'Add Metrics Collector',
      description: 'The Metrics Collector aggregates real-time metrics on response latency, error rates, token throughput, and GPU utilization across the entire inference cluster. A 10% drop in GPU utilization triggers an alert within 30 seconds.',
      explanation: "OpenAI would not know if GPT-4 response times were degrading until users started complaining on Twitter.",
      why: "Raw events become actionable numbers on a dashboard.",
      nodeMatcher: observabilityMatcher('metrics'),
      toMatchers: [observabilityMatcher('logger')],
      successMessage: 'Level 2 complete! Your ChatGPT architecture is production-ready.',
      hints: [
        "Logs are for debugging individual issues. Metrics spot trends — a 10% error rate increase triggers an alert before users notice.",
        buildAction('Metrics Collector', 'Logger', 'Metrics Collector', 'raw events becoming actionable numbers on a dashboard'),
      ],
    }),
    createStep({
      id: 'step-18-cdn-2',
      order: 18,
      title: 'Add CDN (Production)',
      description: 'The CDN sits at the edge serving static UI assets and cached AI responses for common prompts. Responses served from CDN bypass the LLM API entirely — a cached response delivers in under 10ms.',
      explanation: "The same question gets asked millions of times. Caching common responses at the CDN edge means those requests never reach the LLM API.",
      why: "Saving compute cost and reducing latency to under 10ms.",
      nodeMatcher: cdnMatcher(),
      toMatchers: [serviceMatcher('Chat')],
      successMessage: 'CDN added and connected. Level 2 complete — production-ready architecture!',
      hints: [
        "The CDN serves cached AI responses and static assets. A repeated prompt can be served from CDN in 10ms.",
        buildAction('CDN', 'Microservice', 'CDN', 'static assets and cached AI responses being served from edge nodes'),
      ],
    }),
  ],
});

const l3 = createLevel({
  id: 'chatgpt-l3',
  order: 3,
  title: 'Expert Architecture',
  subtitle: 'Design like an OpenAI senior engineer',
  description: 'You have production ChatGPT. Level 3 adds the systems that make it truly OpenAI-grade — streaming token delivery, model routing by subscription tier, prompt injection protection, usage billing, and the data infrastructure that powers safety research.',
  estimatedTime: '~25 mins',
  prerequisite: 'Builds on your Level 2 diagram',
  steps: [
    createStep({
      id: 'step-19-dashboard',
      order: 19,
      title: 'Add Dashboard',
      description: 'The Dashboard visualizes all metrics from the Metrics Collector in real-time panels. OpenAI engineers watch dashboards showing GPU temps, request queues, and error rates simultaneously during incidents.',
      explanation: "Without a unified dashboard correlating GPU temps, request queues, and error rates it is impossible to diagnose incidents in real-time.",
      why: "Metrics flow from services through the collector to a human-readable dashboard that engineers watch 24/7.",
      nodeMatcher: observabilityMatcher('dashboard'),
      toMatchers: [observabilityMatcher('metrics')],
      successMessage: 'Dashboard added and connected. Now the BFF Gateway.',
      hints: [
        "The Dashboard closes the observability loop. Metrics flow from services to Logger to Metrics Collector to Dashboard.",
        buildAction('Dashboard (Grafana)', 'Metrics Collector', 'Dashboard', 'metrics flowing to a human-readable dashboard'),
      ],
    }),
    createStep({
      id: 'step-20-bff',
      order: 20,
      title: 'Add BFF Gateway',
      description: "The BFF (Backend for Frontend) Gateway is a client-specific API aggregator that tailors responses for mobile vs web clients. Mobile might need compressed responses with fewer tokens while web needs full context — the BFF adapts the API contract per client.",
      explanation: "Mobile and web have different rendering capabilities, network conditions, and UX requirements.",
      why: "The BFF prevents the Chat Service from becoming a god service with client-specific logic — it stays clean while the BFF handles client adaptation.",
      nodeMatcher: gatewayMatcher(),
      fromMatchers: [serviceMatcher('Chat')],
      toMatchers: [clientMatcher('mobile'), clientMatcher('web')],
      successMessage: 'BFF Gateway added and connected. Now the Token Streaming Service.',
      hints: [
        "The BFF Gateway is the adapter layer between the Chat Service API and client-specific needs.",
        buildAction('BFF Gateway', 'Microservice', 'BFF Gateway', 'the API being tailored per client type'),
      ],
    }),
    createStep({
      id: 'step-21-streaming',
      order: 21,
      title: 'Add Token Streaming Service',
      description: 'The Token Streaming Service delivers tokens to the browser one by one using Server-Sent Events (SSE). Without streaming the user would wait 10-30 seconds for a complete GPT-4 response before seeing anything.',
      explanation: "GPT-4 generates roughly 30-50 tokens per second. Without streaming the user waits 6-10 seconds in silence.",
      why: "Tokens appear one by one in real-time instead of all at once after a long wait.",
      nodeMatcher: serviceMatcher('Streaming'),
      fromMatchers: [serviceMatcher('Chat')],
      successMessage: 'Token Streaming Service added and connected. Now the Token Bucket Rate Limiter.',
      hints: [
        "ChatGPT streams responses using Server-Sent Events. The Token Streaming Service maintains millions of open connections.",
        buildAction('Serverless Function', 'Microservice', 'Token Streaming Service', 'tokens appearing one by one in real-time'),
      ],
    }),
    createStep({
      id: 'step-22-rate-limiter',
      order: 22,
      title: 'Add Token Bucket Rate Limiter',
      description: "The Token Bucket Rate Limiter enforces API usage quotas using a token bucket algorithm — users get a bucket of tokens refilled at a fixed rate. Plus users get 60 tokens/minute; free users get 20.",
      explanation: "Simple counters allow burst attacks — a user could make 100 requests in 1 second then nothing for 59 seconds.",
      why: "Token bucket allows controlled bursting while enforcing average rate limits across time windows.",
      nodeMatcher: gatewayMatcher(),
      fromMatchers: [gatewayMatcher()],
      successMessage: 'Token Bucket Rate Limiter added and connected. Now the OpenTelemetry Collector.',
      hints: [
        "The Token Bucket algorithm allows OpenAI to enforce per-user rate limits with controlled bursting.",
        buildAction('Token Bucket Rate Limiter', 'API Gateway', 'Token Bucket Rate Limiter', 'API requests being rate-limited'),
      ],
    }),
    createStep({
      id: 'step-23-otel',
      order: 23,
      title: 'Add OpenTelemetry Collector',
      description: "The OpenTelemetry Collector is a vendor-neutral telemetry pipeline that ingests logs, metrics, and traces from all services. It provides a single ingestion point for observability data before routing to backends like Jaeger, Prometheus, or cloud providers.",
      explanation: "Without OTel Collector, each service has a direct integration with Jaeger, Prometheus, Datadog. When you switch vendors, you touch every service.",
      why: "OTel Collector means changing only the collector config when switching vendors.",
      nodeMatcher: observabilityMatcher('tracing'),
      toMatchers: [observabilityMatcher('logger')],
      successMessage: 'OpenTelemetry Collector added and connected. Tutorial complete!',
      hints: [
        "The OTel Collector is the central hub for all observability. Services emit data once; the collector handles routing.",
        buildAction('OpenTelemetry Collector', 'Logger', 'OpenTelemetry Collector', 'all observability data flowing through a single vendor-neutral pipeline'),
      ],
    }),
  ],
});

export const chatgptArchitecture = createTutorial({
  id: 'chatgpt-architecture',
  title: 'Build ChatGPT',
  subtitle: 'From zero to production-ready AI architecture',
  description: 'Build the ChatGPT system architecture from scratch, step by step. Learn how the AI that changed the world actually works under the hood.',
  difficulty: 'beginner',
  tags: ['AI', 'LLM', 'Production', 'ChatGPT'],
  levels: [l1, l2, l3],
});

export type { TutorialLevel } from '@/lib/tutorial/types';
export type { Tutorial as TutorialData } from '@/lib/tutorial/types';
