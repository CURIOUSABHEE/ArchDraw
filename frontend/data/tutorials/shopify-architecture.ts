import type { TutorialData } from './index';

export const shopifyTutorial: TutorialData = {
  id: 'shopify-architecture',
  title: 'How to Design Shopify Architecture',
  description: 'Build an e-commerce platform for 2 million merchants processing $235B in annual sales. Learn cart management, inventory locking, checkout flows, payment processing, and Black Friday traffic spikes.',
  difficulty: 'Advanced',
  category: 'E-Commerce',
  estimatedTime: '~30 mins',
  nodeCount: 12,
  stepCount: 12,
  icon: 'ShoppingBag',
  color: '#96bf48',
  tags: ['Checkout', 'Inventory', 'Payments', 'Flash Sales'],
  steps: [
    {
      id: 1,
      title: 'Start with the Web Client',
      explanation: "Shopify's web client is the storefront — the customer-facing shop. Each merchant has a custom storefront built on Shopify's theme system. The client handles product browsing, cart management, and checkout.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "Shopify powers 2 million storefronts, each with a unique domain and theme. The web client must be fast — a 1-second delay in page load reduces conversions by 7%. Shopify uses a CDN to serve storefronts from edge locations.",
      messages: [
        { type: 'guide', content: "Welcome to the Shopify Architecture tutorial. 2 million merchants, $235 billion in annual sales, and a Black Friday peak of 4.2 million orders per hour." },
        { type: 'guide', content: "Shopify's hardest problem: a merchant with 10 orders/day and one with 10 million orders/day run on the same infrastructure. The architecture must scale from zero to viral without any merchant-specific configuration." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Web Client added. Now the CDN layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "Shopify serves 2 million unique storefronts. How does it handle each merchant's custom domain and theme?",
      defaultSuggestions: ["How does Shopify handle custom domains?", "What is Shopify's theme system?", "How does storefront CDN work?"],
    },
    {
      id: 2,
      title: 'Add CDN',
      explanation: "Shopify's CDN serves storefront assets — product images, CSS, JavaScript — from edge locations worldwide. Product images are the largest assets; the CDN resizes and optimizes them on-the-fly for different devices.",
      action: 'Press ⌘K, search for "CDN", and add it to the canvas. Then connect Client → CDN.',
      searchHint: 'CDN',
      why: "Product images can be 5MB+ in original resolution. The CDN serves optimized versions: 400px thumbnails for mobile, 1200px for desktop, WebP format for modern browsers. This reduces page load time by 60%.",
      messages: [
        { type: 'guide', content: "Shopify's CDN does more than serve files — it resizes product images on-the-fly for different screen sizes." },
        { type: 'guide', content: "When a merchant uploads a 5MB product photo, Shopify stores the original and generates thumbnails lazily. The CDN serves the right size for each device: 400px for mobile, 1200px for desktop. It also converts to WebP automatically for browsers that support it." },
        { type: 'guide', content: 'Press ⌘K, search for "CDN", add it, then connect: Client → CDN.' },
      ],
      validation: {
        requiredNodes: ['cdn'],
        requiredEdges: [{ from: 'Web', to: 'CDN' }],
        successMessage: "CDN added and connected. Now the API layer.",
        errorMessage: 'Add a CDN and connect it from the Client.',
      },
      openingMessage: "Shopify's CDN resizes images on-the-fly. Connect Client → CDN — product assets flow from the user to the nearest edge node.",
      defaultSuggestions: ["What is image optimization CDN?", "How does WebP conversion work?", "What is lazy image generation?"],
    },
    {
      id: 3,
      title: 'Add API Gateway',
      explanation: "Shopify's API Gateway handles storefront API requests, admin API calls, and webhook deliveries. It enforces API rate limits per merchant — a merchant's app can't hammer the API and affect other merchants.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas. Then connect Client → API Gateway.',
      searchHint: 'API Gateway',
      why: "Shopify's API is used by 8,000+ apps in the App Store. The gateway enforces per-merchant rate limits so a poorly written app can't degrade the platform for other merchants.",
      messages: [
        { type: 'guide', content: "Shopify's API Gateway enforces per-merchant rate limits — one merchant's bad app can't affect others." },
        { type: 'guide', content: "Shopify uses a leaky bucket rate limiter: each merchant gets 40 API calls per second. If an app exceeds this, it gets a 429 response. The gateway tracks usage per merchant, not per IP, so a merchant with 100 app instances still shares the same bucket." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", add it, then connect: Client → API Gateway.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [{ from: 'Web', to: 'API Gateway' }],
        successMessage: "API Gateway added and connected. Now distribute the load.",
        errorMessage: 'Add an API Gateway and connect it from the Client.',
      },
      openingMessage: "Shopify uses a leaky bucket rate limiter per merchant. Connect Client → API Gateway — all non-asset requests flow through here.",
      defaultSuggestions: ["What is a leaky bucket rate limiter?", "How does Shopify's API rate limiting work?", "What is per-merchant isolation?"],
    },
    {
      id: 4,
      title: 'Add Load Balancer',
      explanation: "Shopify's Load Balancer distributes requests across service instances. During Black Friday, Shopify pre-scales to 10x normal capacity. The load balancer routes traffic to the right service cluster based on the request type.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas. Then connect API Gateway → Load Balancer.',
      searchHint: 'Load Balancer',
      why: "Black Friday is predictable — Shopify knows it's coming and pre-scales. The load balancer must handle 40x traffic spikes without dropping requests. Shopify uses autoscaling with pre-warming to avoid cold start latency.",
      messages: [
        { type: 'guide', content: "Shopify pre-scales for Black Friday — they don't wait for traffic to spike before adding capacity." },
        { type: 'guide', content: "Shopify's engineering team runs 'flash sale drills' throughout the year. They simulate Black Friday traffic to find bottlenecks before the real event. The load balancer is configured to auto-scale checkout services 10x starting at midnight on Thanksgiving." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", add it, then connect: API Gateway → Load Balancer.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [{ from: 'API Gateway', to: 'Load Balancer' }],
        successMessage: "Load Balancer added and connected. Now the cart service.",
        errorMessage: 'Add a Load Balancer and connect it from the API Gateway.',
      },
      openingMessage: "Shopify pre-scales for Black Friday instead of reacting to traffic. Connect API Gateway → Load Balancer to distribute the storefront load.",
      defaultSuggestions: ["What is proactive scaling?", "How does Shopify prepare for Black Friday?", "What is autoscaling pre-warming?"],
    },
    {
      id: 5,
      title: 'Add Cart Service',
      explanation: "The Cart Service manages shopping carts. Carts are stored in Redis (in-memory) for fast reads and writes. A cart is a temporary data structure — it only becomes permanent when the customer checks out.",
      action: 'Press ⌘K, search for "Cart Service", and add it to the canvas. Then connect Load Balancer → Cart Service.',
      searchHint: 'Cart Service',
      why: "Carts are read and written on every product page interaction. Storing them in a database would be too slow. Redis gives sub-millisecond cart reads, and carts expire automatically if abandoned.",
      messages: [
        { type: 'guide', content: "Shopping carts are stored in Redis — not a database. They're temporary and need sub-millisecond access." },
        { type: 'guide', content: "Every time you add an item, change quantity, or apply a discount code, the Cart Service updates Redis. Carts expire after 30 days of inactivity. During checkout, the cart is validated against current inventory before payment is processed." },
        { type: 'guide', content: 'Press ⌘K, search for "Cart Service", add it, then connect: Load Balancer → Cart Service.' },
      ],
      validation: {
        requiredNodes: ['cart_service'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Cart' }],
        successMessage: "Cart Service added and connected. Now inventory management.",
        errorMessage: 'Add a Cart Service and connect it from the Load Balancer.',
      },
      openingMessage: "Shopify stores carts in Redis instead of a database. Connect Load Balancer → Cart Service — add-to-cart requests are routed here.",
      defaultSuggestions: ["Why store carts in Redis?", "What is cart abandonment?", "How does Shopify handle cart expiry?"],
    },
    {
      id: 6,
      title: 'Add Inventory Service',
      explanation: "The Inventory Service tracks stock levels and handles inventory reservations. When a customer adds an item to their cart, inventory is soft-reserved. When they complete checkout, inventory is hard-decremented.",
      action: 'Press ⌘K, search for "Inventory Service", and add it to the canvas. Then connect Cart Service → Inventory Service.',
      searchHint: 'Inventory Service',
      why: "Inventory is the hardest problem in e-commerce. Two customers can't buy the last item simultaneously. The Inventory Service uses optimistic locking — it checks stock at checkout, not at add-to-cart, to avoid over-reserving.",
      messages: [
        { type: 'guide', content: "Inventory management is the hardest part of e-commerce. Two customers can't buy the last item." },
        { type: 'guide', content: "Shopify uses optimistic locking: inventory isn't reserved when you add to cart (that would block other customers). It's checked and decremented atomically at checkout. If two customers checkout simultaneously, one succeeds and one gets an 'out of stock' error. This is the right tradeoff — better to disappoint one customer than to over-reserve inventory." },
        { type: 'guide', content: 'Press ⌘K, search for "Inventory Service", add it, then connect: Cart Service → Inventory Service.' },
      ],
      validation: {
        requiredNodes: ['inventory_service'],
        requiredEdges: [{ from: 'Cart', to: 'Inventory' }],
        successMessage: "Inventory Service added and connected. Now the checkout flow.",
        errorMessage: 'Add an Inventory Service and connect it from the Cart Service.',
      },
      openingMessage: "Shopify uses optimistic locking for inventory. Connect Cart Service → Inventory Service — the cart validates stock availability here.",
      defaultSuggestions: ["What is optimistic locking?", "How does Shopify prevent overselling?", "What is inventory reservation?"],
    },
    {
      id: 7,
      title: 'Add Checkout Service',
      explanation: "The Checkout Service orchestrates the checkout flow: validate cart, check inventory, calculate taxes, apply discounts, process payment, and create the order. It's a saga — if any step fails, previous steps are rolled back.",
      action: 'Press ⌘K, search for "Checkout Service", and add it to the canvas. Then connect Inventory Service → Checkout Service.',
      searchHint: 'Checkout Service',
      why: "Checkout is a multi-step transaction across multiple services. The Checkout Service uses the Saga pattern to ensure consistency — if payment fails, inventory is released and the order is cancelled.",
      messages: [
        { type: 'guide', content: "The Checkout Service is a saga — it coordinates multiple services and handles failures at each step." },
        { type: 'guide', content: "Checkout steps: 1) Validate cart items still exist. 2) Check inventory. 3) Calculate taxes. 4) Apply discount codes. 5) Process payment. 6) Decrement inventory. 7) Create order. If step 5 fails (payment declined), steps 1-4 are rolled back. If step 6 fails (inventory error), payment is refunded." },
        { type: 'guide', content: 'Press ⌘K, search for "Checkout Service", add it, then connect: Inventory Service → Checkout Service.' },
      ],
      validation: {
        requiredNodes: ['checkout_service'],
        requiredEdges: [{ from: 'Inventory', to: 'Checkout' }],
        successMessage: "Checkout Service added and connected. Now payment processing.",
        errorMessage: 'Add a Checkout Service and connect it from the Inventory Service.',
      },
      openingMessage: "Checkout is a saga across multiple services. Connect Inventory Service → Checkout Service — inventory checks gate the checkout saga.",
      defaultSuggestions: ["What is the Saga pattern?", "How does Shopify handle checkout failures?", "What is distributed transaction rollback?"],
    },
    {
      id: 8,
      title: 'Add Payment Gateway',
      explanation: "Shopify Payments (powered by Stripe) processes credit cards, Apple Pay, Google Pay, and buy-now-pay-later. The Payment Gateway handles PCI compliance, fraud detection, and currency conversion for international merchants.",
      action: 'Press ⌘K, search for "Payment Gateway", and add it to the canvas. Then connect Checkout Service → Payment Gateway.',
      searchHint: 'Payment Gateway',
      why: "Payment processing requires PCI DSS compliance — Shopify handles this so merchants don't have to. The Payment Gateway tokenizes card data so Shopify never stores raw card numbers.",
      messages: [
        { type: 'guide', content: "Shopify Payments handles PCI compliance so merchants never touch raw card data." },
        { type: 'guide', content: "When a customer enters their card, it's tokenized client-side before reaching Shopify's servers. The token is sent to the Payment Gateway, which exchanges it for a charge with the card network. Shopify stores the token, not the card number. This is how Shopify achieves PCI compliance without storing sensitive data." },
        { type: 'guide', content: 'Press ⌘K, search for "Payment Gateway", add it, then connect: Checkout Service → Payment Gateway.' },
      ],
      validation: {
        requiredNodes: ['payment_gateway'],
        requiredEdges: [{ from: 'Checkout', to: 'Payment' }],
        successMessage: "Payment Gateway added and connected. Now order management.",
        errorMessage: 'Add a Payment Gateway and connect it from the Checkout Service.',
      },
      openingMessage: "Shopify tokenizes card data client-side. Connect Checkout Service → Payment Gateway — the checkout saga calls payment processing here.",
      defaultSuggestions: ["What is PCI DSS compliance?", "How does card tokenization work?", "What is Shopify Payments?"],
    },
    {
      id: 9,
      title: 'Add Order Service',
      explanation: "The Order Service creates and manages orders after successful payment. It stores order details, triggers fulfillment, sends confirmation emails, and handles returns and refunds.",
      action: 'Press ⌘K, search for "Order Service", and add it to the canvas. Then connect Payment Gateway → Order Service.',
      searchHint: 'Order Service',
      why: "Orders are the permanent record of a transaction. Unlike carts (temporary), orders must be durable and auditable. The Order Service writes to a relational database with ACID guarantees.",
      messages: [
        { type: 'guide', content: "Orders are permanent records — unlike carts, they're stored in a relational database with ACID guarantees." },
        { type: 'guide', content: "After payment succeeds, the Order Service creates an immutable order record. It triggers the fulfillment workflow, sends a confirmation email, and notifies the merchant. Orders can never be deleted — only cancelled or refunded. This audit trail is required for accounting and dispute resolution." },
        { type: 'guide', content: 'Press ⌘K, search for "Order Service", add it, then connect: Payment Gateway → Order Service.' },
      ],
      validation: {
        requiredNodes: ['order_service'],
        requiredEdges: [{ from: 'Payment', to: 'Order' }],
        successMessage: "Order Service added and connected. Now fulfillment.",
        errorMessage: 'Add an Order Service and connect it from the Payment Gateway.',
      },
      openingMessage: "Orders are immutable records. Connect Payment Gateway → Order Service — successful payment triggers order creation.",
      defaultSuggestions: ["What is an immutable order record?", "How does Shopify handle refunds?", "What is order fulfillment?"],
    },
    {
      id: 10,
      title: 'Add Fulfillment Service',
      explanation: "The Fulfillment Service routes orders to the right fulfillment center, generates shipping labels, and tracks packages. Shopify Fulfillment Network (SFN) competes with Amazon FBA — merchants store inventory in Shopify's warehouses.",
      action: 'Press ⌘K, search for "Fulfillment Service", and add it to the canvas. Then connect Order Service → Fulfillment Service.',
      searchHint: 'Fulfillment Service',
      why: "Fulfillment is where digital meets physical. The Fulfillment Service must route each order to the nearest warehouse with available stock, generate a shipping label, and update tracking in real-time.",
      messages: [
        { type: 'guide', content: "The Fulfillment Service routes orders to warehouses and generates shipping labels automatically." },
        { type: 'guide', content: "When an order is created, the Fulfillment Service checks which warehouse has the item in stock and is closest to the customer. It generates a shipping label via carrier APIs (UPS, FedEx, USPS), sends the label to the warehouse, and creates a tracking number. The customer gets a tracking email automatically." },
        { type: 'guide', content: 'Press ⌘K, search for "Fulfillment Service", add it, then connect: Order Service → Fulfillment Service.' },
      ],
      validation: {
        requiredNodes: ['fulfillment_service'],
        requiredEdges: [{ from: 'Order', to: 'Fulfillment' }],
        successMessage: "Fulfillment Service added and connected. Now tax calculation.",
        errorMessage: 'Add a Fulfillment Service and connect it from the Order Service.',
      },
      openingMessage: "Shopify routes orders to the nearest warehouse with stock. Connect Order Service → Fulfillment Service — confirmed orders trigger the fulfillment workflow.",
      defaultSuggestions: ["What is Shopify Fulfillment Network?", "How does warehouse routing work?", "What is carrier API integration?"],
    },
    {
      id: 11,
      title: 'Add Tax Service',
      explanation: "The Tax Service calculates sales tax for every order based on the customer's location and the merchant's nexus. US sales tax alone has 10,000+ tax jurisdictions — each with different rates and rules.",
      action: 'Press ⌘K, search for "Tax Service", and add it to the canvas. Then connect Checkout Service → Tax Service.',
      searchHint: 'Tax Service',
      why: "Tax calculation is surprisingly complex. A merchant in California selling to a customer in New York must apply New York state tax, county tax, and city tax — all different rates. The Tax Service integrates with Avalara or TaxJar to handle this complexity.",
      messages: [
        { type: 'guide', content: "US sales tax has 10,000+ jurisdictions. The Tax Service handles this complexity so merchants don't have to." },
        { type: 'guide', content: "After the 2018 South Dakota v. Wayfair Supreme Court ruling, online merchants must collect sales tax in states where they have 'economic nexus' — even without a physical presence. Shopify's Tax Service automatically determines nexus, calculates the correct rate, and remits taxes on behalf of merchants." },
        { type: 'guide', content: 'Press ⌘K, search for "Tax Service", add it, then connect: Checkout Service → Tax Service.' },
      ],
      validation: {
        requiredNodes: ['tax_service'],
        requiredEdges: [{ from: 'Checkout', to: 'Tax' }],
        successMessage: "Tax Service added and connected. Final step — the data layer.",
        errorMessage: 'Add a Tax Service and connect it from the Checkout Service.',
      },
      openingMessage: "Shopify handles tax remittance for merchants. Connect Checkout Service → Tax Service — checkout calls the tax service to calculate the correct rate.",
      defaultSuggestions: ["What is economic nexus?", "How does Shopify calculate sales tax?", "What is Avalara?"],
    },
    {
      id: 12,
      title: 'Add NoSQL Database',
      explanation: "Shopify stores product catalogs, merchant configurations, and analytics data in NoSQL databases. Product catalogs have flexible schemas — a clothing merchant has sizes and colors; an electronics merchant has specs and compatibility.",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas. Then connect Order Service → NoSQL Database.',
      searchHint: 'NoSQL Database',
      why: "Product data is highly variable across merchants. A NoSQL document store handles the flexible schema without requiring ALTER TABLE for every new product attribute type.",
      messages: [
        { type: 'guide', content: "Product catalogs are stored in NoSQL — each product is a document with merchant-specific attributes." },
        { type: 'guide', content: "A clothing merchant's product has: size, color, material, care instructions. An electronics merchant's product has: voltage, compatibility, warranty. A food merchant's product has: ingredients, allergens, expiry. NoSQL handles all of these without a shared schema. Shopify's metafields feature lets merchants add any custom attribute." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", add it, then connect: Order Service → NoSQL Database.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [{ from: 'Order', to: 'NoSQL' }],
        successMessage: "🎉 Tutorial complete! You've designed Shopify's core architecture from scratch.",
        errorMessage: 'Add a NoSQL Database and connect it from the Order Service.',
      },
      openingMessage: "Shopify's metafields let merchants add custom product attributes. Connect Order Service → NoSQL Database — order and product data is persisted here.",
      defaultSuggestions: ["What are Shopify metafields?", "How does NoSQL handle schema flexibility?", "What is document storage for e-commerce?"],
    },
  ],
};
