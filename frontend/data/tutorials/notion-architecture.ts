import type { TutorialData } from './chatgpt-architecture';

export const notionTutorial: TutorialData = {
  id: 'notion-architecture',
  title: 'How to Design Notion Architecture',
  description: 'Build a real-time collaborative workspace. Learn operational transforms, block-based data models, conflict resolution, and how 35 million users edit documents simultaneously.',
  difficulty: 'Advanced',
  category: 'Collaboration',
  estimatedTime: '~28 mins',
  nodeCount: 9,
  stepCount: 9,
  icon: 'FileText',
  color: '#ffffff',
  tags: ['CRDT', 'Real-time', 'Collaboration', 'Sync', 'Blocks'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "Notion's client is a web app built on a block-based editor. Every paragraph, heading, image, and database is a block with a unique ID. The client maintains a local copy of the document and syncs changes in real time.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "The block-based client model is fundamental to Notion's architecture. Every edit is a block operation — insert, update, delete — which makes conflict resolution tractable.",
      messages: [
        { type: 'guide', content: "Welcome to the Notion Architecture tutorial. 35 million users collaboratively editing documents, databases, and wikis in real time." },
        { type: 'guide', content: "Notion's client is a block-based editor — every element is a block with a unique ID. When you type, you're not editing a string, you're creating block operations that get synced to every collaborator." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Client added. Now the entry layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "Notion's editor treats every element as a block with a unique ID. Why does that make real-time collaboration easier?",
      defaultSuggestions: ["What is a block-based editor?", "How does Notion handle offline edits?", "What is a CRDT?"],
    },
    {
      id: 2,
      title: 'Add the API Gateway',
      explanation: "All requests from Notion clients flow through an API Gateway. It handles authentication, routes REST requests to backend services, and upgrades WebSocket connections for real-time collaboration.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas.',
      searchHint: 'API Gateway',
      why: "The API Gateway is the single entry point that handles both REST (page loads, search) and WebSocket (real-time sync) traffic. Separating these concerns at the gateway level simplifies the backend.",
      messages: [
        { type: 'guide', content: "Notion handles two types of traffic: REST requests for page loads and WebSocket connections for real-time collaboration." },
        { type: 'guide', content: "The API Gateway routes both. A page load is a REST request to fetch block data. Opening a shared document upgrades to a WebSocket connection for live sync." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [],
        successMessage: "API Gateway added. Now the auth layer.",
        errorMessage: 'Add an API Gateway node to the canvas.',
      },
      openingMessage: "Notion handles both REST and WebSocket traffic. How does the API Gateway manage both connection types?",
      defaultSuggestions: ["What is a WebSocket?", "When does Notion use REST vs WebSocket?", "How does Notion handle reconnections?"],
    },
    {
      id: 3,
      title: 'Add Auth Service',
      explanation: "Notion authenticates users via email magic links or Google OAuth. The Auth Service issues session tokens and enforces workspace-level permissions — who can view, comment, or edit each page.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas.',
      searchHint: 'Auth Service',
      why: "Notion's permission model is hierarchical — workspace > teamspace > page. The Auth Service must evaluate this hierarchy on every request to determine what a user can see and edit.",
      messages: [
        { type: 'guide', content: "Notion uses magic link email auth or Google OAuth — no passwords to remember." },
        { type: 'guide', content: "The Auth Service also enforces Notion's permission hierarchy: workspace admins, members, guests, and public viewers all have different access levels. Every block read or write is permission-checked." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [],
        successMessage: "Auth added. Now the real-time sync layer.",
        errorMessage: 'Add an Auth Service node to the canvas.',
      },
      openingMessage: "Notion's permission model is hierarchical — workspace, teamspace, page. How does that affect every request?",
      defaultSuggestions: ["What is magic link auth?", "How does Notion handle guest access?", "What is a permission hierarchy?"],
    },
    {
      id: 4,
      title: 'Add Sync Service',
      explanation: "The Sync Service is the heart of Notion's real-time collaboration. It receives block operations from clients, applies them using operational transforms, and broadcasts the result to all connected collaborators.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas.',
      searchHint: 'Microservice',
      why: "Real-time collaboration requires a central authority to order concurrent edits. The Sync Service ensures that when two people edit the same block simultaneously, both edits are applied correctly without data loss.",
      messages: [
        { type: 'guide', content: "Here's the hard problem: two people edit the same sentence at the same time. Who wins?" },
        { type: 'guide', content: "The Sync Service uses operational transforms — it receives both edits, transforms them so they're compatible, and applies both. Neither person loses their work. This is the core algorithm behind Google Docs and Notion." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Sync Service added. Now the block storage layer.",
        errorMessage: 'Add a Microservice (Sync Service) to the canvas.',
      },
      openingMessage: "Operational transforms let two people edit the same sentence simultaneously. How does that algorithm work?",
      defaultSuggestions: ["What is operational transform?", "What is a CRDT?", "How does Google Docs handle conflicts?"],
    },
    {
      id: 5,
      title: 'Add Block Service',
      explanation: "The Block Service handles CRUD operations on Notion's block tree. Every page is a tree of blocks. The Block Service resolves block IDs, fetches subtrees, and applies block mutations atomically.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas.',
      searchHint: 'Microservice',
      why: "Separating block storage logic into its own service means the Sync Service can focus on conflict resolution while the Block Service handles the complexity of tree traversal, block versioning, and atomic mutations.",
      messages: [
        { type: 'guide', content: "Every Notion page is a tree of blocks. A page block contains heading blocks, paragraph blocks, database blocks — each with their own children." },
        { type: 'guide', content: "The Block Service handles all reads and writes to this tree. When you move a block, it atomically removes it from one parent and inserts it into another — no partial states." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add another one for the Block Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Block Service added. Now the database layer.",
        errorMessage: 'Add a second Microservice (Block Service) to the canvas.',
      },
      openingMessage: "Notion stores pages as block trees. How does moving a block between pages work atomically?",
      defaultSuggestions: ["What is a block tree?", "How does Notion handle block versioning?", "What is an atomic operation?"],
    },
    {
      id: 6,
      title: 'Add NoSQL Database',
      explanation: "Notion stores all blocks in a NoSQL database. Each block is a document with an ID, type, properties, and parent reference. The flexible document model handles Notion's 50+ block types without schema migrations.",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.',
      searchHint: 'NoSQL Database',
      why: "Notion's 50+ block types (paragraph, heading, database, kanban, calendar, etc.) have different properties. NoSQL's flexible document model handles this without requiring a new SQL column for every block type.",
      messages: [
        { type: 'guide', content: "Notion has 50+ block types — paragraphs, databases, kanban boards, calendars, embeds. Each has different properties." },
        { type: 'guide', content: "NoSQL's flexible document model handles this perfectly. A paragraph block has text content. A database block has schema and rows. Both are just documents with different shapes — no schema migrations needed." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [],
        successMessage: "NoSQL Database added. Now the cache layer.",
        errorMessage: 'Add a NoSQL Database node to the canvas.',
      },
      openingMessage: "Notion has 50+ block types with different schemas. Why is NoSQL a better fit than SQL here?",
      defaultSuggestions: ["What is a document database?", "How does Notion store database blocks?", "What is schema flexibility?"],
    },
    {
      id: 7,
      title: 'Add In-Memory Cache',
      explanation: "Notion caches frequently accessed pages and block trees in Redis. When you open a popular shared page, the block tree is served from cache — not the database. Cache TTL is 60 seconds for collaborative pages.",
      action: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas.',
      searchHint: 'In-Memory Cache',
      why: "Page loads must be fast. Fetching a complex block tree from the database on every load would be too slow. The cache serves the block tree in under 5ms, making page loads feel instant.",
      messages: [
        { type: 'guide', content: "When 1000 people have the same Notion page open, you don't want 1000 database reads per second." },
        { type: 'guide', content: "The cache stores the block tree for popular pages. Cache TTL is short — 60 seconds — because collaborative pages change frequently. But even a 60-second cache dramatically reduces database load." },
        { type: 'guide', content: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['in_memory_cache'],
        requiredEdges: [],
        successMessage: "Cache added. Now the async event layer.",
        errorMessage: 'Add an In-Memory Cache node to the canvas.',
      },
      openingMessage: "Notion uses a 60-second cache TTL for collaborative pages. Why so short?",
      defaultSuggestions: ["What is cache invalidation?", "How does Notion handle cache consistency?", "What is a TTL?"],
    },
    {
      id: 8,
      title: 'Add Message Queue',
      explanation: "When a block is updated, downstream services need to react — search indexing, notification delivery, analytics, and backup. A Message Queue decouples these from the write path so they don't slow down the editor.",
      action: 'Press ⌘K, search for "Message Queue", and add it to the canvas.',
      searchHint: 'Message Queue',
      why: "Search indexing and notification delivery are slow operations. Running them synchronously on every block edit would make the editor feel laggy. The queue makes the write path fast and lets downstream services catch up asynchronously.",
      messages: [
        { type: 'guide', content: "Every block edit triggers downstream work: update the search index, send notifications to @mentioned users, log analytics..." },
        { type: 'guide', content: "A Message Queue decouples all of this from the write path. The Block Service publishes a 'block_updated' event and returns immediately. Search indexing happens asynchronously — the editor stays fast." },
        { type: 'guide', content: 'Press ⌘K, search for "Message Queue", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['message_queue'],
        requiredEdges: [],
        successMessage: "Message Queue added. Final step — object storage.",
        errorMessage: 'Add a Message Queue node to the canvas.',
      },
      openingMessage: "Notion decouples search indexing from the write path. What happens if the queue falls behind?",
      defaultSuggestions: ["What is async processing?", "How does Notion handle @mentions?", "What is eventual consistency?"],
    },
    {
      id: 9,
      title: 'Add Object Storage',
      explanation: "Images, files, and attachments uploaded to Notion are stored in object storage. Files are uploaded directly from the client to object storage using pre-signed URLs — they never pass through Notion's servers.",
      action: 'Press ⌘K, search for "Object Storage", and add it to the canvas.',
      searchHint: 'Object Storage',
      why: "Direct client-to-storage uploads via pre-signed URLs offload bandwidth from Notion's servers. A 100MB file upload goes directly to S3 — Notion's backend only handles the metadata.",
      messages: [
        { type: 'guide', content: "When you upload an image to Notion, it doesn't go through Notion's servers. It goes directly to object storage." },
        { type: 'guide', content: "Notion generates a pre-signed URL, the client uploads directly to S3, then tells Notion the upload is complete. Notion stores the URL as a block property. This keeps Notion's servers out of the file transfer path entirely." },
        { type: 'guide', content: 'Press ⌘K, search for "Object Storage", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['object_storage'],
        requiredEdges: [],
        successMessage: "🎉 Tutorial complete! You've designed Notion's core collaborative architecture from scratch.",
        errorMessage: 'Add an Object Storage node to the canvas.',
      },
      openingMessage: "Notion uses pre-signed URLs for direct client uploads. How does that reduce server load?",
      defaultSuggestions: ["What is a pre-signed URL?", "How does Notion handle large file uploads?", "What is object storage?"],
    },
  ],
};
