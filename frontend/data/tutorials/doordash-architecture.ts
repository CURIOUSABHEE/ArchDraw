import type { TutorialData } from './index';

export const doordashTutorial: TutorialData = {
  id: 'doordash-architecture',
  title: 'How to Design DoorDash Architecture',
  description: 'Build a food delivery platform completing 2 billion deliveries annually. Learn real-time order routing, dasher dispatch, ETA prediction, geofencing, and the three-sided marketplace problem.',
  difficulty: 'Advanced',
  category: 'Delivery Platform',
  estimatedTime: '~30 mins',
  nodeCount: 11,
  stepCount: 11,
  icon: 'Bike',
  color: '#ff3008',
  tags: ['Real-time Routing', 'ETA', 'Geofencing', 'Dispatch'],
  steps: [
    {
      id: 1,
      title: 'Start with the Web Client',
      explanation: "DoorDash has three clients: the customer app (ordering), the Dasher app (delivery), and the merchant tablet (order management). All three are part of the same system. We start with the customer-facing web client.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "DoorDash is a three-sided marketplace: customers, dashers, and merchants. Each side has different real-time requirements. Customers need live order tracking. Dashers need turn-by-turn navigation. Merchants need instant order notifications.",
      messages: [
        { type: 'guide', content: "Welcome to the DoorDash Architecture tutorial. 2 billion deliveries annually, 27 countries, and a real-time system that must predict your delivery time accurately enough that you trust it." },
        { type: 'guide', content: "DoorDash's hardest problem: predicting delivery time accurately. Too optimistic and customers are disappointed. Too pessimistic and they order from a competitor. The ETA model runs in real-time using ML, traffic data, restaurant prep times, and dasher location." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Web Client added. Now the API layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "DoorDash is a three-sided marketplace. How do the needs of customers, dashers, and merchants differ architecturally?",
      defaultSuggestions: ["What is a three-sided marketplace?", "How does DoorDash handle real-time tracking?", "What makes delivery ETA hard to predict?"],
    },
    {
      id: 2,
      title: 'Add API Gateway',
      explanation: "DoorDash's API Gateway handles requests from all three clients — customer app, Dasher app, and merchant tablet. It routes to the correct microservice and handles authentication for each client type.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas. Then connect Client → API Gateway.',
      searchHint: 'API Gateway',
      why: "Three different client types with different auth requirements and different rate limits. The API Gateway enforces per-client-type policies — a Dasher app has different permissions than a customer app.",
      messages: [
        { type: 'guide', content: "DoorDash's API Gateway handles three different client types with different permissions and rate limits." },
        { type: 'guide', content: "A customer app can browse restaurants and place orders. A Dasher app can accept deliveries and update location. A merchant tablet can accept/reject orders and update prep times. The gateway enforces these permissions — a customer can't update a restaurant's menu, even if they bypass the UI." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", add it, then connect: Client → API Gateway.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [{ from: 'Web', to: 'API Gateway' }],
        successMessage: "API Gateway added and connected. Now distribute the load.",
        errorMessage: 'Add an API Gateway and connect it from the Client.',
      },
      openingMessage: "DoorDash has three client types with different permissions. Connect Client → API Gateway — all three clients enter through here.",
      defaultSuggestions: ["How does DoorDash handle multi-client auth?", "What is role-based access control?", "How does the Dasher app differ from the customer app?"],
    },
    {
      id: 3,
      title: 'Add Load Balancer',
      explanation: "DoorDash's Load Balancer distributes traffic across service instances. Lunch and dinner rushes create predictable traffic spikes — DoorDash pre-scales at 11am and 5pm daily.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas. Then connect API Gateway → Load Balancer.',
      searchHint: 'Load Balancer',
      why: "Food delivery has predictable daily traffic patterns. Pre-scaling before lunch and dinner rushes is more efficient than reactive autoscaling, which has a 2-3 minute lag before new instances are ready.",
      messages: [
        { type: 'guide', content: "DoorDash traffic spikes at lunch (12pm) and dinner (6pm) every day — completely predictable." },
        { type: 'guide', content: "DoorDash pre-scales their order service at 11:30am and 5:30pm daily. By the time the rush hits, capacity is already available. Reactive autoscaling would mean the first 2-3 minutes of the rush hit under-provisioned servers — exactly when customers are most likely to abandon." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", add it, then connect: API Gateway → Load Balancer.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [{ from: 'API Gateway', to: 'Load Balancer' }],
        successMessage: "Load Balancer added and connected. Now authentication.",
        errorMessage: 'Add a Load Balancer and connect it from the API Gateway.',
      },
      openingMessage: "DoorDash pre-scales before meal rushes. Connect API Gateway → Load Balancer to distribute the three-client traffic.",
      defaultSuggestions: ["What is proactive autoscaling?", "How does DoorDash handle meal rush traffic?", "What is the lag in reactive autoscaling?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: "DoorDash's Auth Service handles customer accounts, Dasher onboarding (with background checks), and merchant authentication. Dashers require identity verification — the Auth Service integrates with background check providers.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas. Then connect Load Balancer → Auth Service.',
      searchHint: 'Auth Service',
      why: "Dasher onboarding is more complex than customer signup — it requires identity verification, background checks, and vehicle registration. The Auth Service manages the multi-step onboarding flow.",
      messages: [
        { type: 'guide', content: "DoorDash's auth handles three different account types with very different onboarding requirements." },
        { type: 'guide', content: "Customer signup is instant — email and password. Dasher onboarding takes days: identity verification, background check via Checkr, vehicle registration, and insurance verification. The Auth Service tracks onboarding state and only activates Dasher accounts after all checks pass." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", add it, then connect: Load Balancer → Auth Service.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Auth' }],
        successMessage: "Auth Service added and connected. Now the order service.",
        errorMessage: 'Add an Auth Service and connect it from the Load Balancer.',
      },
      openingMessage: "Dasher onboarding requires background checks. Connect Load Balancer → Auth Service so every request is validated.",
      defaultSuggestions: ["How does DoorDash verify Dashers?", "What is identity verification?", "How does background check integration work?"],
    },
    {
      id: 5,
      title: 'Add Order Service',
      explanation: "The Order Service manages the lifecycle of a delivery order: placed → accepted by restaurant → being prepared → picked up by Dasher → delivered. Each state transition triggers notifications and updates to all three parties.",
      action: 'Press ⌘K, search for "Order Service", and add it to the canvas. Then connect Auth Service → Order Service.',
      searchHint: 'Order Service',
      why: "An order involves three parties who all need real-time updates. The Order Service is the state machine that coordinates between customer, restaurant, and Dasher — ensuring all three stay in sync.",
      messages: [
        { type: 'guide', content: "The Order Service is a state machine coordinating three parties simultaneously." },
        { type: 'guide', content: "Order states: PLACED → RESTAURANT_ACCEPTED → PREPARING → READY_FOR_PICKUP → DASHER_ASSIGNED → PICKED_UP → DELIVERED. Each transition notifies all three parties. If the restaurant rejects the order, the customer is refunded and the Dasher assignment is cancelled. The Order Service orchestrates all of this." },
        { type: 'guide', content: 'Press ⌘K, search for "Order Service", add it, then connect: Auth Service → Order Service.' },
      ],
      validation: {
        requiredNodes: ['order_service'],
        requiredEdges: [{ from: 'Auth', to: 'Order' }],
        successMessage: "Order Service added and connected. Now the Dasher dispatch system.",
        errorMessage: 'Add an Order Service and connect it from the Auth Service.',
      },
      openingMessage: "The Order Service coordinates three parties simultaneously. Connect Auth Service → Order Service — authenticated orders flow here for state management.",
      defaultSuggestions: ["What is an order state machine?", "How does DoorDash handle order rejection?", "What is three-party coordination?"],
    },
    {
      id: 6,
      title: 'Add Dasher Service',
      explanation: "The Dasher Service manages Dasher availability, location tracking, and delivery assignment. It knows which Dashers are online, where they are, and how many deliveries they're currently handling.",
      action: 'Press ⌘K, search for "Dasher Service", and add it to the canvas. Then connect Order Service → Dasher Service.',
      searchHint: 'Dasher Service',
      why: "Dasher location updates every 4 seconds from the Dasher app. The Dasher Service must process millions of location updates per minute and maintain a real-time view of every active Dasher's position.",
      messages: [
        { type: 'guide', content: "The Dasher Service processes millions of location updates per minute — every active Dasher pings every 4 seconds." },
        { type: 'guide', content: "With 1 million active Dashers, that's 250,000 location updates per second. The Dasher Service stores current positions in Redis (not a database) for fast geospatial queries. When a new order comes in, the Routing Engine queries Redis to find nearby available Dashers." },
        { type: 'guide', content: 'Press ⌘K, search for "Dasher Service", add it, then connect: Order Service → Dasher Service.' },
      ],
      validation: {
        requiredNodes: ['dasher_service'],
        requiredEdges: [{ from: 'Order', to: 'Dasher' }],
        successMessage: "Dasher Service added and connected. Now the routing engine.",
        errorMessage: 'Add a Dasher Service and connect it from the Order Service.',
      },
      openingMessage: "DoorDash processes 250,000 location updates per second. Connect Order Service → Dasher Service — new orders trigger dasher location queries.",
      defaultSuggestions: ["How does DoorDash track Dasher location?", "What is Redis geospatial?", "How does real-time location storage work?"],
    },
    {
      id: 7,
      title: 'Add Routing Engine',
      explanation: "The Routing Engine assigns Dashers to orders using an optimization algorithm. It considers: Dasher proximity to restaurant, Dasher current workload, estimated restaurant prep time, and traffic conditions.",
      action: 'Press ⌘K, search for "Routing Engine", and add it to the canvas. Then connect Dasher Service → Routing Engine.',
      searchHint: 'Routing Engine',
      why: "Optimal Dasher assignment is an NP-hard optimization problem. DoorDash uses a heuristic algorithm that runs in milliseconds — not the mathematically optimal solution, but good enough to minimize delivery times at scale.",
      messages: [
        { type: 'guide', content: "The Routing Engine solves an optimization problem: which Dasher should pick up which order to minimize total delivery time?" },
        { type: 'guide', content: "The algorithm considers: distance from Dasher to restaurant, whether the Dasher is already carrying another order (batching), restaurant prep time (no point sending a Dasher who'll arrive before the food is ready), and traffic. DoorDash batches orders — one Dasher can carry two orders from nearby restaurants to nearby customers." },
        { type: 'guide', content: 'Press ⌘K, search for "Routing Engine", add it, then connect: Dasher Service → Routing Engine.' },
      ],
      validation: {
        requiredNodes: ['routing_engine'],
        requiredEdges: [{ from: 'Dasher', to: 'Routing' }],
        successMessage: "Routing Engine added and connected. Now ETA prediction.",
        errorMessage: 'Add a Routing Engine and connect it from the Dasher Service.',
      },
      openingMessage: "DoorDash batches orders so one Dasher carries two deliveries. Connect Dasher Service → Routing Engine — location data feeds the matching algorithm.",
      defaultSuggestions: ["What is order batching?", "How does DoorDash optimize Dasher assignment?", "What is the traveling salesman problem?"],
    },
    {
      id: 8,
      title: 'Add ETA Service',
      explanation: "The ETA Service predicts delivery time using ML models trained on millions of historical deliveries. It considers: restaurant prep time (learned per restaurant), Dasher travel time (from real-time traffic), and handoff time.",
      action: 'Press ⌘K, search for "ETA Service", and add it to the canvas. Then connect Routing Engine → ETA Service.',
      searchHint: 'ETA Service',
      why: "ETA accuracy directly impacts customer satisfaction. DoorDash's ML model predicts prep time per restaurant (McDonald's is faster than a sit-down restaurant), travel time from live traffic, and adds buffer for parking and handoff.",
      messages: [
        { type: 'guide', content: "The ETA Service is a ML model that predicts delivery time with enough accuracy that customers trust it." },
        { type: 'guide', content: "The model has learned: this McDonald's location takes 4 minutes to prepare a Big Mac. This intersection has a 2-minute traffic delay at 6pm on Fridays. This apartment building takes 3 minutes to navigate to the right unit. All of this is learned from historical delivery data and updated in real-time." },
        { type: 'guide', content: 'Press ⌘K, search for "ETA Service", add it, then connect: Routing Engine → ETA Service.' },
      ],
      validation: {
        requiredNodes: ['eta_service'],
        requiredEdges: [{ from: 'Routing', to: 'ETA' }],
        successMessage: "ETA Service added and connected. Now geofencing.",
        errorMessage: 'Add an ETA Service and connect it from the Routing Engine.',
      },
      openingMessage: "DoorDash's ETA model learns per-restaurant prep times. Connect Routing Engine → ETA Service — the routing algorithm feeds into ETA prediction.",
      defaultSuggestions: ["How does DoorDash predict ETA?", "What is ML-based ETA prediction?", "How does DoorDash learn restaurant prep times?"],
    },
    {
      id: 9,
      title: 'Add Geofence Service',
      explanation: "The Geofence Service defines delivery zones and detects when Dashers enter or exit key locations — restaurant pickup zones, customer delivery zones, and no-delivery areas. It triggers events when Dashers cross geofence boundaries.",
      action: 'Press ⌘K, search for "Geofence Service", and add it to the canvas. Then connect Dasher Service → Geofence Service.',
      searchHint: 'Geofence Service',
      why: "Geofencing enables automatic status updates: when a Dasher enters the restaurant's geofence, the customer is notified 'Your Dasher has arrived at the restaurant'. When they enter the customer's geofence, 'Your Dasher is nearby'.",
      messages: [
        { type: 'guide', content: "The Geofence Service triggers automatic status updates when Dashers cross location boundaries." },
        { type: 'guide', content: "Each restaurant has a 100-meter geofence. When a Dasher enters it, the Order Service transitions to DASHER_AT_RESTAURANT and notifies the customer. When the Dasher enters the customer's geofence, the customer gets 'Your Dasher is 2 minutes away'. These automatic triggers reduce the need for manual status updates." },
        { type: 'guide', content: 'Press ⌘K, search for "Geofence Service", add it, then connect: Dasher Service → Geofence Service.' },
      ],
      validation: {
        requiredNodes: ['geofence_service'],
        requiredEdges: [{ from: 'Dasher', to: 'Geofence' }],
        successMessage: "Geofence Service added and connected. Now the message queue.",
        errorMessage: 'Add a Geofence Service and connect it from the Dasher Service.',
      },
      openingMessage: "Geofencing triggers automatic order status updates. Connect Dasher Service → Geofence Service — Dasher location triggers boundary crossing events.",
      defaultSuggestions: ["What is geofencing?", "How does DoorDash detect location boundary crossings?", "What is geospatial indexing?"],
    },
    {
      id: 10,
      title: 'Add Message Queue',
      explanation: "DoorDash's Message Queue (Kafka) handles async events: order placed, Dasher assigned, order picked up, delivery completed. These events fan out to notifications, analytics, and billing.",
      action: 'Press ⌘K, search for "Message Queue", and add it to the canvas. Then connect Order Service → Message Queue.',
      searchHint: 'Message Queue',
      why: "When an order is delivered, 5 things happen simultaneously: customer gets a receipt, Dasher gets paid, merchant gets settlement, analytics records the delivery, and the ML model gets a training data point. Kafka decouples all of these.",
      messages: [
        { type: 'guide', content: "When a delivery completes, 5 systems need to react simultaneously. Kafka decouples them all." },
        { type: 'guide', content: "A single 'ORDER_DELIVERED' event goes to Kafka. Five consumers process it independently: the notification service sends the receipt, the payment service pays the Dasher, the settlement service pays the merchant, the analytics pipeline records metrics, and the ML training pipeline adds a data point to improve future ETA predictions." },
        { type: 'guide', content: 'Press ⌘K, search for "Message Queue", add it, then connect: Order Service → Message Queue.' },
      ],
      validation: {
        requiredNodes: ['message_queue'],
        requiredEdges: [{ from: 'Order', to: 'Message' }],
        successMessage: "Message Queue added and connected. Final step — the data cache.",
        errorMessage: 'Add a Message Queue and connect it from the Order Service.',
      },
      openingMessage: "A single delivery completion triggers 5 downstream systems. Connect Order Service → Message Queue — order events fan out to all consumers here.",
      defaultSuggestions: ["How does Kafka fan-out work?", "What is consumer group?", "How does DoorDash handle payment on delivery?"],
    },
    {
      id: 11,
      title: 'Add In-Memory Cache',
      explanation: "DoorDash caches restaurant menus, Dasher locations, and active order states in an in-memory cache. Restaurant menus are read thousands of times per minute but change rarely — perfect for caching.",
      action: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas. Then connect Order Service → In-Memory Cache.',
      searchHint: 'In-Memory Cache',
      why: "Restaurant menus are the most-read data in DoorDash. Caching them in Redis means menu loads are sub-millisecond. Without caching, every menu view would hit the database — catastrophic at scale.",
      messages: [
        { type: 'guide', content: "Restaurant menus are read millions of times per day but change rarely — ideal cache candidates." },
        { type: 'guide', content: "DoorDash caches menus in Redis with a 5-minute TTL. When a restaurant updates their menu, the cache is invalidated immediately. Dasher locations are also cached in Redis with a 4-second TTL (matching the location update frequency). Active order states are cached to avoid database reads on every status check." },
        { type: 'guide', content: 'Press ⌘K, search for "In-Memory Cache", add it, then connect: Order Service → In-Memory Cache.' },
      ],
      validation: {
        requiredNodes: ['in_memory_cache'],
        requiredEdges: [{ from: 'Order', to: 'Cache' }],
        successMessage: "🎉 Tutorial complete! You've designed DoorDash's core architecture from scratch.",
        errorMessage: 'Add an In-Memory Cache and connect it from the Order Service.',
      },
      openingMessage: "DoorDash caches menus with a 5-minute TTL. Connect Order Service → In-Memory Cache — active order states and menus are served from here.",
      defaultSuggestions: ["What is cache TTL?", "How does cache invalidation work?", "How does DoorDash cache Dasher locations?"],
    },
  ],
};
