import type { TutorialData } from './index';

export const stripeTutorial: TutorialData = {
  id: 'stripe-architecture',
  title: 'How to Design Stripe Architecture',
  description: 'Build a production payment processing system. Learn idempotency keys, webhook delivery, fraud detection, and distributed ledger design that handles trillions in payments.',
  difficulty: 'Advanced',
  category: 'Payments',
  estimatedTime: '~28 mins',
  nodeCount: 10,
  stepCount: 10,
  icon: 'CreditCard',
  color: '#635bff',
  tags: ['Idempotency', 'Webhooks', 'Fraud', 'Ledger', 'Security'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "Stripe's client is the merchant's checkout page — a web or mobile app that collects card details and sends them to Stripe. Stripe.js tokenizes card data in the browser so raw card numbers never touch the merchant's servers.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "The client-side tokenization model is fundamental to Stripe's security architecture. It means merchants can accept payments without ever handling sensitive card data.",
      messages: [
        { type: 'guide', content: "Welcome to the Stripe Architecture tutorial. Stripe processes hundreds of billions of dollars annually across 197 countries." },
        { type: 'guide', content: "The client is the merchant's checkout page. Stripe.js runs in the browser, tokenizes card data locally, and sends a token to Stripe — raw card numbers never touch the merchant's server." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Client added. Now the entry layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "Stripe.js tokenizes card data in the browser. Why is that critical for PCI compliance?",
      defaultSuggestions: ["What is PCI compliance?", "What is card tokenization?", "How does Stripe.js work?"],
    },
    {
      id: 2,
      title: 'Add the API Gateway',
      explanation: "All payment requests flow through Stripe's API Gateway. It validates API keys, enforces rate limits, and routes requests to the correct internal service.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas. Then connect Client → API Gateway.',
      searchHint: 'API Gateway',
      why: "Stripe's API Gateway is the enforcement point for API key validation, rate limiting, and idempotency key deduplication — all critical for a payment system.",
      messages: [
        { type: 'guide', content: "Every payment request hits the API Gateway first. It validates your API key and enforces rate limits." },
        { type: 'guide', content: "The gateway also handles idempotency key deduplication — if the same payment request is sent twice (network retry), the gateway returns the first result instead of charging twice." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", add it, then connect: Client → API Gateway.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [{ from: 'Web', to: 'API Gateway' }],
        successMessage: "API Gateway added and connected. Now the auth layer.",
        errorMessage: 'Add an API Gateway and connect it from the Client.',
      },
      openingMessage: "Stripe's API Gateway handles idempotency key deduplication. Connect Client → API Gateway — all merchant payment requests flow through here.",
      defaultSuggestions: ["What is an idempotency key?", "How does Stripe handle retries?", "What is API versioning?"],
    },
    {
      id: 3,
      title: 'Add Auth Service',
      explanation: "Stripe authenticates merchants using API keys — a publishable key for client-side operations and a secret key for server-side operations. The Auth Service validates these keys and determines the merchant's permissions.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas. Then connect API Gateway → Auth Service.',
      searchHint: 'Auth Service',
      why: "API key validation is the first line of defense. A compromised secret key could allow unauthorized charges. The Auth Service also enforces per-merchant rate limits and permission scopes.",
      messages: [
        { type: 'guide', content: "Stripe uses API keys for authentication — not username/password. Every merchant gets a publishable key (safe for client-side) and a secret key (server-side only)." },
        { type: 'guide', content: "The Auth Service validates these keys on every request and enforces permission scopes. A restricted key might only be allowed to create charges, not issue refunds." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", add it, then connect: API Gateway → Auth Service.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [{ from: 'API Gateway', to: 'Auth' }],
        successMessage: "Auth added and connected. Now the core payment orchestration.",
        errorMessage: 'Add an Auth Service and connect it from the API Gateway.',
      },
      openingMessage: "Stripe uses two API keys — publishable and secret. Connect API Gateway → Auth Service so every request is validated before processing.",
      defaultSuggestions: ["What is a publishable key?", "How does Stripe handle key rotation?", "What are restricted API keys?"],
    },
    {
      id: 4,
      title: 'Add Payment Service',
      explanation: "The Payment Service orchestrates the entire payment flow. It uses idempotency keys to prevent double charges, coordinates with fraud detection, calls the bank network, and updates the ledger — all in under 200ms.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas. Then connect Auth Service → Payment Service (Microservice).',
      searchHint: 'Microservice',
      why: "The Payment Service is Stripe's core IP. Isolating it as a microservice means it can be updated, A/B tested, and scaled independently. Idempotency is implemented here to guarantee exactly-once payment semantics.",
      messages: [
        { type: 'guide', content: "The Payment Service is the brain of Stripe — it orchestrates the entire payment flow in under 200ms." },
        { type: 'guide', content: "It implements idempotency keys: a unique ID per payment attempt. If the same request arrives twice (network retry), the second one returns the first result. This is one of the hardest problems in distributed payments." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add it, then connect: Auth Service → Payment Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Auth', to: 'Microservice' }],
        successMessage: "Payment Service added and connected. Now fraud detection.",
        errorMessage: 'Add a Microservice (Payment Service) and connect it from the Auth Service.',
      },
      openingMessage: "Stripe implements idempotency keys to prevent double charges. Connect Auth Service → Payment Service — validated requests flow here for orchestration.",
      defaultSuggestions: ["How do idempotency keys work?", "What is exactly-once semantics?", "How does Stripe handle network failures?"],
    },
    {
      id: 5,
      title: 'Add Fraud Detection',
      explanation: "Stripe Radar runs 1000+ ML signals per transaction — card velocity, device fingerprint, email age, IP reputation. All in under 50ms.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas. Then connect Payment Service → Fraud Detection (Microservice).',
      searchHint: 'Microservice',
      why: "Fraud detection is a competitive differentiator for Stripe. Merchants choose Stripe partly because Radar reduces chargebacks. Every false positive is a lost sale, so precision matters as much as recall.",
      messages: [
        { type: 'guide', content: "Before charging a card, Stripe runs it through Radar — their ML fraud detection system." },
        { type: 'guide', content: "Radar evaluates 1000+ signals in under 50ms: card velocity (how many charges in the last hour), device fingerprint, email age, IP reputation, and behavioral patterns across Stripe's entire network." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add another one for Fraud Detection, then connect: Payment Service → Fraud Detection.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Microservice', to: 'Microservice' }],
        successMessage: "Fraud Detection added. Now the bank connection.",
        errorMessage: 'Add a second Microservice (Fraud Detection) and connect it from the Payment Service.',
      },
      openingMessage: "Stripe Radar evaluates 1000+ signals in 50ms. Connect Payment Service → Fraud Detection — every payment is screened before reaching the bank.",
      defaultSuggestions: ["What is a chargeback?", "How does Stripe Radar work?", "What is card velocity?"],
    },
    {
      id: 6,
      title: 'Add Payment Gateway',
      explanation: "The Payment Gateway connects Stripe to the actual bank networks — Visa, Mastercard, and ACH. It translates Stripe's internal payment request into the ISO 8583 message format that banks understand.",
      action: 'Press ⌘K, search for "Payment Gateway", and add it to the canvas. Then connect Fraud Detection → Payment Gateway.',
      searchHint: 'Payment Gateway',
      why: "Banks speak a different protocol (ISO 8583) than modern APIs. The Payment Gateway is the translator between Stripe's modern infrastructure and the legacy banking network.",
      messages: [
        { type: 'guide', content: "After fraud checks pass, the payment needs to reach the actual bank. That's the Payment Gateway's job." },
        { type: 'guide', content: "Banks use ISO 8583 — a message format from the 1980s. The Payment Gateway translates Stripe's modern API request into this format and sends it to Visa, Mastercard, or the ACH network." },
        { type: 'guide', content: 'Press ⌘K, search for "Payment Gateway", add it, then connect: Fraud Detection → Payment Gateway.' },
      ],
      validation: {
        requiredNodes: ['payment_gateway'],
        requiredEdges: [{ from: 'Microservice', to: 'Payment' }],
        successMessage: "Payment Gateway added. Now webhook delivery.",
        errorMessage: 'Add a Payment Gateway and connect it from the Fraud Detection service.',
      },
      openingMessage: "Banks still use ISO 8583 from the 1980s. Connect Fraud Detection → Payment Gateway — only fraud-cleared payments reach the bank network.",
      defaultSuggestions: ["What is ISO 8583?", "How does card authorization work?", "What is the difference between authorization and capture?"],
    },
    {
      id: 7,
      title: 'Add Webhook Service',
      explanation: "Stripe attempts webhook delivery with exponential backoff — immediately, then 1hr, 4hr, 24hr, 72hr. Merchants must make their webhook handlers idempotent because the same event can be delivered twice.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas. Then connect Payment Service → Webhook Service.',
      searchHint: 'Microservice',
      why: "Webhooks are how Stripe notifies merchants of payment events. Reliable delivery with retry logic is critical — a missed 'payment_succeeded' event could mean a merchant never ships an order.",
      messages: [
        { type: 'guide', content: "When a payment succeeds, Stripe needs to notify the merchant's server. That's done via webhooks." },
        { type: 'guide', content: "The Webhook Service retries with exponential backoff: immediately, then 1hr, 4hr, 24hr, 72hr. Because of retries, merchants must handle the same event twice gracefully — idempotent webhook handlers." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add another one for the Webhook Service, then connect: Payment Service → Webhook Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Microservice', to: 'Microservice' }],
        successMessage: "Webhook Service added. Now async event processing.",
        errorMessage: 'Add a third Microservice (Webhook Service) and connect it from the Payment Service.',
      },
      openingMessage: "Stripe retries webhooks up to 72 hours. Connect Payment Service → Webhook Service — the payment service triggers merchant notifications here.",
      defaultSuggestions: ["What is idempotent webhook handling?", "How does Stripe retry webhooks?", "What is exponential backoff?"],
    },
    {
      id: 8,
      title: 'Add Message Queue',
      explanation: "Stripe uses a message queue to decouple payment events from downstream processing — webhook delivery, ledger updates, analytics, and fraud model retraining all consume from the same event stream.",
      action: 'Press ⌘K, search for "Message Queue", and add it to the canvas. Then connect Payment Service → Message Queue.',
      searchHint: 'Message Queue',
      why: "Payment events trigger many downstream actions. A queue ensures that a slow webhook delivery doesn't block ledger updates, and a ledger failure doesn't prevent analytics from recording the transaction.",
      messages: [
        { type: 'guide', content: "When a payment completes, many things need to happen: update the ledger, deliver webhooks, update analytics, retrain fraud models..." },
        { type: 'guide', content: "A Message Queue decouples all of this. The Payment Service publishes one event. Every downstream service consumes it independently. A slow webhook delivery doesn't block the ledger update." },
        { type: 'guide', content: 'Press ⌘K, search for "Message Queue", add it, then connect: Payment Service → Message Queue.' },
      ],
      validation: {
        requiredNodes: ['message_queue'],
        requiredEdges: [{ from: 'Microservice', to: 'Message' }],
        successMessage: "Message Queue added. Now the ledger.",
        errorMessage: 'Add a Message Queue and connect it from the Payment Service.',
      },
      openingMessage: "Stripe decouples payment events from downstream processing. Connect Payment Service → Message Queue to fan out the payment_succeeded event.",
      defaultSuggestions: ["What is event-driven architecture?", "How does Stripe handle queue failures?", "What is at-least-once delivery?"],
    },
    {
      id: 9,
      title: 'Add SQL Database (Ledger)',
      explanation: "Stripe uses double-entry bookkeeping — every debit has a corresponding credit. The ledger is append-only. No record is ever updated or deleted. This makes the entire payment history auditable forever.",
      action: 'Press ⌘K, search for "SQL Database", and add it to the canvas. Then connect Payment Service → SQL Database.',
      searchHint: 'SQL Database',
      why: "Financial data requires ACID transactions and an immutable audit trail. SQL's transaction guarantees ensure that a charge and its corresponding ledger entry are always consistent.",
      messages: [
        { type: 'guide', content: "Every payment creates a ledger entry. Stripe uses double-entry bookkeeping — every debit has a corresponding credit." },
        { type: 'guide', content: "The ledger is append-only and immutable. No record is ever updated or deleted. This makes the entire payment history auditable forever — critical for financial compliance." },
        { type: 'guide', content: 'Press ⌘K, search for "SQL Database", add it, then connect: Payment Service → SQL Database.' },
      ],
      validation: {
        requiredNodes: ['sql_db'],
        requiredEdges: [{ from: 'Microservice', to: 'SQL' }],
        successMessage: "Ledger added. Final step — observability.",
        errorMessage: 'Add a SQL Database (Ledger) and connect it from the Payment Service.',
      },
      openingMessage: "Stripe uses an append-only ledger with double-entry bookkeeping. Connect Payment Service → SQL Database — every payment is recorded as an immutable ledger entry.",
      defaultSuggestions: ["What is double-entry bookkeeping?", "What is an append-only ledger?", "How does Stripe handle refunds in the ledger?"],
    },
    {
      id: 10,
      title: 'Add Observability',
      explanation: "Stripe monitors payment success rates, fraud detection accuracy, webhook delivery rates, and API latency in real time. A 0.1% drop in payment success rate represents millions of dollars in lost revenue.",
      action: 'Press ⌘K, search for "Logger", and add it to the canvas. Then search for "Metrics Collector" and add that too. Connect both from the Payment Service.',
      searchHint: 'Logger',
      why: "Every failed payment is a lost sale for a merchant. Observability lets Stripe detect degradation in payment success rates within seconds and page the on-call engineer before merchants notice.",
      messages: [
        { type: 'guide', content: "Final step — observability. A 0.1% drop in payment success rate at Stripe's scale is millions of dollars in lost merchant revenue." },
        { type: 'guide', content: "The Logger captures detailed payment traces for debugging. The Metrics Collector tracks payment success rates, fraud block rates, and webhook delivery rates in real time." },
        { type: 'guide', content: 'Press ⌘K, search for "Logger", add it. Then search for "Metrics Collector" and add that too. Connect both from the Payment Service.' },
      ],
      validation: {
        requiredNodes: ['logger', 'metrics_collector'],
        requiredEdges: [
          { from: 'Microservice', to: 'Logger' },
          { from: 'Microservice', to: 'Metrics' },
        ],
        successMessage: "🎉 Tutorial complete! You've designed Stripe's core payment architecture from scratch.",
        errorMessage: 'Add both Logger and Metrics Collector and connect them from the Payment Service.',
      },
      openingMessage: "A 0.1% payment failure rate at Stripe's scale is millions in lost revenue. Connect Payment Service → Logger and Payment Service → Metrics Collector.",
      defaultSuggestions: ["What metrics does Stripe track?", "How does Stripe handle incidents?", "What is a payment success rate?"],
    },
  ],
};
