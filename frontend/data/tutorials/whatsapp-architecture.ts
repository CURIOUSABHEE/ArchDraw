import type { TutorialData } from './chatgpt-architecture';

export const whatsappTutorial: TutorialData = {
  id: 'whatsapp-architecture',
  title: 'How to Design WhatsApp Architecture',
  description: 'Design a messaging system for 2 billion users. Understand message delivery guarantees, presence detection, offline queuing, and end-to-end encryption architecture.',
  difficulty: 'Advanced',
  category: 'Messaging',
  estimatedTime: '~28 mins',
  nodeCount: 10,
  stepCount: 10,
  icon: 'MessageCircle',
  color: '#25d366',
  tags: ['WebSockets', 'Encryption', 'Presence', 'Queue', 'Scale'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "WhatsApp's client is the iOS and Android app. It maintains a persistent WebSocket connection to the server for real-time message delivery and handles local encryption/decryption of all messages.",
      action: 'Press ⌘K, search for "Mobile", and add it to the canvas.',
      searchHint: 'Mobile',
      why: "WhatsApp is mobile-first by design. The client handles end-to-end encryption locally — the server never sees plaintext messages. This shapes the entire architecture.",
      messages: [
        { type: 'guide', content: "Welcome to the WhatsApp Architecture tutorial. 2 billion users, 100 billion messages per day, end-to-end encrypted." },
        { type: 'guide', content: "WhatsApp's client is the iOS/Android app. It maintains a persistent WebSocket connection and handles all encryption locally — the server never sees your message content." },
        { type: 'guide', content: 'Press ⌘K, search for "Mobile", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_mobile'],
        requiredEdges: [],
        successMessage: "Client added. Now the entry layer.",
        errorMessage: 'Add a Mobile Client node to the canvas first.',
      },
      openingMessage: "WhatsApp handles E2E encryption on the client. What does that mean for the server architecture?",
      defaultSuggestions: ["What is end-to-end encryption?", "How does WhatsApp store messages?", "What is a WebSocket?"],
    },
    {
      id: 2,
      title: 'Add the API Gateway',
      explanation: "All connections from WhatsApp clients flow through an API Gateway. It handles initial connection routing, authentication, and directs clients to the correct chat server.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas.',
      searchHint: 'API Gateway',
      why: "With 2 billion users, WhatsApp needs a single entry point that can route connections to thousands of chat servers. The API Gateway abstracts this routing complexity.",
      messages: [
        { type: 'guide', content: "2 billion clients need to connect to the backend. They all go through the API Gateway first." },
        { type: 'guide', content: "The gateway authenticates the connection and routes it to the right chat server — the one that will maintain the persistent WebSocket for that user." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [],
        successMessage: "API Gateway added. Now distribute the connections.",
        errorMessage: 'Add an API Gateway node to the canvas.',
      },
      openingMessage: "The API Gateway routes 2 billion connections. How does it decide which chat server to assign each user?",
      defaultSuggestions: ["How does connection routing work?", "What is consistent hashing?", "How does WhatsApp handle reconnections?"],
    },
    {
      id: 3,
      title: 'Add Load Balancer',
      explanation: "WhatsApp uses persistent WebSocket connections. A single WhatsApp server handles 1 million concurrent connections — the highest connection density in the industry, made possible by Erlang's actor model.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.',
      searchHint: 'Load Balancer',
      why: "With 2 billion users, even at 10% concurrency that's 200 million simultaneous connections. The load balancer distributes these across thousands of Erlang-based chat servers.",
      messages: [
        { type: 'guide', content: "WhatsApp's load balancer has an unusual property — it routes persistent WebSocket connections, not stateless HTTP requests." },
        { type: 'guide', content: "Each WhatsApp server handles 1 million concurrent connections using Erlang's lightweight process model. The load balancer distributes users across these servers and must maintain connection affinity." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [],
        successMessage: "Load balancer added. Now the auth layer.",
        errorMessage: 'Add a Load Balancer node to the canvas.',
      },
      openingMessage: "WhatsApp servers handle 1 million connections each using Erlang. Why is Erlang uniquely suited for this?",
      defaultSuggestions: ["What is Erlang's actor model?", "What is connection affinity?", "How does WhatsApp scale chat servers?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: "WhatsApp authenticates users via phone number verification using SMS OTP. The Auth Service validates the OTP, issues a session token, and registers the device's public key for end-to-end encryption.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas.',
      searchHint: 'Auth Service',
      why: "Phone number auth is simpler than username/password but requires SMS delivery infrastructure. The Auth Service also manages the key exchange that enables end-to-end encryption.",
      messages: [
        { type: 'guide', content: "WhatsApp authenticates via phone number — no username or password. You get an SMS OTP, enter it, and you're in." },
        { type: 'guide', content: "The Auth Service validates the OTP, creates a session, and registers your device's public encryption key. This key exchange is what enables end-to-end encryption between devices." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [],
        successMessage: "Auth added. Now the core messaging service.",
        errorMessage: 'Add an Auth Service node to the canvas.',
      },
      openingMessage: "WhatsApp uses phone number + SMS OTP for auth. What are the tradeoffs vs username/password?",
      defaultSuggestions: ["What is OTP authentication?", "How does WhatsApp handle SIM swaps?", "What is the Signal Protocol?"],
    },
    {
      id: 5,
      title: 'Add Chat Service',
      explanation: "The Chat Service routes messages between users. It follows a store-and-forward model: the message is stored on the server until the recipient's device confirms delivery, then deleted. This is why WhatsApp cannot recover deleted messages.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas.',
      searchHint: 'Microservice',
      why: "The store-and-forward model is what enables delivery guarantees. The server holds the message until the recipient acknowledges it, then removes it — keeping storage minimal.",
      messages: [
        { type: 'guide', content: "The Chat Service is the core of WhatsApp — it routes encrypted messages between users." },
        { type: 'guide', content: "It uses store-and-forward: your message is stored on the server until the recipient's device confirms receipt, then deleted. WhatsApp's servers never hold messages long-term — that's why they can't recover them." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Chat Service added. Now offline message storage.",
        errorMessage: 'Add a Microservice (Chat Service) to the canvas.',
      },
      openingMessage: "WhatsApp uses store-and-forward. What happens to a message if the recipient is offline for 30 days?",
      defaultSuggestions: ["What is store-and-forward?", "How does WhatsApp handle delivery receipts?", "What are the double blue ticks?"],
    },
    {
      id: 6,
      title: 'Add Message Queue',
      explanation: "WhatsApp uses a custom queue for offline users. Messages wait up to 30 days. After 30 days, the sender gets a delivery failure notification. The queue also handles group message fan-out.",
      action: 'Press ⌘K, search for "Message Queue", and add it to the canvas.',
      searchHint: 'Message Queue',
      why: "Without a queue, messages to offline users would be lost. The queue buffers them until the recipient comes online, enabling the delivery guarantee that makes WhatsApp reliable.",
      messages: [
        { type: 'guide', content: "What happens when you send a message to someone who's offline? It goes into a queue." },
        { type: 'guide', content: "WhatsApp's queue holds messages for up to 30 days. When the recipient comes online, messages are delivered in order. After 30 days, the sender gets a failure notification." },
        { type: 'guide', content: 'Press ⌘K, search for "Message Queue", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['message_queue'],
        requiredEdges: [],
        successMessage: "Message Queue added. Now persistent storage.",
        errorMessage: 'Add a Message Queue node to the canvas.',
      },
      openingMessage: "WhatsApp queues messages for 30 days. How does group messaging fan-out work at scale?",
      defaultSuggestions: ["How does group messaging work?", "What is message fan-out?", "How does WhatsApp handle large groups?"],
    },
    {
      id: 7,
      title: 'Add NoSQL Database',
      explanation: "WhatsApp stores message metadata, user profiles, and group information in a NoSQL database. Message content itself is deleted after delivery — only metadata (timestamps, delivery status) is retained.",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.',
      searchHint: 'NoSQL Database',
      why: "Message metadata has a flexible schema and requires high write throughput. NoSQL handles billions of status updates (sent, delivered, read) per day without the overhead of SQL joins.",
      messages: [
        { type: 'guide', content: "WhatsApp stores message metadata — not content — in a NoSQL database. Content is deleted after delivery." },
        { type: 'guide', content: "The database stores delivery timestamps, read receipts, group membership, and user profiles. At 100 billion messages per day, that's 100 billion status updates — NoSQL handles this throughput where SQL would struggle." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [],
        successMessage: "NoSQL Database added. Now presence detection.",
        errorMessage: 'Add a NoSQL Database node to the canvas.',
      },
      openingMessage: "WhatsApp only stores metadata, not message content. How does that affect the database design?",
      defaultSuggestions: ["What metadata does WhatsApp store?", "How does WhatsApp handle GDPR?", "What database does WhatsApp use?"],
    },
    {
      id: 8,
      title: 'Add Presence Service',
      explanation: "'Last seen' data is one of WhatsApp's most queried pieces of data — billions of presence checks happen per hour. The Presence Service uses a distributed hash table to serve online/offline status at scale.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas.',
      searchHint: 'Microservice',
      why: "Presence is queried every time you open a chat. At 2 billion users, even 1% opening a chat simultaneously is 20 million presence queries per second. It needs its own dedicated service.",
      messages: [
        { type: 'guide', content: "Every time you open a WhatsApp chat, you see 'last seen 2 minutes ago'. That's the Presence Service." },
        { type: 'guide', content: "It's one of the most queried services in WhatsApp — billions of presence checks per hour. WhatsApp uses a distributed hash table to serve this at scale, partitioned by user ID." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add another one for the Presence Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Presence Service added. Now media handling.",
        errorMessage: 'Add a second Microservice (Presence Service) to the canvas.',
      },
      openingMessage: "Billions of 'last seen' queries per hour — how does a distributed hash table make that fast?",
      defaultSuggestions: ["What is a distributed hash table?", "How does WhatsApp handle 'last seen' privacy?", "What is presence detection?"],
    },
    {
      id: 9,
      title: 'Add Media Service',
      explanation: "Images, videos, and documents are handled by a dedicated Media Service. Media is compressed and encrypted on the client before upload, stored in object storage, and served via CDN.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas.',
      searchHint: 'Microservice',
      why: "Media files are large and expensive to transfer. Separating media handling into its own service allows independent scaling — media uploads are bursty and require different infrastructure than text messages.",
      messages: [
        { type: 'guide', content: "Text messages are tiny. But photos and videos are large — they need their own service." },
        { type: 'guide', content: "The Media Service handles upload, compression, and storage. Media is encrypted on the client before upload, so the server stores encrypted blobs it can never read." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add another one for the Media Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Media Service added. Now the storage layer.",
        errorMessage: 'Add a third Microservice (Media Service) to the canvas.',
      },
      openingMessage: "WhatsApp encrypts media on the client before upload. What does that mean for the server?",
      defaultSuggestions: ["How does WhatsApp compress images?", "What is client-side encryption?", "How does WhatsApp handle video?"],
    },
    {
      id: 10,
      title: 'Add Object Storage',
      explanation: "All encrypted media files are stored in object storage. WhatsApp stores media for 30 days — after that it's deleted from servers. Users must re-download from the sender if they need it again.",
      action: 'Press ⌘K, search for "Object Storage", and add it to the canvas.',
      searchHint: 'Object Storage',
      why: "Object storage is infinitely scalable and cheap per GB — perfect for encrypted binary blobs. The 30-day retention policy keeps storage costs manageable at 2 billion user scale.",
      messages: [
        { type: 'guide', content: "Encrypted media files need a home. That's object storage — think S3." },
        { type: 'guide', content: "WhatsApp stores media for 30 days. After that it's deleted from servers. The 30-day limit keeps storage costs manageable — at 2 billion users, even 1 photo per user per day is enormous." },
        { type: 'guide', content: 'Press ⌘K, search for "Object Storage", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['object_storage'],
        requiredEdges: [],
        successMessage: "🎉 Tutorial complete! You've designed WhatsApp's core architecture from scratch.",
        errorMessage: 'Add an Object Storage node to the canvas.',
      },
      openingMessage: "WhatsApp deletes media after 30 days. How does that policy affect the architecture?",
      defaultSuggestions: ["Why does WhatsApp delete media?", "How does WhatsApp handle backups?", "What is object storage?"],
    },
  ],
};
