import type { TutorialData } from './index';

export const githubTutorial: TutorialData = {
  id: 'github-architecture',
  title: 'How to Design GitHub Architecture',
  description: 'Build a code hosting platform for 100 million developers with 420 million repositories. Learn Git object storage, pull request workflows, CI/CD pipelines, code search, and webhook delivery at scale.',
  difficulty: 'Advanced',
  category: 'Developer Tools',
  estimatedTime: '~30 mins',
  nodeCount: 12,
  stepCount: 12,
  icon: 'Github',
  color: '#333333',
  tags: ['Git Storage', 'CI/CD', 'Code Search', 'Webhooks'],
  steps: [
    {
      id: 1,
      title: 'Start with the Web Client',
      explanation: "GitHub's web client is where developers browse code, review pull requests, and manage issues. It renders syntax-highlighted code, diffs, and markdown — all server-side rendered for fast initial loads.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "GitHub's web client must render code diffs with syntax highlighting for 100+ programming languages. Server-side rendering ensures fast initial page loads even for large files.",
      messages: [
        { type: 'guide', content: "Welcome to the GitHub Architecture tutorial. 100 million developers, 420 million repositories, and a push that triggers CI/CD pipelines, notifies collaborators, and updates integrations — all within seconds." },
        { type: 'guide', content: "GitHub's scale of Git storage alone is staggering. Every commit, every file, every version of every file across 420 million repositories. Git's content-addressable storage is what makes this possible." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Web Client added. Now the API layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "GitHub renders syntax-highlighted diffs for 100+ languages. How does server-side rendering help with large code files?",
      defaultSuggestions: ["How does GitHub render code?", "What is syntax highlighting at scale?", "How does GitHub handle large files?"],
    },
    {
      id: 2,
      title: 'Add API Gateway',
      explanation: "GitHub's API Gateway handles REST API v3, GraphQL API v4, and Git protocol requests. The GraphQL API lets clients fetch exactly the data they need — a PR review page can fetch PR details, comments, and CI status in a single query.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas. Then connect Client → API Gateway.',
      searchHint: 'API Gateway',
      why: "GitHub's GraphQL API was a major architectural improvement over REST. A single GraphQL query can replace 10 REST calls — critical for GitHub's complex data relationships (PRs, reviews, checks, comments).",
      messages: [
        { type: 'guide', content: "GitHub offers both REST and GraphQL APIs. The GraphQL API replaced dozens of REST calls with a single query." },
        { type: 'guide', content: "Loading a pull request page used to require 10+ REST API calls: get PR details, get commits, get reviews, get comments, get CI status, get labels... GraphQL lets the client specify exactly what it needs in one request. GitHub's own web app uses the GraphQL API internally." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", add it, then connect: Client → API Gateway.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [{ from: 'Web', to: 'API Gateway' }],
        successMessage: "API Gateway added and connected. Now distribute the load.",
        errorMessage: 'Add an API Gateway and connect it from the Client.',
      },
      openingMessage: "GitHub's GraphQL API replaced 10 REST calls with one query. Connect Client → API Gateway — all GitHub requests start here.",
      defaultSuggestions: ["What is GraphQL?", "How does GitHub's GraphQL API work?", "What is over-fetching in REST?"],
    },
    {
      id: 3,
      title: 'Add Load Balancer',
      explanation: "GitHub's Load Balancer distributes traffic across service instances. GitHub uses HAProxy for its load balancer — one of the highest-traffic HAProxy deployments in the world.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas. Then connect API Gateway → Load Balancer.',
      searchHint: 'Load Balancer',
      why: "GitHub's traffic is highly variable — a viral repository can get millions of clone requests in hours. The load balancer must handle sudden spikes without dropping Git operations.",
      messages: [
        { type: 'guide', content: "GitHub uses HAProxy as its load balancer — handling millions of Git operations and API requests per day." },
        { type: 'guide', content: "When a popular open-source project releases a new version, thousands of CI systems clone the repository simultaneously. The load balancer distributes these clone requests across Git server instances. Git operations are stateless, so any server can handle any clone request." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", add it, then connect: API Gateway → Load Balancer.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [{ from: 'API Gateway', to: 'Load Balancer' }],
        successMessage: "Load Balancer added and connected. Now authentication.",
        errorMessage: 'Add a Load Balancer and connect it from the API Gateway.',
      },
      openingMessage: "A viral repository release causes thousands of simultaneous clones. Connect API Gateway → Load Balancer to handle the spike.",
      defaultSuggestions: ["What is HAProxy?", "How does GitHub handle clone spikes?", "What is Git protocol load balancing?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: "GitHub's Auth Service handles personal access tokens, OAuth apps, GitHub Apps, and SSH key authentication. GitHub Apps are the modern way to integrate with GitHub — they have fine-grained permissions and can act as themselves, not as a user.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas. Then connect Load Balancer → Auth Service.',
      searchHint: 'Auth Service',
      why: "GitHub Apps replaced OAuth Apps because they have repository-level permissions. An OAuth App gets access to all your repos; a GitHub App can be granted access to only specific repos.",
      messages: [
        { type: 'guide', content: "GitHub's auth handles multiple credential types: passwords, tokens, SSH keys, and GitHub App JWTs." },
        { type: 'guide', content: "GitHub Apps are the modern integration model. Instead of 'this app has access to all your repos', GitHub Apps request specific permissions on specific repos. A CI app only needs read access to your code repo — not your private repos. The Auth Service validates GitHub App JWTs and enforces these fine-grained permissions." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", add it, then connect: Load Balancer → Auth Service.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Auth' }],
        successMessage: "Auth Service added and connected. Now the core — Git storage.",
        errorMessage: 'Add an Auth Service and connect it from the Load Balancer.',
      },
      openingMessage: "GitHub Apps have repository-level permissions. Connect Load Balancer → Auth Service — every Git operation and API call is authenticated here.",
      defaultSuggestions: ["What is a GitHub App?", "How does fine-grained permission work?", "What is the difference between OAuth App and GitHub App?"],
    },
    {
      id: 5,
      title: 'Add Repository Service',
      explanation: "GitHub's Repository Service handles repository creation, metadata, and access control. It's the service that knows which users have access to which repositories and manages repository settings.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas. Then connect Auth Service → Repository Service (Microservice).',
      searchHint: 'Microservice',
      why: "Repository metadata (name, description, visibility, collaborators) is separate from Git object storage. The Repository Service manages metadata while Git Object Storage manages the actual code.",
      messages: [
        { type: 'guide', content: "The Repository Service manages metadata — who owns the repo, who has access, what the settings are." },
        { type: 'guide', content: "When you create a repository, the Repository Service creates the metadata record and provisions storage. When you add a collaborator, it updates the access control list. When you make a repo private, it updates visibility. The actual Git objects (commits, trees, blobs) are stored separately in Git Object Storage." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", add it, then connect: Auth Service → Repository Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [{ from: 'Auth', to: 'Microservice' }],
        successMessage: "Repository Service added and connected. Now Git object storage.",
        errorMessage: 'Add a Microservice (Repository Service) and connect it from the Auth Service.',
      },
      openingMessage: "GitHub separates repository metadata from Git objects. Connect Auth Service → Repository Service — validated repo access flows here.",
      defaultSuggestions: ["What is repository metadata?", "How does GitHub store repository settings?", "What is access control list?"],
    },
    {
      id: 6,
      title: 'Add Git Object Storage',
      explanation: "Git Object Storage stores all Git objects: blobs (file contents), trees (directory structures), commits, and tags. Git uses content-addressable storage — every object is identified by its SHA-1 hash. Identical files across repositories share the same blob.",
      action: 'Press ⌘K, search for "Git Object Storage", and add it to the canvas. Then connect Repository Service → Git Object Storage.',
      searchHint: 'Git Object Storage',
      why: "Content-addressable storage means deduplication is automatic. If 1 million repositories all contain the same popular library file, GitHub stores it once. The SHA-1 hash is the same regardless of which repo it's in.",
      messages: [
        { type: 'guide', content: "Git's content-addressable storage is GitHub's secret weapon for storage efficiency." },
        { type: 'guide', content: "Every file in Git is stored as a blob identified by its SHA-1 hash. If the same file exists in 1 million repositories (like a popular library), GitHub stores it once. When you push a commit, GitHub checks if each blob already exists before storing it. This deduplication saves petabytes of storage." },
        { type: 'guide', content: 'Press ⌘K, search for "Git Object Storage", add it, then connect: Repository Service → Git Object Storage.' },
      ],
      validation: {
        requiredNodes: ['git_storage'],
        requiredEdges: [{ from: 'Microservice', to: 'Git' }],
        successMessage: "Git Object Storage added and connected. Now code review.",
        errorMessage: 'Add a Git Object Storage and connect it from the Repository Service.',
      },
      openingMessage: "Git's content-addressable storage deduplicates identical files across repos. Connect Repository Service → Git Object Storage — repository code is stored here.",
      defaultSuggestions: ["What is content-addressable storage?", "How does Git deduplication work?", "What is SHA-1 in Git?"],
    },
    {
      id: 7,
      title: 'Add Code Review Service',
      explanation: "The Code Review Service manages pull requests, reviews, and comments. It stores PR metadata, review states, and inline comments. It also computes diffs between branches for display.",
      action: 'Press ⌘K, search for "Code Review Service", and add it to the canvas. Then connect Repository Service → Code Review Service.',
      searchHint: 'Code Review Service',
      why: "Pull requests are GitHub's core product. The Code Review Service must compute diffs between any two commits in real-time, store review states, and notify reviewers — all while handling millions of active PRs.",
      messages: [
        { type: 'guide', content: "Pull requests are GitHub's most important feature. The Code Review Service manages the entire PR lifecycle." },
        { type: 'guide', content: "When you open a PR, the Code Review Service computes the diff between your branch and the base branch. It stores inline comments with their position in the diff. When the base branch changes, it re-computes which comments are still relevant. This diff computation is expensive — GitHub caches diffs aggressively." },
        { type: 'guide', content: 'Press ⌘K, search for "Code Review Service", add it, then connect: Repository Service → Code Review Service.' },
      ],
      validation: {
        requiredNodes: ['code_review_service'],
        requiredEdges: [{ from: 'Microservice', to: 'Code' }],
        successMessage: "Code Review Service added and connected. Now CI/CD.",
        errorMessage: 'Add a Code Review Service and connect it from the Repository Service.',
      },
      openingMessage: "GitHub re-computes diffs when the base branch changes. Connect Repository Service → Code Review Service — PR creation triggers diff computation here.",
      defaultSuggestions: ["How does GitHub compute diffs?", "What is a pull request?", "How does GitHub handle inline comments?"],
    },
    {
      id: 8,
      title: 'Add CI Runner',
      explanation: "GitHub Actions CI Runners execute workflow jobs. Runners are ephemeral VMs that spin up, run the job, and terminate. GitHub provides hosted runners (Ubuntu, Windows, macOS) and supports self-hosted runners.",
      action: 'Press ⌘K, search for "CI Runner", and add it to the canvas. Then connect Code Review Service → CI Runner.',
      searchHint: 'CI Runner',
      why: "CI runners must be ephemeral — each job gets a clean environment. Shared state between jobs would cause flaky tests. Ephemeral VMs guarantee isolation but require fast provisioning (under 30 seconds).",
      messages: [
        { type: 'guide', content: "GitHub Actions runners are ephemeral VMs — each job gets a fresh environment, then the VM is destroyed." },
        { type: 'guide', content: "When a push triggers a workflow, GitHub provisions a new VM, clones the repository, runs the job steps, and destroys the VM. This takes under 30 seconds for hosted runners. The ephemeral model prevents test pollution between jobs. GitHub runs millions of CI jobs per day across their runner fleet." },
        { type: 'guide', content: 'Press ⌘K, search for "CI Runner", add it, then connect: Code Review Service → CI Runner.' },
      ],
      validation: {
        requiredNodes: ['ci_runner'],
        requiredEdges: [{ from: 'Code', to: 'CI' }],
        successMessage: "CI Runner added and connected. Now webhook delivery.",
        errorMessage: 'Add a CI Runner and connect it from the Code Review Service.',
      },
      openingMessage: "GitHub Actions runners are ephemeral VMs. Connect Code Review Service → CI Runner — PR opens and pushes trigger CI workflows.",
      defaultSuggestions: ["What is GitHub Actions?", "What is an ephemeral runner?", "How does GitHub provision CI VMs?"],
    },
    {
      id: 9,
      title: 'Add Webhook Dispatcher',
      explanation: "The Webhook Dispatcher delivers events to external services when things happen on GitHub: push, PR opened, issue created, CI completed. It handles retries, delivery guarantees, and rate limiting per endpoint.",
      action: 'Press ⌘K, search for "Webhook Dispatcher", and add it to the canvas. Then connect Repository Service → Webhook Dispatcher.',
      searchHint: 'Webhook Dispatcher',
      why: "GitHub delivers billions of webhook events per day to millions of endpoints. The Webhook Dispatcher must handle slow endpoints (with timeouts), failed deliveries (with retries), and endpoints that go down (with exponential backoff).",
      messages: [
        { type: 'guide', content: "GitHub delivers billions of webhook events per day. The Webhook Dispatcher handles retries and slow endpoints." },
        { type: 'guide', content: "When a push happens, GitHub sends a webhook to every registered endpoint. If an endpoint is slow (>10 seconds), the request times out. If it fails, GitHub retries with exponential backoff: 5s, 30s, 5m, 30m, 2h. After 3 days of failures, the webhook is disabled. This protects GitHub from slow endpoints blocking the delivery queue." },
        { type: 'guide', content: 'Press ⌘K, search for "Webhook Dispatcher", add it, then connect: Repository Service → Webhook Dispatcher.' },
      ],
      validation: {
        requiredNodes: ['webhook_dispatcher'],
        requiredEdges: [{ from: 'Microservice', to: 'Webhook' }],
        successMessage: "Webhook Dispatcher added and connected. Now code search.",
        errorMessage: 'Add a Webhook Dispatcher and connect it from the Repository Service.',
      },
      openingMessage: "GitHub retries failed webhooks with exponential backoff. Connect Repository Service → Webhook Dispatcher — repository events are fanned out here.",
      defaultSuggestions: ["What is exponential backoff?", "How does GitHub handle webhook failures?", "What is webhook delivery guarantee?"],
    },
    {
      id: 10,
      title: 'Add Search Service',
      explanation: "GitHub's Search Service indexes all public code — 200 billion lines of code across 420 million repositories. It powers code search, repository search, and the new AI-powered code search that understands natural language queries.",
      action: 'Press ⌘K, search for "Search Engine", and add it to the canvas. Then connect Repository Service → Search Service.',
      searchHint: 'Search Engine',
      why: "Indexing 200 billion lines of code requires a custom search engine. GitHub built their own code search infrastructure (Blackbird) because general-purpose search engines like Elasticsearch couldn't handle code-specific queries efficiently.",
      messages: [
        { type: 'guide', content: "GitHub indexes 200 billion lines of code. They built a custom search engine called Blackbird for this." },
        { type: 'guide', content: "Code search has unique requirements: exact symbol matching, regex support, language-aware tokenization (a dot in Python means something different than in English). GitHub's Blackbird engine handles these requirements. The new AI-powered search also understands natural language: 'how does authentication work in this repo'." },
        { type: 'guide', content: 'Press ⌘K, search for "Search Engine", add it, then connect: Repository Service → Search Service.' },
      ],
      validation: {
        requiredNodes: ['search_engine'],
        requiredEdges: [{ from: 'Microservice', to: 'Search' }],
        successMessage: "Search Service added and connected. Final step — the NoSQL database.",
        errorMessage: 'Add a Search Service and connect it from the Repository Service.',
      },
      openingMessage: "GitHub built a custom search engine for code. Connect Repository Service → Search Service — new commits are indexed here for search.",
      defaultSuggestions: ["What is GitHub Blackbird?", "How does code search differ from text search?", "What is language-aware tokenization?"],
    },
    {
      id: 11,
      title: 'Add NoSQL Database',
      explanation: "GitHub stores issue metadata, PR data, user activity, and notification state in NoSQL databases. Issues and PRs have flexible schemas — different issue types have different fields, and custom fields vary per repository.",
      action: 'Press ⌘K, search for "NoSQL Database", and add it to the canvas. Then connect Code Review Service → NoSQL Database.',
      searchHint: 'NoSQL Database',
      why: "GitHub Projects (the project management feature) lets teams add custom fields to issues. NoSQL's flexible schema handles arbitrary custom fields without schema migrations.",
      messages: [
        { type: 'guide', content: "GitHub stores issues and PRs in NoSQL — custom fields and flexible schemas make relational storage impractical." },
        { type: 'guide', content: "GitHub Projects lets teams add custom fields: priority, sprint, story points, custom labels. Each team has different fields. In a relational DB, you'd need a separate table per custom field type. NoSQL stores each issue as a document with whatever fields the team has configured." },
        { type: 'guide', content: 'Press ⌘K, search for "NoSQL Database", add it, then connect: Code Review Service → NoSQL Database.' },
      ],
      validation: {
        requiredNodes: ['nosql_db'],
        requiredEdges: [{ from: 'Code', to: 'NoSQL' }],
        successMessage: "NoSQL Database added and connected. Final step — object storage.",
        errorMessage: 'Add a NoSQL Database and connect it from the Code Review Service.',
      },
      openingMessage: "GitHub Projects supports custom fields per team. Connect Code Review Service → NoSQL Database — PR and issue metadata is persisted here.",
      defaultSuggestions: ["How does GitHub store issues?", "What are GitHub Projects custom fields?", "How does NoSQL handle custom schemas?"],
    },
    {
      id: 12,
      title: 'Add Object Storage',
      explanation: "GitHub stores release artifacts, LFS (Large File Storage) objects, and CI artifacts in Object Storage. Git LFS lets developers store large binary files (videos, datasets, models) outside the Git repository.",
      action: 'Press ⌘K, search for "Object Storage", and add it to the canvas. Then connect CI Runner → Object Storage.',
      searchHint: 'Object Storage',
      why: "Git is terrible at large binary files — they bloat the repository history. Git LFS stores large files in Object Storage and replaces them with pointer files in the Git repository. The actual binary is downloaded on demand.",
      messages: [
        { type: 'guide', content: "Git LFS stores large binary files in Object Storage instead of the Git repository." },
        { type: 'guide', content: "Without LFS, a 1GB video file would be stored in every clone of the repository forever. With LFS, the Git repo contains a 200-byte pointer file. The actual video is in Object Storage and downloaded only when needed. GitHub Actions CI artifacts (build outputs, test reports) are also stored here and expire after 90 days." },
        { type: 'guide', content: 'Press ⌘K, search for "Object Storage", add it, then connect: CI Runner → Object Storage.' },
      ],
      validation: {
        requiredNodes: ['object_storage'],
        requiredEdges: [{ from: 'CI', to: 'Object' }],
        successMessage: "🎉 Tutorial complete! You've designed GitHub's core architecture from scratch.",
        errorMessage: 'Add an Object Storage and connect it from the CI Runner.',
      },
      openingMessage: "Git LFS replaces large files with pointer files. Connect CI Runner → Object Storage — CI artifacts and LFS objects are stored here.",
      defaultSuggestions: ["What is Git LFS?", "How does GitHub store CI artifacts?", "What is a Git pointer file?"],
    },
  ],
};
