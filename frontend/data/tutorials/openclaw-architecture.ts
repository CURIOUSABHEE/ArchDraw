import type { TutorialData } from './chatgpt-architecture';

export const openclawTutorial: TutorialData = {
  id: 'openclaw-architecture',
  title: 'How to Design OpenClaw Architecture',
  description: 'Build an AI-powered subscription analytics platform. Learn how real-time event streaming, data warehouses, and LLMs work together to generate business intelligence.',
  difficulty: 'Intermediate',
  category: 'Analytics & AI',
  estimatedTime: '~25 mins',
  nodeCount: 12,
  stepCount: 9,
  icon: 'BarChart2',
  color: '#6366f1',
  tags: ['Analytics', 'Kafka', 'LLM', 'Data Warehouse', 'AI Insights'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client Dashboard',
      explanation: 'The client is the web dashboard that analysts use to view subscription metrics, churn rates, and AI-generated insights. Every analytics platform starts from the consumer of the data.',
      action: 'Add a "Client (Web / Mobile)" node to the canvas.',
      why: 'The dashboard defines what the system needs to produce — charts, reports, and AI insights. Starting here keeps the architecture user-focused.',
      searchHint: 'Client',
      messages: [
        { type: 'guide', content: "Welcome to the OpenClaw Architecture tutorial! 📊 We're building an AI-powered subscription analytics platform." },
        { type: 'guide', content: "OpenClaw ingests subscription events in real time, stores them in a data warehouse, and uses an LLM to generate business insights automatically." },
        { type: 'guide', content: 'Start by adding the Client (Web / Mobile) node — this is the analyst dashboard where insights are consumed.' },
      ],
      validation: {
        requiredNodes: ['client'],
        requiredEdges: [],
        successMessage: "Client dashboard added. Now let's secure access to it.",
        errorMessage: 'Add a Client (Web / Mobile) node to the canvas first.',
      },
    },
    {
      id: 2,
      title: 'Add API Gateway + Auth Service',
      explanation: 'All requests from the dashboard go through an API Gateway. The Auth Service validates JWT tokens and ensures only authorized analysts can access sensitive subscription data.',
      action: 'Add an "API Gateway" node and an "Auth Service" node. Connect: Client → API Gateway → Auth Service.',
      why: 'Subscription data is sensitive business information. Auth ensures only the right people see revenue metrics and customer data.',
      searchHint: 'API Gateway',
      messages: [
        { type: 'guide', content: 'Before any data reaches the dashboard, requests must be authenticated.' },
        { type: 'guide', content: 'Add an API Gateway and an Auth Service. Connect Client → API Gateway, then API Gateway → Auth Service.' },
        { type: 'guide', content: 'The API Gateway is the single entry point — it routes requests and enforces rate limits before they hit your services.' },
      ],
      validation: {
        requiredNodes: ['client', 'api_gateway', 'auth_service'],
        requiredEdges: [
          { from: 'Client', to: 'API Gateway' },
          { from: 'API Gateway', to: 'Auth' },
        ],
        successMessage: 'Access layer secured. Now add the data ingestion service.',
        errorMessage: 'Add API Gateway and Auth Service, then connect Client → API Gateway → Auth Service.',
      },
    },
    {
      id: 3,
      title: 'Add Data Ingestion Service',
      explanation: 'The Data Ingestion Service receives subscription events from external sources — new subscriptions, cancellations, upgrades, and payment failures. It validates and normalizes events before passing them downstream.',
      action: 'Add a "Microservice" node (Data Ingestion Service) and connect it from the API Gateway.',
      why: 'Centralizing ingestion in one service means you can add new event sources without changing downstream systems. It also handles deduplication and schema validation.',
      searchHint: 'Microservice',
      messages: [
        { type: 'guide', content: 'Subscription events come from many sources — payment processors, billing systems, CRMs.' },
        { type: 'guide', content: 'Add a Microservice node to represent the Data Ingestion Service. Connect: API Gateway → Data Ingestion Service.' },
        { type: 'guide', content: 'This service normalizes all incoming events into a consistent schema before they enter the pipeline.' },
      ],
      validation: {
        requiredNodes: ['api_gateway', 'microservice'],
        requiredEdges: [{ from: 'API Gateway', to: 'Microservice' }],
        successMessage: 'Data ingestion layer added. Now handle high-volume event streaming.',
        errorMessage: 'Add a Microservice (Data Ingestion) connected from the API Gateway.',
      },
    },
    {
      id: 4,
      title: 'Add Kafka for Event Streaming',
      explanation: 'Subscription platforms generate thousands of events per second during billing cycles. Kafka decouples the ingestion service from all downstream consumers — the data warehouse, analytics engine, and notification systems can all consume events independently.',
      action: 'Add a "Kafka / Streaming" node and connect it from the Microservice (Data Ingestion).',
      why: 'Without Kafka, the ingestion service would need to write to every downstream system synchronously. If any one fails, events are lost. Kafka provides durability and fan-out.',
      searchHint: 'Kafka',
      messages: [
        { type: 'guide', content: 'During a billing cycle, thousands of subscription events fire simultaneously. A single service cannot handle this synchronously.' },
        { type: 'guide', content: 'Add a Kafka / Streaming node. Connect: Data Ingestion Service → Kafka.' },
        { type: 'guide', content: 'Kafka acts as a durable buffer — events are stored and replayed if any consumer goes down. Multiple services can consume the same event stream independently.' },
      ],
      validation: {
        requiredNodes: ['microservice', 'kafka_streaming'],
        requiredEdges: [{ from: 'Microservice', to: 'Kafka' }],
        successMessage: 'Event streaming layer added. Now store historical data.',
        errorMessage: 'Add a Kafka / Streaming node connected from the Microservice.',
      },
    },
    {
      id: 5,
      title: 'Add Data Warehouse',
      explanation: 'The Data Warehouse stores all historical subscription events for long-term analytics. Unlike operational databases, it is optimized for complex aggregation queries — monthly recurring revenue, churn cohorts, and retention curves.',
      action: 'Add a "Data Warehouse" node and connect it from Kafka.',
      why: 'Operational databases are optimized for fast reads/writes on individual records. A data warehouse is optimized for scanning billions of rows to compute aggregates — exactly what analytics needs.',
      searchHint: 'Data Warehouse',
      messages: [
        { type: 'guide', content: 'Where does all this event data actually live long-term?' },
        { type: 'guide', content: 'Add a Data Warehouse node. Connect: Kafka → Data Warehouse.' },
        { type: 'guide', content: 'The data warehouse is the source of truth for all historical analytics. Think BigQuery, Snowflake, or Redshift — columnar storage optimized for analytical queries.' },
      ],
      validation: {
        requiredNodes: ['kafka_streaming', 'data_warehouse'],
        requiredEdges: [{ from: 'Kafka', to: 'Data Warehouse' }],
        successMessage: 'Historical data storage added. Now add the operational database.',
        errorMessage: 'Add a Data Warehouse connected from Kafka.',
      },
    },
    {
      id: 6,
      title: 'Add SQL Database for Subscription Records',
      explanation: 'The SQL Database stores the current state of each subscription — plan type, billing date, customer details, and payment status. This is the operational database, separate from the analytical data warehouse.',
      action: 'Add a "SQL Database" node and connect it from the Microservice (Data Ingestion).',
      why: 'You need two types of storage: operational (current state, fast lookups) and analytical (historical, aggregations). Mixing them in one database creates performance problems at scale.',
      searchHint: 'SQL Database',
      messages: [
        { type: 'guide', content: "The data warehouse handles analytics, but you also need to store each customer's current subscription state." },
        { type: 'guide', content: 'Add a SQL Database. Connect: Data Ingestion Service → SQL Database.' },
        { type: 'guide', content: 'This stores the live subscription records — who is subscribed, to what plan, and when they renew. Fast point lookups, not aggregations.' },
      ],
      validation: {
        requiredNodes: ['microservice', 'sql_db'],
        requiredEdges: [{ from: 'Microservice', to: 'SQL' }],
        successMessage: 'Operational database added. Now build the analytics engine.',
        errorMessage: 'Add a SQL Database connected from the Microservice.',
      },
    },
    {
      id: 7,
      title: 'Add Analytics Engine',
      explanation: 'The Analytics Engine is a microservice that queries the data warehouse to compute metrics — MRR, churn rate, LTV, and cohort retention. It runs scheduled jobs and caches results for the dashboard.',
      action: 'Add another "Microservice" node (Analytics Engine) and connect it from the Data Warehouse.',
      why: 'Separating analytics computation from ingestion means you can scale them independently. Heavy analytical queries do not compete with real-time event ingestion.',
      searchHint: 'Microservice',
      messages: [
        { type: 'guide', content: 'Raw events in the data warehouse need to be transformed into meaningful metrics.' },
        { type: 'guide', content: 'Add another Microservice node for the Analytics Engine. Connect: Data Warehouse → Analytics Engine.' },
        { type: 'guide', content: 'This service runs scheduled aggregation jobs — computing daily MRR, weekly churn, and monthly cohort retention curves.' },
      ],
      validation: {
        requiredNodes: ['data_warehouse', 'microservice'],
        requiredEdges: [{ from: 'Data Warehouse', to: 'Microservice' }],
        successMessage: 'Analytics engine added. Now add AI-powered insights.',
        errorMessage: 'Add a Microservice (Analytics Engine) connected from the Data Warehouse.',
      },
    },
    {
      id: 8,
      title: 'Add LLM API + RAG Pipeline',
      explanation: 'The LLM API generates natural language insights from the computed metrics — "Churn increased 12% this week, primarily in the Pro tier after the price change." The RAG Pipeline retrieves relevant historical context to ground the LLM responses.',
      action: 'Add an "LLM API" node and a "RAG Pipeline" node. Connect: Analytics Engine → RAG Pipeline → LLM API.',
      why: 'Raw metrics require interpretation. An LLM can surface non-obvious patterns and generate executive summaries automatically, saving analysts hours of manual reporting.',
      searchHint: 'LLM API',
      messages: [
        { type: 'guide', content: "Here's where OpenClaw gets interesting — AI-generated insights." },
        { type: 'guide', content: 'Add an LLM API node and a RAG Pipeline node. Connect: Analytics Engine → RAG Pipeline → LLM API.' },
        { type: 'guide', content: 'The RAG Pipeline retrieves relevant historical metrics and business context, then passes them to the LLM to generate actionable insights in plain English.' },
      ],
      validation: {
        requiredNodes: ['llm_api', 'rag_pipeline'],
        requiredEdges: [
          { from: 'Microservice', to: 'RAG' },
          { from: 'RAG', to: 'LLM' },
        ],
        successMessage: 'AI insights layer added. Now wire up the dashboard.',
        errorMessage: 'Add LLM API and RAG Pipeline with the correct connections.',
      },
    },
    {
      id: 9,
      title: 'Add Dashboard Service + Metrics Collector',
      explanation: 'The Dashboard Service aggregates data from the Analytics Engine and LLM API to serve the frontend. The Metrics Collector monitors system health — ingestion lag, query latency, and LLM response times.',
      action: 'Add a "Dashboard" node and a "Metrics Collector" node. Connect Dashboard from the Analytics Engine, and Metrics Collector from the Microservice.',
      why: 'The dashboard service decouples the frontend from backend complexity. The metrics collector ensures you know when the pipeline is slow or broken before your customers do.',
      searchHint: 'Dashboard',
      messages: [
        { type: 'guide', content: 'Final step — wire up the dashboard and add observability.' },
        { type: 'guide', content: 'Add a Dashboard node and a Metrics Collector node. Connect: Analytics Engine → Dashboard, and Microservice → Metrics Collector.' },
        { type: 'guide', content: "Congratulations — you've designed the OpenClaw analytics platform! 🎉 From raw subscription events to AI-generated business insights." },
      ],
      validation: {
        requiredNodes: ['dashboard', 'metrics_collector'],
        requiredEdges: [
          { from: 'Microservice', to: 'Metrics' },
        ],
        successMessage: "🎉 Tutorial complete! You've designed the OpenClaw AI analytics architecture.",
        errorMessage: 'Add Dashboard and Metrics Collector with the correct connections.',
      },
    },
  ],
};
