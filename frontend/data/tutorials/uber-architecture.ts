import type { TutorialData } from './chatgpt-architecture';

export const uberTutorial: TutorialData = {
  id: 'uber-architecture',
  title: 'How to Design Uber Architecture',
  description: 'Build a real-time ride matching system. Learn location tracking, surge pricing, driver-rider coordination, and payment processing at global scale.',
  difficulty: 'Advanced',
  category: 'Real-time Systems',
  estimatedTime: '~30 mins',
  nodeCount: 10,
  stepCount: 10,
  icon: 'Car',
  color: '#000000',
  tags: ['WebSockets', 'Maps API', 'Real-time', 'Matching', 'Payments'],
  steps: [
    {
      id: 1,
      title: 'Start with the Client',
      explanation: "Uber has two clients: the rider app and the driver app. Both are mobile-first and require real-time GPS updates, push notifications, and live map rendering.",
      action: 'Press ⌘K, search for "Mobile", and add it to the canvas.',
      searchHint: 'Mobile',
      why: "Every architecture starts from the user. Uber's dual-client model (rider + driver) shapes every downstream architectural decision.",
      messages: [
        { type: 'guide', content: "Welcome to the Uber Architecture tutorial. We're building a real-time ride matching system used by 25 million trips per day." },
        { type: 'guide', content: "Uber has two mobile clients — the rider app and the driver app. Both stream live GPS data and receive real-time updates. This dual-client model shapes the entire architecture." },
        { type: 'guide', content: 'Press ⌘K, search for "Mobile", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_mobile'],
        requiredEdges: [],
        successMessage: "Client added. Now let's add the entry layer.",
        errorMessage: 'Add a Mobile Client node to the canvas first.',
      },
      openingMessage: "Uber has two mobile clients — rider and driver. How does that dual-client model affect the architecture?",
      defaultSuggestions: ["What does the rider app do?", "How does the driver app differ?", "What comes after the client?"],
    },
    {
      id: 2,
      title: 'Add the API Gateway',
      explanation: "All requests from both rider and driver apps flow through a single API Gateway. It handles authentication, rate limiting, and routes requests to the correct backend service.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas.',
      searchHint: 'API Gateway',
      why: "Uber has dozens of microservices. The API Gateway is the single entry point that abstracts this complexity and enforces security policies consistently across both apps.",
      messages: [
        { type: 'guide', content: "Both the rider and driver apps need a single entry point into the backend. That's the API Gateway." },
        { type: 'guide', content: "It handles auth, rate limiting, and routes requests to the right service — matching, location, payments, etc. Without it, both apps would need to know the address of every backend service." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [],
        successMessage: "API Gateway added. Now distribute the traffic.",
        errorMessage: 'Add an API Gateway node to the canvas.',
      },
      openingMessage: "The API Gateway is the single entry point for both rider and driver apps. What does it protect against?",
      defaultSuggestions: ["What is rate limiting?", "How does Uber handle auth?", "Why not connect directly to services?"],
    },
    {
      id: 3,
      title: 'Add Load Balancer',
      explanation: "Uber's API Gateway routes to a Load Balancer that distributes requests across hundreds of application servers. During surge events, traffic can spike 10x in seconds.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.',
      searchHint: 'Load Balancer',
      why: "Uber sees massive traffic spikes during rush hour, bad weather, and events. The load balancer enables horizontal scaling so no single server becomes a bottleneck.",
      messages: [
        { type: 'guide', content: "During New Year's Eve, Uber sees 10x normal traffic in minutes. A single server can't handle that." },
        { type: 'guide', content: "The Load Balancer distributes requests across hundreds of servers. It also enables zero-downtime deployments — servers can be updated one at a time." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [],
        successMessage: "Load balancer added. Now the auth layer.",
        errorMessage: 'Add a Load Balancer node to the canvas.',
      },
      openingMessage: "Uber sees 10x traffic spikes during surge events. How does the load balancer handle that?",
      defaultSuggestions: ["What is auto-scaling?", "How does Uber handle traffic spikes?", "What is a health check?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: "Every request from riders and drivers must be authenticated. The Auth Service validates JWT tokens, checks account status, and enforces per-user rate limits.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas.',
      searchHint: 'Auth Service',
      why: "Without auth, anyone could request rides or accept trips as a driver. Auth is also how Uber enforces driver background check status and rider payment method validity.",
      messages: [
        { type: 'guide', content: "Before any ride request reaches the matching engine, the user must be authenticated. Add an Auth Service." },
        { type: 'guide', content: "The Auth Service validates JWT tokens, checks that the driver has passed background checks, and confirms the rider has a valid payment method on file." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [],
        successMessage: "Auth layer added. Now the core matching engine.",
        errorMessage: 'Add an Auth Service node to the canvas.',
      },
      openingMessage: "Auth is critical for both security and trust. How does Uber verify driver background checks at request time?",
      defaultSuggestions: ["What is a JWT token?", "How does Uber verify drivers?", "What happens if auth fails?"],
    },
    {
      id: 5,
      title: 'Add Matching Service',
      explanation: "The Matching Service is Uber's core algorithm. It uses a geohash grid system, dividing the world into cells. Drivers in nearby cells are candidates for matching, scored by distance, driver rating, and acceptance rate.",
      action: 'Press ⌘K, search for "Microservice", and add it to the canvas.',
      searchHint: 'Microservice',
      why: "The matching algorithm is Uber's core IP. Isolating it as a microservice means it can be updated, A/B tested, and scaled independently from auth, payments, or location.",
      messages: [
        { type: 'guide', content: "The heart of Uber is the Matching Service — the algorithm that connects riders to drivers in seconds." },
        { type: 'guide', content: "It uses a geohash grid: the world is divided into cells. Drivers in nearby cells are candidates. Each candidate is scored by distance, rating, and acceptance rate. The best match gets the trip." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Matching Service added. Now the location layer.",
        errorMessage: 'Add a Microservice (Matching Service) to the canvas.',
      },
      openingMessage: "Uber's matching algorithm uses geohash grids. How does that make finding nearby drivers fast?",
      defaultSuggestions: ["What is a geohash?", "How does Uber score driver candidates?", "What is surge pricing?"],
    },
    {
      id: 6,
      title: 'Add Location Service',
      explanation: "Driver GPS pings every 4 seconds. At 5 million active drivers, that's 1.25 million location updates per second. The Location Service ingests, stores, and serves this real-time GPS data.",
      action: 'Press ⌘K, search for "Microservice", and add another one to the canvas.',
      searchHint: 'Microservice',
      why: "Location data is the lifeblood of Uber. Without real-time driver positions, the matching algorithm can't function. The Location Service must handle extreme write throughput.",
      messages: [
        { type: 'guide', content: "Every active driver pings their GPS location every 4 seconds. At 5 million active drivers, that's 1.25 million writes per second." },
        { type: 'guide', content: "The Location Service ingests all these pings, stores them in a geospatial index, and serves them to the Matching Service. It's one of the highest-throughput services in Uber's stack." },
        { type: 'guide', content: 'Press ⌘K, search for "Microservice", and add another one to the canvas for the Location Service.' },
      ],
      validation: {
        requiredNodes: ['microservice'],
        requiredEdges: [],
        successMessage: "Location Service added. Now the mapping layer.",
        errorMessage: 'Add a second Microservice (Location Service) to the canvas.',
      },
      openingMessage: "1.25 million GPS updates per second — how does the Location Service handle that write throughput?",
      defaultSuggestions: ["What is a geospatial index?", "How does Uber store GPS data?", "What database handles location data?"],
    },
    {
      id: 7,
      title: 'Add Maps API',
      explanation: "Uber uses a Maps API for routing, ETA calculation, and turn-by-turn navigation. ETA calculation runs 100+ routing algorithms simultaneously to find the optimal path.",
      action: 'Press ⌘K, search for "Maps API", and add it to the canvas.',
      searchHint: 'Maps API',
      why: "Accurate ETAs are critical for rider trust and driver efficiency. Uber built their own mapping system (acquired deCarta) but also integrates with Google Maps as a fallback.",
      messages: [
        { type: 'guide', content: "When Uber shows you '4 minutes away', that ETA comes from the Maps API — not a guess." },
        { type: 'guide', content: "The Maps API runs 100+ routing algorithms simultaneously, factoring in real-time traffic, road closures, and historical patterns. Uber actually built their own mapping system but uses Google Maps as fallback." },
        { type: 'guide', content: 'Press ⌘K, search for "Maps API", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['maps_api'],
        requiredEdges: [],
        successMessage: "Maps API added. Now the async event layer.",
        errorMessage: 'Add a Maps API node to the canvas.',
      },
      openingMessage: "Uber runs 100+ routing algorithms per ETA calculation. Why so many?",
      defaultSuggestions: ["How does Uber calculate ETAs?", "What is routing optimization?", "Did Uber build their own maps?"],
    },
    {
      id: 8,
      title: 'Add Message Queue',
      explanation: "When a trip completes, 15+ downstream services need to be notified — billing, driver stats, rating prompts, analytics, fraud detection. A Message Queue decouples all of these from the trip completion event.",
      action: 'Press ⌘K, search for "Message Queue", and add it to the canvas.',
      searchHint: 'Message Queue',
      why: "Without a queue, the trip completion flow would need to call 15 services synchronously. If any one fails, the entire flow fails. The queue makes the system resilient.",
      messages: [
        { type: 'guide', content: "When a trip ends, 15+ things need to happen: charge the rider, update driver earnings, prompt for ratings, log analytics, run fraud checks..." },
        { type: 'guide', content: "A Message Queue decouples all of this. The trip service publishes one 'trip_completed' event. Every downstream service subscribes and reacts independently at its own pace." },
        { type: 'guide', content: 'Press ⌘K, search for "Message Queue", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['message_queue'],
        requiredEdges: [],
        successMessage: "Message Queue added. Now the data layer.",
        errorMessage: 'Add a Message Queue node to the canvas.',
      },
      openingMessage: "15 services need to react when a trip ends. How does a message queue prevent any one failure from cascading?",
      defaultSuggestions: ["What is async processing?", "How does Uber handle payment failures?", "What events does Uber publish?"],
    },
    {
      id: 9,
      title: 'Add SQL Database',
      explanation: "Uber stores trip records, user accounts, driver profiles, and payment history in a SQL database. Trip data requires ACID transactions — a completed trip must atomically update the rider's payment and driver's earnings.",
      action: 'Press ⌘K, search for "SQL Database", and add it to the canvas.',
      searchHint: 'SQL Database',
      why: "Payment and trip data requires strong consistency. If Uber charges a rider but fails to record the trip, that's a financial and legal problem. SQL's ACID guarantees prevent this.",
      messages: [
        { type: 'guide', content: "Trip records and payments require strong consistency — you can't charge a rider without recording the trip." },
        { type: 'guide', content: "SQL's ACID transactions guarantee that charging the rider and crediting the driver happen atomically. Either both succeed or neither does. NoSQL's eventual consistency is the wrong choice here." },
        { type: 'guide', content: 'Press ⌘K, search for "SQL Database", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['sql_db'],
        requiredEdges: [],
        successMessage: "SQL Database added. Final step — observability.",
        errorMessage: 'Add a SQL Database node to the canvas.',
      },
      openingMessage: "Uber uses SQL for trip records. Why does payment data specifically require ACID transactions?",
      defaultSuggestions: ["What are ACID transactions?", "How does Uber store trip history?", "Why not use NoSQL for payments?"],
    },
    {
      id: 10,
      title: 'Add Observability',
      explanation: "At Uber's scale, something is always failing somewhere. A Metrics Collector and Logger let engineers monitor matching latency, location update throughput, and payment success rates in real time.",
      action: 'Press ⌘K, search for "Metrics Collector", and add it to the canvas. Then search for "Logger" and add that too.',
      searchHint: 'Metrics Collector',
      why: "Without observability, a 1% drop in matching success rate could go undetected for hours. At 25 million daily trips, that's 250,000 failed rides.",
      messages: [
        { type: 'guide', content: "Final step — observability. At 25 million daily trips, even a 1% failure rate is 250,000 failed rides." },
        { type: 'guide', content: "The Metrics Collector tracks matching latency, GPS update throughput, and payment success rates. The Logger captures detailed traces for debugging specific failures." },
        { type: 'guide', content: 'Press ⌘K, search for "Metrics Collector", add it. Then search for "Logger" and add that too.' },
      ],
      validation: {
        requiredNodes: ['metrics_collector', 'logger'],
        requiredEdges: [],
        successMessage: "🎉 Tutorial complete! You've designed Uber's core architecture from scratch.",
        errorMessage: 'Add both Metrics Collector and Logger to the canvas.',
      },
      openingMessage: "A 1% matching failure rate at Uber's scale is 250,000 failed rides. How does observability catch that fast?",
      defaultSuggestions: ["What metrics does Uber track?", "What is p99 latency?", "How does Uber detect outages?"],
    },
  ],
};
