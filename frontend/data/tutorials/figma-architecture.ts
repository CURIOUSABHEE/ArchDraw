import type { TutorialData } from './index';

export const figmaTutorial: TutorialData = {
  id: 'figma-architecture',
  title: 'How to Design Figma Architecture',
  description: 'Build a collaborative design tool for 4 million teams. Learn CRDTs for conflict-free simultaneous editing, presence awareness, canvas rendering, and version history at scale.',
  difficulty: 'Advanced',
  category: 'Collaboration Tools',
  estimatedTime: '~25 mins',
  nodeCount: 10,
  stepCount: 10,
  icon: 'PenTool',
  color: '#f24e1e',
  tags: ['CRDTs', 'Collaboration', 'Canvas', 'Presence'],
  steps: [
    {
      id: 1,
      title: 'Start with the Web Client',
      explanation: "Figma runs entirely in the browser using WebAssembly. The client renders the design canvas, handles user input, and syncs changes with the server. It's one of the most complex browser applications ever built.",
      action: 'Press ⌘K, search for "Web", and add it to the canvas.',
      searchHint: 'Web',
      why: "Figma's web client is remarkable — it renders a full design canvas in the browser using WebGL for GPU-accelerated rendering. Most design tools required native apps; Figma proved the browser was powerful enough.",
      messages: [
        { type: 'guide', content: "Welcome to the Figma Architecture tutorial. 4 million design teams, simultaneous editing, and a CRDT implementation so good that two designers can move the same element at the same time without conflict." },
        { type: 'guide', content: "Figma's biggest technical achievement: solving the simultaneous edit problem. When two designers move the same element at the same time, neither edit is lost. They use CRDTs — Conflict-free Replicated Data Types." },
        { type: 'guide', content: 'Press ⌘K, search for "Web", and add it to the canvas.' },
      ],
      validation: {
        requiredNodes: ['client_web'],
        requiredEdges: [],
        successMessage: "Web Client added. Now the API layer.",
        errorMessage: 'Add a Web Client node to the canvas first.',
      },
      openingMessage: "Figma runs in the browser using WebAssembly and WebGL. What makes browser-based rendering competitive with native apps?",
      defaultSuggestions: ["How does Figma use WebAssembly?", "What is WebGL rendering?", "Why did Figma choose the browser over native?"],
    },
    {
      id: 2,
      title: 'Add API Gateway',
      explanation: "Figma's API Gateway handles file loading, asset uploads, plugin API calls, and REST API requests. Real-time collaboration traffic goes through WebSockets, but file operations and asset management go through the gateway.",
      action: 'Press ⌘K, search for "API Gateway", and add it to the canvas. Then connect Client → API Gateway.',
      searchHint: 'API Gateway',
      why: "Figma separates real-time collaboration (WebSocket) from file operations (REST). The API Gateway handles the REST side — loading files, saving versions, managing permissions, and serving the plugin API.",
      messages: [
        { type: 'guide', content: "Figma has two communication channels: WebSockets for real-time edits, and REST API for file operations." },
        { type: 'guide', content: "The API Gateway handles the REST side: loading a file, saving a version, inviting a collaborator, or calling a plugin API. Real-time edit operations bypass the gateway and go directly to the collaboration server via WebSocket." },
        { type: 'guide', content: 'Press ⌘K, search for "API Gateway", add it, then connect: Client → API Gateway.' },
      ],
      validation: {
        requiredNodes: ['api_gateway'],
        requiredEdges: [{ from: 'Web', to: 'API Gateway' }],
        successMessage: "API Gateway added and connected. Now distribute the load.",
        errorMessage: 'Add an API Gateway and connect it from the Client.',
      },
      openingMessage: "Figma uses WebSockets for real-time edits and REST for file operations. Connect Client → API Gateway — all REST traffic flows here.",
      defaultSuggestions: ["What is the difference between WebSocket and REST?", "How does Figma load files?", "What is the Figma plugin API?"],
    },
    {
      id: 3,
      title: 'Add Load Balancer',
      explanation: "Figma's Load Balancer routes collaboration sessions to the correct server. All users editing the same file must be routed to the same collaboration server — this is sticky routing based on file ID.",
      action: 'Press ⌘K, search for "Load Balancer", and add it to the canvas. Then connect API Gateway → Load Balancer.',
      searchHint: 'Load Balancer',
      why: "Sticky routing by file ID ensures all collaborators on the same file connect to the same server. This is essential for the CRDT engine — all edits must flow through a single point of coordination.",
      messages: [
        { type: 'guide', content: "Figma's load balancer uses sticky routing — all users editing the same file go to the same server." },
        { type: 'guide', content: "If Alice and Bob are both editing 'Homepage Design', they must connect to the same collaboration server. The load balancer hashes the file ID to determine which server handles that file. This ensures the CRDT engine sees all edits in one place." },
        { type: 'guide', content: 'Press ⌘K, search for "Load Balancer", add it, then connect: API Gateway → Load Balancer.' },
      ],
      validation: {
        requiredNodes: ['load_balancer'],
        requiredEdges: [{ from: 'API Gateway', to: 'Load Balancer' }],
        successMessage: "Load Balancer added and connected. Now authentication.",
        errorMessage: 'Add a Load Balancer and connect it from the API Gateway.',
      },
      openingMessage: "Figma routes all collaborators on the same file to the same server. Connect API Gateway → Load Balancer using file-ID-based sticky routing.",
      defaultSuggestions: ["What is sticky routing?", "How does Figma handle server failures?", "What is file-based sharding?"],
    },
    {
      id: 4,
      title: 'Add Auth Service',
      explanation: "Figma's Auth Service handles user authentication and file permissions. It enforces access control — can this user view, edit, or comment on this file? Permissions are checked on every WebSocket connection and every API call.",
      action: 'Press ⌘K, search for "Auth Service", and add it to the canvas. Then connect Load Balancer → Auth Service.',
      searchHint: 'Auth Service',
      why: "Figma files can be public, team-only, or private. The Auth Service enforces these permissions at the connection level — a viewer can't send edit operations even if they bypass the UI.",
      messages: [
        { type: 'guide', content: "Figma's auth enforces permissions at the connection level — not just the UI." },
        { type: 'guide', content: "When you connect to a Figma file via WebSocket, the Auth Service checks your permission level. Viewers get a read-only connection. Editors get a read-write connection. Even if a viewer modifies the client code, the server rejects edit operations from viewer-level connections." },
        { type: 'guide', content: 'Press ⌘K, search for "Auth Service", add it, then connect: Load Balancer → Auth Service.' },
      ],
      validation: {
        requiredNodes: ['auth_service'],
        requiredEdges: [{ from: 'Load Balancer', to: 'Auth' }],
        successMessage: "Auth Service added and connected. Now the heart of Figma — the CRDT engine.",
        errorMessage: 'Add an Auth Service and connect it from the Load Balancer.',
      },
      openingMessage: "Figma enforces permissions at the WebSocket connection level. Connect Load Balancer → Auth Service — every session is permission-checked here.",
      defaultSuggestions: ["What is permission enforcement?", "How does Figma handle view-only access?", "What is server-side validation?"],
    },
    {
      id: 5,
      title: 'Add CRDT Engine',
      explanation: "The CRDT Engine is Figma's core innovation. CRDTs (Conflict-free Replicated Data Types) allow multiple users to edit the same document simultaneously without conflicts. Every edit is represented as an operation that can be merged with any other operation in any order.",
      action: 'Press ⌘K, search for "CRDT Engine", and add it to the canvas. Then connect Auth Service → CRDT Engine.',
      searchHint: 'CRDT Engine',
      why: "Without CRDTs, simultaneous edits require a lock — only one user can edit at a time. CRDTs eliminate locks entirely. Two users can move the same element simultaneously and both edits are preserved without either user waiting.",
      messages: [
        { type: 'guide', content: "The CRDT Engine is what makes Figma's collaboration feel instant. No locks, no conflicts, no 'waiting for other user'." },
        { type: 'guide', content: "When Alice moves a rectangle to (100, 200) and Bob simultaneously moves it to (300, 400), the CRDT engine merges both operations. The result is deterministic — both clients converge to the same final state. Figma uses a last-write-wins CRDT for position, which means the later timestamp wins." },
        { type: 'guide', content: 'Press ⌘K, search for "CRDT Engine", add it, then connect: Auth Service → CRDT Engine.' },
      ],
      validation: {
        requiredNodes: ['crdt_engine'],
        requiredEdges: [{ from: 'Auth', to: 'CRDT' }],
        successMessage: "CRDT Engine added and connected. Now presence awareness.",
        errorMessage: 'Add a CRDT Engine and connect it from the Auth Service.',
      },
      openingMessage: "CRDTs allow simultaneous edits without locks. Connect Auth Service → CRDT Engine — authenticated edit operations flow here for conflict resolution.",
      defaultSuggestions: ["What is a CRDT?", "How does last-write-wins work?", "What is operational transformation?"],
    },
    {
      id: 6,
      title: 'Add Presence Service',
      explanation: "The Presence Service tracks who is currently in a file, where their cursor is, and what they're selecting. This is what powers the colored cursors and 'Alice is viewing this frame' indicators in Figma.",
      action: 'Press ⌘K, search for "Presence Service", and add it to the canvas. Then connect CRDT Engine → Presence Service.',
      searchHint: 'Presence Service',
      why: "Presence data is ephemeral and high-frequency — cursor positions update 60 times per second. It's stored in memory, not a database. The Presence Service broadcasts cursor positions to all collaborators in real-time.",
      messages: [
        { type: 'guide', content: "The Presence Service powers the colored cursors — you can see exactly where your teammates are looking in real-time." },
        { type: 'guide', content: "Cursor positions update up to 60 times per second. Storing this in a database would be catastrophic. The Presence Service keeps cursor state in memory and broadcasts updates via WebSocket. When a user disconnects, their cursor disappears within 1 second." },
        { type: 'guide', content: 'Press ⌘K, search for "Presence Service", add it, then connect: CRDT Engine → Presence Service.' },
      ],
      validation: {
        requiredNodes: ['presence_service'],
        requiredEdges: [{ from: 'CRDT', to: 'Presence' }],
        successMessage: "Presence Service added and connected. Now the canvas renderer.",
        errorMessage: 'Add a Presence Service and connect it from the CRDT Engine.',
      },
      openingMessage: "Cursor positions update 60 times per second. Connect CRDT Engine → Presence Service — the CRDT engine propagates position changes to presence.",
      defaultSuggestions: ["How does Figma show live cursors?", "What is presence awareness?", "How does Figma handle user disconnect?"],
    },
    {
      id: 7,
      title: 'Add Canvas Renderer',
      explanation: "Figma's Canvas Renderer uses WebGL to render the design canvas at 60fps. It handles zoom, pan, layer rendering, and vector graphics. The renderer runs on the client but the server maintains the authoritative canvas state.",
      action: 'Press ⌘K, search for "Canvas Renderer", and add it to the canvas. Then connect CRDT Engine → Canvas Renderer.',
      searchHint: 'Canvas Renderer',
      why: "WebGL gives Figma GPU-accelerated rendering. A complex design with thousands of layers renders at 60fps because the GPU handles the compositing. CPU-based rendering would be too slow for large files.",
      messages: [
        { type: 'guide', content: "Figma's Canvas Renderer uses WebGL for GPU-accelerated rendering — that's why it stays smooth even with thousands of layers." },
        { type: 'guide', content: "The renderer receives the document state from the CRDT engine and renders it using WebGL. When you zoom in, the renderer re-renders at higher resolution. When a collaborator moves an element, the CRDT engine updates the state and the renderer re-draws that element." },
        { type: 'guide', content: 'Press ⌘K, search for "Canvas Renderer", add it, then connect: CRDT Engine → Canvas Renderer.' },
      ],
      validation: {
        requiredNodes: ['canvas_renderer'],
        requiredEdges: [{ from: 'CRDT', to: 'Canvas' }],
        successMessage: "Canvas Renderer added and connected. Now version history.",
        errorMessage: 'Add a Canvas Renderer and connect it from the CRDT Engine.',
      },
      openingMessage: "The Canvas Renderer gets its state from the CRDT engine. Connect CRDT Engine → Canvas Renderer — the authoritative document state drives rendering.",
      defaultSuggestions: ["What is WebGL?", "How does Figma render vector graphics?", "What is GPU-accelerated rendering?"],
    },
    {
      id: 8,
      title: 'Add Version History',
      explanation: "Figma's Version History stores snapshots of the document at regular intervals and whenever a user manually saves a version. You can revert to any previous version. Figma stores the full document state, not just diffs.",
      action: 'Press ⌘K, search for "Version History", and add it to the canvas. Then connect CRDT Engine → Version History.',
      searchHint: 'Version History',
      why: "Storing full snapshots (not diffs) makes version restore instant — no need to replay operations. The tradeoff is storage cost, but Figma compresses snapshots aggressively.",
      messages: [
        { type: 'guide', content: "Figma stores full document snapshots for version history — not diffs. Restoring a version is instant." },
        { type: 'guide', content: "Every 30 minutes, Figma saves an automatic version. Users can also save named versions ('Before client review'). Each version is a compressed snapshot of the full document. Restoring is instant because there's no diff replay — just load the snapshot." },
        { type: 'guide', content: 'Press ⌘K, search for "Version History", add it, then connect: CRDT Engine → Version History.' },
      ],
      validation: {
        requiredNodes: ['version_history'],
        requiredEdges: [{ from: 'CRDT', to: 'Version' }],
        successMessage: "Version History added and connected. Now the storage layer.",
        errorMessage: 'Add a Version History and connect it from the CRDT Engine.',
      },
      openingMessage: "Figma stores full snapshots instead of diffs. Connect CRDT Engine → Version History — the CRDT state gets snapshotted here periodically.",
      defaultSuggestions: ["What is snapshot vs diff versioning?", "How does Figma compress documents?", "How does version restore work?"],
    },
    {
      id: 9,
      title: 'Add Object Storage',
      explanation: "Figma stores design files, version snapshots, exported assets, and uploaded images in Object Storage. A large Figma file can be hundreds of megabytes — object storage handles this at any scale.",
      action: 'Press ⌘K, search for "Object Storage", and add it to the canvas. Then connect Version History → Object Storage.',
      searchHint: 'Object Storage',
      why: "Design files are binary blobs — not structured data. Object storage is the right tool: infinitely scalable, cheap per GB, and accessible via URL for direct client downloads.",
      messages: [
        { type: 'guide', content: "Figma files are stored as binary blobs in Object Storage — not in a database." },
        { type: 'guide', content: "When you open a Figma file, the client downloads the binary file directly from Object Storage via a signed URL. The CRDT engine then applies any pending operations on top of the stored state. This is faster than serving files through the API server." },
        { type: 'guide', content: 'Press ⌘K, search for "Object Storage", add it, then connect: Version History → Object Storage.' },
      ],
      validation: {
        requiredNodes: ['object_storage'],
        requiredEdges: [{ from: 'Version', to: 'Object' }],
        successMessage: "Object Storage added and connected. Final step — the cache layer.",
        errorMessage: 'Add an Object Storage and connect it from the Version History.',
      },
      openingMessage: "Figma serves files via signed URLs directly from Object Storage. Connect Version History → Object Storage — version snapshots are persisted here.",
      defaultSuggestions: ["What is a signed URL?", "How does Figma load large files?", "What is object storage?"],
    },
    {
      id: 10,
      title: 'Add In-Memory Cache',
      explanation: "Figma caches active document state, user permissions, and team metadata in an in-memory cache. Active files are kept hot in memory so collaborators can join instantly without loading from Object Storage.",
      action: 'Press ⌘K, search for "In-Memory Cache", and add it to the canvas. Then connect CRDT Engine → In-Memory Cache.',
      searchHint: 'In-Memory Cache',
      why: "When 10 designers are actively editing a file, loading it from Object Storage on every connection would be slow. The cache keeps the active document state in memory so new collaborators join instantly.",
      messages: [
        { type: 'guide', content: "Active Figma files are kept in memory — when you join a file that others are editing, you get the live state instantly." },
        { type: 'guide', content: "The In-Memory Cache stores the current document state for active files. When you join a file, the server sends you the cached state plus any pending CRDT operations. When the last collaborator leaves, the state is flushed to Object Storage and evicted from cache." },
        { type: 'guide', content: 'Press ⌘K, search for "In-Memory Cache", add it, then connect: CRDT Engine → In-Memory Cache.' },
      ],
      validation: {
        requiredNodes: ['in_memory_cache'],
        requiredEdges: [{ from: 'CRDT', to: 'Cache' }],
        successMessage: "🎉 Tutorial complete! You've designed Figma's core collaborative architecture from scratch.",
        errorMessage: 'Add an In-Memory Cache and connect it from the CRDT Engine.',
      },
      openingMessage: "Figma keeps active files in memory. Connect CRDT Engine → In-Memory Cache — the live document state is cached here for new collaborator joins.",
      defaultSuggestions: ["How does Figma manage active file state?", "What is cache eviction?", "How does Figma handle the last user leaving?"],
    },
  ],
};
